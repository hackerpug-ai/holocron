## Borg Assimilation Assignment — SHARD {shard_id}

ROOT:  {root}
SHARD: {shard_key}
FILES ({n}) — you own every one of these:
{path_list}

READ EVERY FILE LISTED. Not a sample.

Return ONE JSON object to {returns_path} with findings[] (claim, path, line, evidence verbatim, kind)
and receipts[] (path, lines, opening_quote) so every listed file appears in one or the other.
Quote ONE complete distinctive source line. Use the FULL path from the list.
Then reply with only: the path, files_read count, findings count, sophistication.
