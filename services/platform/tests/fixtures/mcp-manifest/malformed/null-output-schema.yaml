# Malformed manifest: output_schema is null for get_research_session
# Used by mcp-verify-manifest-field-validation.test.ts
protocol: "2025-11-25"
transports:
  - stdio
  - streamable-http
stateless: true
no_server_sampling: true
auth_policy:
  stdio:
    trust_boundary: "Local process"
  streamable_http:
    type: "api-key"
    header: "Authorization"
    scheme: "bearer"
    origin_validation: true
cancellation_policy:
  posture: "cooperative"
  supported: true
tools:
  - id: get_research_session
    input_schema:
      type: object
      properties:
        sessionId:
          type: string
          minLength: 1
      required: [sessionId]
    output_schema: null
    defaults: {}
    errors:
      - code: NOT_FOUND
        description: "Session does not exist"
      - code: INTERNAL_SERVER_ERROR
        description: "Convex query failed"
    pagination: null
    side_effects: null
    idempotency: null
    replay: null
    transports: [stdio, streamable-http]
    fixtures: null
