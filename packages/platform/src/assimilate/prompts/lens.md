## Borg Assimilation Assignment — LENS {lens}

ROOT: {root}
Read across the whole manifest through the {lens} lens.
Same JSON return contract as shard crawlers. Write to {returns_path}.
Evidence must be verbatim from files on disk.
