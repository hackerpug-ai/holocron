# GATE-FIX-S28R3-QA7 — Committed non-secret fixture for credential-inventory unit contracts.
#
# Purpose: self-contained always-on inventory tests without personal secrets.yaml.
# Contract:
#   - NO real secrets
#   - R2_RESTORE_* intentionally ABSENT (or empty) so residual is DEPENDENCY-S28-R2-RO
#   - MUST NOT be accepted as live restore credentials by REQUIRE_LIVE_R2_RO paths
#   - inventory-restore-credentials.sh emits presence/length only (never values)
#
# Safe placeholder shapes only.

R2_BUCKET_NAME: holocron-backup
R2_ENDPOINT: https://example.invalid.r2.cloudflarestorage.com
R2_ACCOUNT_ID: ""

# Writer-shaped keys may appear as empty/absent so inventory can report lengths;
# they are not live credentials and must not satisfy restore-only live gates.
R2_ACCESS_KEY_ID: ""
R2_SECRET_ACCESS_KEY: ""

# Restore keys deliberately omitted — inventory residual DEPENDENCY-S28-R2-RO
# R2_RESTORE_ACCESS_KEY_ID:
# R2_RESTORE_SECRET_ACCESS_KEY:
