#!/usr/bin/env sh

# 1. Lint staged files (eslint --fix on changed .ts/.tsx/.js/.jsx files)
pnpm exec lint-staged

# 2. TypeScript type-check (full project — catches cross-file breakage)
pnpm tsc --noEmit

# 3. Tests
pnpm test
