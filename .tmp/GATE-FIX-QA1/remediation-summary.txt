GATE-FIX-QA1 technical remediation: remove early pg_promote mid-PITR

Blocking finding T-PROMOTE-EARLY fixed:
- tryStartPostgres no longer calls SELECT pg_promote(...) after ~30s reachable
  or as last-chance after poll budget.
- Promote path accepts started:true only when probeRecoveryState reports
  reachable && inRecovery === false (natural recovery_target_action=promote).
- Pause path accepts started:true only when reachable && inRecovery === true.
- Kept: classifyPostgresStartFailure / mapPostgresStartFailureNamedErrors,
  short /tmp socket dir, -t 120, tryStop before start, poll budgets.
- Optional: selectBestFireDrillBaseline ranks meaningful survivors by
  target_timestamp desc, then domain row totals as tiebreaker.

Verification:
- PLATFORM_IT=1 vitest: sprint28-gate-fix-qa1 + sprint28-fire-drill-baseline-consume
  → 11 passed
- pnpm tsgo --noEmit → EXIT 0
- pnpm biome check services/platform/src/backup/ → clean
