{
  "lens": "product",
  "verdict": "APPROVED",
  "requirements": [
    {
      "id": "AC-1",
      "satisfied": true,
      "notes": "Step1 fail 20260729T042338Z was sole generic 'Postgres failed to start' + truncated archive-get with wipe. Product now maps recovery-ended-before-target / archive-get WAL exhaustion via classifyPostgresStartFailure + mapPostgresStartFailureNamedErrors into gate-matching 'outside available WAL' language on namedErrors; tryStartPostgres aligns with fire-drill (stale stop, -t 120, settle, promote nudge, longer poll). Gate-plan untouched. Does not weaken empty/corrupt-chain fail-closed. GREEN: TC-1a/b/c pure classification + mapping."
    },
    {
      "id": "AC-2",
      "satisfied": true,
      "notes": "Observed step3 junk baseline had all-zero row_counts and ghost restic prefix resticc5ms5egca88d4616ab. captureAndUploadRecoveryBaseline now verifies restic via restic snapshots --json before upload (refuse ok:false/uploaded:false); buildRecoveryBaseline re-captures domain counts when supplied totals are zero. Hooks (emitBaseBackup / bindRestic) go through capture path. Live TC-4: fake id refused against 17 real snapshots. skipResticVerify reserved for synthetic suite only."
    },
    {
      "id": "AC-3",
      "satisfied": true,
      "notes": "resolveFireDrillBaseline no longer sorts solely by target_timestamp (which selected newer zero-count junk over valid older baseline). selectBestFireDrillBaseline + isBaselineParityMeaningful prefer non-zero domain + restic id length, requireMeaningful fail-closed on junk-only sets with explicit error. Prevents POSTGRES_PARITY_PASS=false solely from expected zeros while restored rows match live domain. Steps 4–5 inherit truthful parity only when a meaningful baseline binds. Residual: discovery does not live-list restic at select time (length+counts only); emit refuse closes new ghosts; pre-existing non-zero+ghost would fail blob honestly, not false-pass."
    },
    {
      "id": "AC-4",
      "satisfied": true,
      "notes": "Diff limited to services/platform/src/backup/{restore,recovery-baseline,fire-drill,index}.ts + sprint28 tests. Evidence: biome check backup/ exit 0; tsgo --noEmit exit 0 (green-summary / SUMMARY)."
    }
  ],
  "findings": [
    {
      "severity": "LOW",
      "id": "P-DISC-RESTIC-LIVE",
      "summary": "Discovery scores restic via id length, not live restic snapshots verify; acceptable for observed zero-count+ghost failure given emit-time refuse and fail-closed junk-only.",
      "blocks_approval": false
    },
    {
      "severity": "INFO",
      "id": "P-E2E-HUMAN-GATE",
      "summary": "Product path is honest for CAP-BAK-01; full human-gate re-pass is out of this cycle per task (land only). Classification + emit refuse + selection are proven; orchestrator re-run remains the ship proof for end-to-end steps 1–5.",
      "blocks_approval": false
    }
  ],
  "summary": "APPROVED. GATE-FIX-QA1 product path addresses real QA fail 20260729T042338Z without weakening gate-plan: (1) promote start maps incomplete recovery/WAL exhaustion to named outside-available-WAL errors and aligns start/promote with fire-drill so in-window restores can become queryable; (2) baseline emit refuses ghost restic ids and re-captures non-zero domain counts; (3) fire-drill discovery prefers parity-meaningful baselines and fails closed on zero-count-only junk so steps 3–5 cannot false-fail POSTGRES_PARITY from stale zeros while restored state is real. Feature-forward honesty toward CAP-BAK-01 is preserved (fail closed, no assertion dilute, no hand-written gate results). All four ACs satisfied from product/spec lens."
}
