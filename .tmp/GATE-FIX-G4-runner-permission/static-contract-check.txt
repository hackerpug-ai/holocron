GATE-FIX-G4 runner-permission follow-up

Scope
-----
Workflow-only remediation for the observed GitHub runners API 403. The sole
workflow change is `permissions.actions: read` alongside the existing
`permissions.contents: read` in `.github/workflows/ci-e2e.yml`.

Focused checks
--------------
Command: `pnpm exec vitest run tests/ci/fork-safety.test.ts`
Result: PASS — 1 test file passed; 4 tests passed.

Command: direct YAML contract assertion over `.github/workflows/ci-e2e.yml`
Result: PASS — 8 checks passed:
- permissions.contents
- permissions.actions
- self-hosted, holocron, e2e runner labels
- pnpm 9.15.4 pin
- github.token / github.repository runner-status wiring
- maestro-reference-flow-${{ github.run_id }} artifact naming
- always() artifact upload
- all action references SHA pinned

Command: `git diff --check`
Result: PASS.

Actionlint
----------
Unavailable in this environment (`actionlint: unavailable`).

Evidence boundary
-----------------
No CI dispatch was attempted. No provenance or fabricated artifact was
created. No workflow labels, pnpm pin, action SHA, secret/variable wiring,
fail-closed runner contract, or artifact upload contract was changed.
