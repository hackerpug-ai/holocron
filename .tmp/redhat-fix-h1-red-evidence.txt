REDHAT-FIX H1+H2+H3+G-ORACLE — RED EVIDENCE
================================================

Captured BEFORE implementation, proving the current code uses generateText
(not generateObject) — constrained decoding (response_format: json_schema) is
NOT delivered. This is the sprint's core value proposition.

=== rg -n 'generateText' services/platform/src/inference/extract-structured.ts ===
303:      // For local OpenAI-compatible models, use generateText with explicit JSON
305:      const { generateText } = await import('ai');
328:      const result = await generateText(

Line 305: imports generateText (should be generateObject)
Line 328: calls generateText with a prompt + manual JSON instruction, then
          manually extracts JSON via text.match(/\{[\s\S]*\}/) + JSON.parse.
          response_format: json_schema is NEVER sent on the wire.

=== rg -n 'generateText' services/platform/src/inference/probe-capability.ts ===
88:  // Use generateText with explicit JSON instruction (more reliable for local models)
89:  const { generateText } = await import('ai');
102:    const result = await generateText({

Line 89: probe imports generateText (should be generateObject)
Line 102: probe calls generateText then manually parses — so it does NOT
          actually detect whether the backend supports structured-output
          requests; it only checks whether the model can emit JSON-ish text.

=== rg -n 'generateObject' services/platform/src/inference/extract-structured.ts ===
(only in a comment at line 304: "more reliable than generateObject")
Real code occurrences: 0

=== rg -n 'generateObject' services/platform/src/inference/probe-capability.ts ===
(only in comments)
Real code occurrences: 0

=== Tripwire (H3) ===
extract-structured.ts lines 256-279: tripwire only scans the INPUT string.
No OUTPUT-side scan exists, so model-GENERATED sensitive content passes
through undetected.

RED STATE: extractStructured and probeJsonSchemaSupport use generateText,
not generateObject. Constrained decoding not delivered. Output tripwire absent.

==========================================================
GREEN EVIDENCE (after fix)
==========================================================

AC GREP CHECKS (all pass):
  extract-structured generateObject count: 13 (>=1)  PASS
  probe-capability generateObject count:   16 (>=1)  PASS
  extract-structured generateText count:    0 (==0)  PASS
  probe-capability generateText count:      0 (==0)  PASS

TYPECHECK (pnpm tsgo --noEmit): exit 0  PASS
LINT (pnpm biome check <both files>): exit 0  PASS

INTEGRATION TESTS (PLATFORM_IT=1, real fleet at :4545):
  Test Files: 4 passed (4)
  Tests:      30 passed (30)
  Duration:   46.33s
  PASS — struct-repair-loop, struct-explicit-fail, struct-tripwire-blocked, struct-boot-probe

CHANGES DELIVERED:
  H1 (CRITICAL): generateText → generateObject in both files. extractStructured
    now calls generateObject({model, schema, prompt, abortSignal}) — the SDK sends
    response_format: json_schema on the wire. All manual JSON string extraction
    (text.match + JSON.parse) removed.
  H2 (HIGH): mode selection consumes resolved.structuredOutput (initial guess) +
    adaptive live fallback. constrained = raw model (json_schema on wire);
    repair = model wrapped to strip response_format + schema-guided prompt.
  H3 (HIGH): output-side tripwire added. After generateObject returns, the parsed
    object is serialized (JSON.stringify) and scanned for sensitive patterns BEFORE
    Zod re-validation and success-status write. Input-side check kept (defense in depth).
  G-ORACLE (HIGH): probe uses generateObject (not generateText) — detects whether
    the role ACTUALLY honors json_schema. Cross-checked with manifest structuredOutput
    flag (manifest authoritative when false). extractStructured's repair loop also
    adapts: NoObjectGeneratedError in constrained mode → falls back to repair.

NOTE on v7 API: AI SDK v7 removed the legacy `mode: 'json'|'tool'` toggle from
  generateObject. The repair-mode behavior (prompt-level JSON without backend
  json_schema) is realized by wrapping the model in a middleware that strips
  responseFormat + experimental_repairText for markdown fences + a schema-guided
  prompt. This is required because local reasoning models (GLM-4.7) emit
  structured output via the `reasoning` channel (content=null) whenever
  response_format is set.
