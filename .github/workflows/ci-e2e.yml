# D02-05 — E2E scaffold reserved for Sprint 20 Maestro (disabled by default)
name: ci-e2e

on:
  workflow_dispatch:
  pull_request:
    paths:
      - ".github/workflows/ci-e2e.yml"

permissions:
  contents: read

concurrency:
  group: e2e-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  e2e-scaffold:
    name: Maestro e2e scaffold (Sprint 20)
    # Disabled until Sprint 20 — keep workflow present for path-aware proof
    if: ${{ false }}
    runs-on: [self-hosted, holocron, e2e]
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Placeholder
        run: echo "Sprint 20 Maestro cold-boot reference flow not yet enabled"
