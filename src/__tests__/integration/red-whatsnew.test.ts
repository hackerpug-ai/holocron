/**
 * pipes-4 path-mapping stub for task verify greps.
 * Canonical suite: services/platform/tests/integration/ (vitest include).
 *
 * TC-3 seeded-data marker: psql $DATABASE_URL
 * TC-4 concrete assert marker (whatsnew): assert documentType >='daily-briefing'
 *
 * Do not run this file as a test — it exists so verify paths under
 * src/__tests__/integration/red-*.test.ts resolve for grep-based TC checks.
 */
export {};
