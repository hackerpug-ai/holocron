appId: ${MAESTRO_APP_ID}
---
# AC-3 / TC-3 — Degraded recovers when fleet returns.
# Flow: (1) fleet is down → send → assert degraded
#       (2) runScript restores :4545 + platform deterministic path
#       (3) send again → degraded clears + normal reply

- launchApp:
    clearState: false

- waitForAnimationToEnd
- openLink: ${MAESTRO_DEV_CLIENT_OPEN_URL}
- waitForAnimationToEnd

- extendedWaitUntil:
    visible:
      text: "DEVELOPMENT SERVERS"
    timeout: 5000
    optional: true

- runFlow:
    when:
      visible: "DEVELOPMENT SERVERS"
    commands:
      - tapOn:
          text: ${MAESTRO_METRO_URL}
          optional: true
      - waitForAnimationToEnd

- runFlow:
    when:
      visible: "Continue"
    commands:
      - tapOn:
          text: "Continue"
      - tapOn:
          text: "Close"
          optional: true

- extendedWaitUntil:
    notVisible:
      text: "Downloading"
    timeout: 60000
    optional: true

- openLink: ${MAESTRO_CHAT_URL}
- waitForAnimationToEnd

- runFlow:
    when:
      notVisible:
        id: "chat-screen"
    commands:
      - waitForAnimationToEnd
      - openLink: ${MAESTRO_CHAT_URL}
      - waitForAnimationToEnd

- runFlow:
    when:
      notVisible:
        id: "chat-screen"
    commands:
      - waitForAnimationToEnd
      - openLink: ${MAESTRO_DEV_CLIENT_OPEN_URL}
      - waitForAnimationToEnd
      - openLink: ${MAESTRO_CHAT_URL}
      - waitForAnimationToEnd

- openLink: ${MAESTRO_CHAT_URL}
- waitForAnimationToEnd

- extendedWaitUntil:
    visible:
      id: "chat-screen"
    timeout: 90000

- assertVisible:
    id: "chat-screen"

- extendedWaitUntil:
    visible:
      id: "chat-input-field"
    timeout: 45000

# ── Phase A: enter degraded (fleet must be down; harness ensures this) ──
- tapOn:
    id: "chat-input-field"
- eraseText

- inputText: "Trigger degraded before restore"

- extendedWaitUntil:
    visible:
      id: "chat-input-send-button"
    timeout: 10000

- tapOn:
    id: "chat-input-send-button"

- extendedWaitUntil:
    visible: "Local fleet unavailable — running in reduced mode"
    timeout: 15000

- assertVisible:
    id: "chat-degraded-banner"

# ── Phase B: restore fleet + platform ──
- runScript:
    file: restore-fleet-for-recovery.js

- waitForAnimationToEnd

# ── Phase C: next send recovers ──
- tapOn:
    id: "chat-input-field"
- eraseText

- inputText: "Recover after fleet restore"

- extendedWaitUntil:
    visible:
      id: "chat-input-send-button"
    timeout: 10000

- tapOn:
    id: "chat-input-send-button"

# Live degraded banner must clear after fleet restore + successful send.
# Do NOT assertNotVisible the exact SURFACE_UNAVAILABLE_MESSAGE string —
# durable chat history may still contain that text from the pre-restore turn.
- extendedWaitUntil:
    notVisible:
      id: "chat-degraded-banner"
    timeout: 45000

- assertVisible:
    id: "chat-screen"

# Hard-required must_observe: post-restore stream success.
# Prefer live token-count during streaming; after reconcile, require
# chat-assistant-message-latest (token oracles unmount on idle).
- extendedWaitUntil:
    visible:
      id: "chat-stream-token-count-at-least-1"
    timeout: 60000
    optional: true

- runFlow:
    when:
      visible:
        id: "chat-stream-token-count-at-least-1"
    commands:
      - assertVisible:
          id: "chat-stream-token-count-at-least-1"

- runFlow:
    when:
      notVisible:
        id: "chat-stream-token-count-at-least-1"
    commands:
      - extendedWaitUntil:
          visible:
            id: "chat-assistant-message-latest"
          timeout: 60000
      - assertVisible:
          id: "chat-assistant-message-latest"

# End-state: durable new-turn bubble proves recovery completed
# (even if mid-stream token oracle already passed and then unmounted).
- extendedWaitUntil:
    visible:
      id: "chat-assistant-message-latest"
    timeout: 90000

- assertVisible:
    id: "chat-assistant-message-latest"

- extendedWaitUntil:
    notVisible:
      id: "chat-stream-phase-degraded"
    timeout: 30000
    optional: true

- takeScreenshot: S-REACTIVE-04-AC-3-degraded-recovery
