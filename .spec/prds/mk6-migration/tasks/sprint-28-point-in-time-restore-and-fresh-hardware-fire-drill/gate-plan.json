{
  "sprint": "sprint-28-point-in-time-restore-and-fresh-hardware-fire-drill",
  "sprint_identity": {
    "resolved_sprint_path": "/Users/inference1/Projects/holocron/.spec/prds/mk6-migration/tasks/sprint-28-point-in-time-restore-and-fresh-hardware-fire-drill",
    "sprint_slug": "sprint-28-point-in-time-restore-and-fresh-hardware-fire-drill"
  },
  "capability": "CAP-BAK-01",
  "planned_at": "20260728T000000Z",
  "planned_steps": 6,
  "dispatcher": "/usr/local/bin/bun services/platform/src/cli/holo.ts",
  "notes": [
    "REDHAT-FIX-H2: every literal_cmd uses the real bun dispatcher (not PATH holo stubs).",
    "Step 6 must observe a named restore-path failure \u2014 never sole-green on 'unknown flag: --pitr'.",
    "Executable oracle suite: PLATFORM_IT=1 pnpm vitest run services/platform/tests/integration/sprint28-human-gate-oracles.test.ts",
    "REDHAT-FIX-S28R3: step2 requires REQUIRE_LIVE_R2_RO=1 + distinct R2_RESTORE_* + prove-r2-readonly (never ro-test placeholders as live green; residual DEPENDENCY-S28-R2-RO if absent).",
    "REDHAT-FIX-S28R3: step3 runs scripts/run-fire-drill-on-fresh-target.sh against provisioned volume mountpoints + attestation (not host .tmp/REDHAT-FIX-H2/step3-*). Steps 4\u20135 read .tmp/REDHAT-FIX-S28R3/${GATE_RUN_ID}/parity-report.json.",
    "GATE-FIX-S28R3-QA3: live steps 1/3/6 preflight scripts/assert-gate-run-id.sh \u2014 pattern ^[A-Za-z0-9](?:[A-Za-z0-9_-]{0,62}[A-Za-z0-9])?$ ; no :-manual defaults; use \"${GATE_RUN_ID}\" only.",
    "GATE-FIX-S28R3-QA2: HUMAN-GATE.md is rendered from this plan (scripts/render-human-gate-from-plan.sh); sole authoritative literal_cmd source.",
    "GATE-FIX-S28R3-QA2 / H2: step2 R2_CREDENTIAL_POLICY uses exact prefix arn:aws:s3:::holocron-backup/pgbackrest/* (never bare bucket/*).",
    "GATE-FIX-S28R3-QA3 / M-3: step3 trap EXIT removes container, volumes, and ${HOST}-net.",
    "GATE-FIX-S28R3-QA5: step3 evidence under .tmp/REDHAT-FIX-S28R3/${GATE_RUN_ID}/ (provision/fire-drill logs, attestation, parity-report); steps 4\u20135 assert-gate-run-id then read only that run's parity-report.json.",
    "GATE-FIX-S28R3-QA6: step3 HOST=\"$(bash scripts/derive-s28-fresh-host.sh)\" \u2014 bounded collision-resistant host; full GATE_RUN_ID remains for evidence paths.",
    "GATE-FIX-S28R3-QA22: credential-ambient gate path uses only absolute executables (/bin/bash, /usr/bin/tee, /usr/bin/jq, absolute docker candidates); no bare bash/tee/jq/docker/dirname.",
    "GATE-FIX-S28R3-QA23: absolute bun/grep/mkdir/find/cat/wc/tr on gate stream; credential children use absolute grep/nc/env/python3; secret transport via FD 3 (exec-env-from-fd) not env -i KEY=secret / docker exec -e."
  ],
  "steps": [
    {
      "n": 1,
      "text": "Run holo restore --pitr <timestamp> against a scratch DB \u2014 restores to the named point exactly (or fail-closed outside WAL with named error).",
      "type": "terminal",
      "method": "real-cli",
      "literal_cmd": "set -euo pipefail; /bin/bash scripts/assert-gate-run-id.sh; SCRATCH=\".tmp/REDHAT-FIX-H2/${GATE_RUN_ID}/step1-scratch\"; LOG=\".tmp/REDHAT-FIX-H2/${GATE_RUN_ID}/step1-restore.txt\"; /bin/mkdir -p \"$SCRATCH\" \"${LOG%/*}\"; /usr/local/bin/bun services/platform/src/cli/holo.ts restore --pitr \"${PITR_TIMESTAMP:-2099-01-01T00:00:00Z}\" --scratch \"$SCRATCH\" --target-action promote 2>&1 | /usr/bin/tee \"$LOG\"; ! /usr/bin/grep -q 'unknown flag: --pitr' \"$LOG\"",
      "assertion": {
        "kind": "exit_and_log_regex",
        "expected_exit": null,
        "notes": "Valid in-window PITR \u2192 exit 0 + SELECT 1. Outside-window / empty \u2192 non-zero named restore error. Never unknown flag: --pitr. GATE-FIX-S28R3-QA3: scripts/assert-gate-run-id.sh preflight required; no :-manual; use \"${GATE_RUN_ID}\" only.",
        "must_observe_any": [
          "overall:.*OK",
          "outside available WAL",
          "no base backup available",
          "backup chain missing",
          "backup config missing secrets"
        ],
        "must_not_observe": ["unknown flag: --pitr"]
      }
    },
    {
      "n": 2,
      "text": "Provision / prove a fresh restore target with zero mini access (multi-axis isolation PASS) using live distinct R2_RESTORE_* under REQUIRE_LIVE_R2_RO=1.",
      "type": "terminal",
      "method": "real-script",
      "literal_cmd": "set -euo pipefail; /bin/bash scripts/assert-gate-run-id.sh; EVID=\".tmp/REDHAT-FIX-S28R3/${GATE_RUN_ID}\"; /bin/mkdir -p \"$EVID\"; SECRETS=\"${HOLOCRON_SECRETS_PATH:-${HOLO_SECRETS_PATH:-services/platform/config/secrets.yaml}}\"; if [[ -f \"$SECRETS\" ]]; then while IFS= read -r line || [[ -n \"$line\" ]]; do [[ \"$line\" =~ ^[[:space:]]*# ]] && continue; [[ \"$line\" =~ ^([A-Za-z0-9_]+):[[:space:]]*(.*)$ ]] || continue; k=\"${BASH_REMATCH[1]}\"; v=\"${BASH_REMATCH[2]}\"; v=\"${v%\\\"}\"; v=\"${v#\\\"}\"; v=\"${v%\\'}\"; v=\"${v#\\'}\"; case \"$k\" in R2_RESTORE_ACCESS_KEY_ID|R2_RESTORE_SECRET_ACCESS_KEY|R2_RESTORE_SESSION_TOKEN|R2_ENDPOINT|R2_ACCOUNT_ID|R2_BUCKET_NAME) if [[ -z \"${!k:-}\" && -n \"$v\" ]]; then export \"$k=$v\"; fi ;; esac; done <\"$SECRETS\"; fi; if [[ -z \"${R2_RESTORE_ACCESS_KEY_ID:-}\" || -z \"${R2_RESTORE_SECRET_ACCESS_KEY:-}\" ]]; then echo \"RESIDUAL: DEPENDENCY-S28-R2-RO — distinct live R2_RESTORE_* absent from secrets/env; refuse ro-test placeholders as live green\" | /usr/bin/tee \"$EVID/step2-isolation.txt\"; exit 1; fi; export REQUIRE_LIVE_R2_RO=1; export R2_ACCESS_KEY_ID=\"$R2_RESTORE_ACCESS_KEY_ID\"; export R2_SECRET_ACCESS_KEY=\"$R2_RESTORE_SECRET_ACCESS_KEY\"; export R2_SESSION_TOKEN=\"${R2_RESTORE_SESSION_TOKEN:-}\"; export R2_CREDENTIAL_KIND=object-read-only; export R2_CREDENTIAL_POLICY='{\"Version\":\"2012-10-17\",\"Statement\":[{\"Sid\":\"HolocronRestoreList\",\"Effect\":\"Allow\",\"Action\":[\"s3:ListBucket\",\"s3:GetBucketLocation\"],\"Resource\":[\"arn:aws:s3:::holocron-backup\"]},{\"Sid\":\"HolocronRestoreGet\",\"Effect\":\"Allow\",\"Action\":[\"s3:GetObject\"],\"Resource\":[\"arn:aws:s3:::holocron-backup/pgbackrest/*\"]}]}'; MINI_HOST=\"${MINI_HOST:-203.0.113.1}\" MINI_IPV4=\"${MINI_IPV4:-203.0.113.1}\" MINI_IPV6=\"${MINI_IPV6:-2001:db8::1}\" MINI_TAILNET_IP=\"${MINI_TAILNET_IP:-203.0.113.2}\" MINI_LAN_IP=\"${MINI_LAN_IP:-203.0.113.3}\" MINI_DNS_ALIASES=\"${MINI_DNS_ALIASES:-mini.invalid}\" MINI_SOCKET_DEFAULTS=\"${MINI_SOCKET_DEFAULTS:-0}\" MINI_UNIX_SOCKETS=\"${MINI_UNIX_SOCKETS:-/tmp/.s.PGSQL.5432-gate-absent}\" TARGET_ATTESTED_IDENTITY=\"${TARGET_ATTESTED_IDENTITY:-target-vm-uuid-gate}\" MINI_ATTESTED_IDENTITY=\"${MINI_ATTESTED_IDENTITY:-mini-hw-uuid-gate}\" REQUIRE_ATTESTED_IDENTITY=1 NC_TIMEOUT_SEC=1 REQUIRE_LIVE_R2_RO=1 /bin/bash scripts/prove-r2-readonly.sh 2>&1 | /usr/bin/tee \"$EVID/step2-r2-readonly.txt\"; MINI_HOST=\"${MINI_HOST:-203.0.113.1}\" MINI_IPV4=\"${MINI_IPV4:-203.0.113.1}\" MINI_IPV6=\"${MINI_IPV6:-2001:db8::1}\" MINI_TAILNET_IP=\"${MINI_TAILNET_IP:-203.0.113.2}\" MINI_LAN_IP=\"${MINI_LAN_IP:-203.0.113.3}\" MINI_DNS_ALIASES=\"${MINI_DNS_ALIASES:-mini.invalid}\" MINI_SOCKET_DEFAULTS=\"${MINI_SOCKET_DEFAULTS:-0}\" MINI_UNIX_SOCKETS=\"${MINI_UNIX_SOCKETS:-/tmp/.s.PGSQL.5432-gate-absent}\" TARGET_ATTESTED_IDENTITY=\"${TARGET_ATTESTED_IDENTITY:-target-vm-uuid-gate}\" MINI_ATTESTED_IDENTITY=\"${MINI_ATTESTED_IDENTITY:-mini-hw-uuid-gate}\" REQUIRE_ATTESTED_IDENTITY=1 R2_ACCESS_KEY_ID=\"$R2_RESTORE_ACCESS_KEY_ID\" R2_SECRET_ACCESS_KEY=\"$R2_RESTORE_SECRET_ACCESS_KEY\" R2_RESTORE_ACCESS_KEY_ID=\"$R2_RESTORE_ACCESS_KEY_ID\" R2_RESTORE_SECRET_ACCESS_KEY=\"$R2_RESTORE_SECRET_ACCESS_KEY\" R2_CREDENTIAL_KIND=object-read-only R2_CREDENTIAL_POLICY=\"$R2_CREDENTIAL_POLICY\" NC_TIMEOUT_SEC=1 REQUIRE_LIVE_R2_RO=1 /bin/bash scripts/prove-isolation.sh 2>&1 | /usr/bin/tee \"$EVID/step2-isolation.txt\"; /usr/bin/grep -qE 'RESULT:[[:space:]]+PASS' \"$EVID/step2-isolation.txt\"",
      "assertion": {
        "kind": "exit_and_log_regex",
        "expected_exit": 0,
        "notes": "REDHAT-FIX-S28R3/HIGH-1: live distinct R2_RESTORE_* + prove-r2-readonly required. Absent keys \u2192 non-zero + DEPENDENCY-S28-R2-RO (honest residual; never ro-test green). GATE-FIX-S28R3-QA4/H-2: assert-gate-run-id before mkdir; evidence under .tmp/REDHAT-FIX-S28R3/${GATE_RUN_ID}/.",
        "require_all_regex": [
          "AXIS network:[[:space:]]*PASS",
          "AXIS identity:[[:space:]]*PASS",
          "RESULT:[[:space:]]+PASS"
        ]
      }
    },
    {
      "n": 3,
      "text": "Compare row counts pre/post-restore \u2014 exact match against the pre-failure snapshot (parity-report row_counts) on provisioned fresh-target volumes.",
      "type": "terminal",
      "method": "real-cli",
      "literal_cmd": "set -euo pipefail; DOCKER=\"\"; for _d in /usr/bin/docker /usr/local/bin/docker /opt/homebrew/bin/docker; do [[ -x \"$_d\" ]] && DOCKER=\"$_d\" && break; done; [[ -n \"$DOCKER\" ]] || { echo \"error: docker absolute executable not found\" >&2; exit 2; }; /bin/bash scripts/assert-gate-run-id.sh; EVID=\".tmp/REDHAT-FIX-S28R3/${GATE_RUN_ID}\"; /bin/mkdir -p \"$EVID\"; HOST=\"$(/bin/bash scripts/derive-s28-fresh-host.sh)\"; PG_PORT=\"${RESTORE_PG_PORT:-$((56000 + RANDOM % 3000))}\"; trap '\"$DOCKER\" rm -f \"$HOST\" >/dev/null 2>&1 || true; \"$DOCKER\" volume rm -f \"${HOST}-pgdata\" \"${HOST}-blobs\" >/dev/null 2>&1 || true; \"$DOCKER\" network rm -f \"${HOST}-net\" >/dev/null 2>&1 || true' EXIT; SECRETS=\"${HOLOCRON_SECRETS_PATH:-${HOLO_SECRETS_PATH:-services/platform/config/secrets.yaml}}\"; if [[ -f \"$SECRETS\" ]]; then while IFS= read -r line || [[ -n \"$line\" ]]; do [[ \"$line\" =~ ^[[:space:]]*# ]] && continue; [[ \"$line\" =~ ^([A-Za-z0-9_]+):[[:space:]]*(.*)$ ]] || continue; k=\"${BASH_REMATCH[1]}\"; v=\"${BASH_REMATCH[2]}\"; v=\"${v%\\\"}\"; v=\"${v#\\\"}\"; v=\"${v%\\'}\"; v=\"${v#\\'}\"; case \"$k\" in R2_RESTORE_ACCESS_KEY_ID|R2_RESTORE_SECRET_ACCESS_KEY|R2_RESTORE_SESSION_TOKEN|R2_ENDPOINT|R2_ACCOUNT_ID|R2_BUCKET_NAME) if [[ -z \"${!k:-}\" && -n \"$v\" ]]; then export \"$k=$v\"; fi ;; esac; done <\"$SECRETS\"; fi; if [[ -z \"${R2_RESTORE_ACCESS_KEY_ID:-}\" || -z \"${R2_RESTORE_SECRET_ACCESS_KEY:-}\" ]]; then echo \"RESIDUAL: DEPENDENCY-S28-R2-RO — distinct live R2_RESTORE_* absent from secrets/env; refuse ro-test placeholders as live green\" | /usr/bin/tee \"$EVID/step3-provision.txt\"; exit 1; fi; REQUIRE_LIVE_R2_RO=1 R2_RESTORE_OBJECT_PREFIX=\"pgbackrest\" R2_PGBACKREST_PREFIX=\"pgbackrest\" STAGING_ROOT=\"$EVID/fresh-restore\" /bin/bash scripts/provision-fresh-restore-target.sh --host \"$HOST\" --skip-isolation --pg-port \"$PG_PORT\" 2>&1 | /usr/bin/tee \"$EVID/step3-provision.txt\"; REQUIRE_LIVE_R2_RO=1 R2_RESTORE_OBJECT_PREFIX=\"pgbackrest\" R2_PGBACKREST_PREFIX=\"pgbackrest\" /bin/bash scripts/run-fire-drill-on-fresh-target.sh --host \"$HOST\" --target-timestamp \"${PITR_TIMESTAMP:?set PITR_TIMESTAMP to an in-window ISO}\" --attestation \"$EVID/attestation.json\" --report \"$EVID/parity-report.json\" 2>&1 | /usr/bin/tee \"$EVID/step3-fire-drill.txt\"; test -f \"$EVID/attestation.json\"; /usr/bin/jq -e '.schema == \"holo.fresh-target.fire-drill-attestation.v1\" and .ok == true and (.volumes.pgdata | type == \"string\") and (.mountpoints.scratch | type == \"string\" and (test(\"REDHAT-FIX-H2/step3\") | not))' \"$EVID/attestation.json\"; test -f \"$EVID/parity-report.json\"; /usr/bin/jq -e '.POSTGRES_PARITY_PASS == true and (.row_counts | type == \"object\")' \"$EVID/parity-report.json\"",
      "assertion": {
        "kind": "exit_and_log_regex",
        "expected_exit": 0,
        "notes": "REDHAT-FIX-S28R3/CRITICAL-1: volume-bound runner + attestation; restore destinations are provisioned volume Mountpoints (not .tmp/REDHAT-FIX-H2/step3-*). GATE-FIX-S28R3-QA3: required allowlisted GATE_RUN_ID preflight; trap removes ${HOST}-net. GATE-FIX-S28R3-QA5/H-2: evidence under .tmp/REDHAT-FIX-S28R3/${GATE_RUN_ID}/ (step3-provision.txt, step3-fire-drill.txt, attestation.json, parity-report.json; STAGING_ROOT=$EVID/fresh-restore). GATE-FIX-S28R3-QA6: HOST via scripts/derive-s28-fresh-host.sh (\u226464, collision-resistant).",
        "require_all_regex": [
          "POSTGRES_PARITY_PASS",
          "row_counts",
          "run-fire-drill-on-fresh-target|fire-drill-attestation|bound scratch"
        ],
        "must_not_observe": ["unknown flag:", "unknown command"]
      }
    },
    {
      "n": 4,
      "text": "Compare the evidence-ledger chain pre/post-restore \u2014 as-of chain SHA-256 matches baseline.",
      "type": "terminal",
      "method": "real-cli",
      "literal_cmd": "set -euo pipefail; /bin/bash scripts/assert-gate-run-id.sh; EVID=\".tmp/REDHAT-FIX-S28R3/${GATE_RUN_ID}\"; test -f \"$EVID/parity-report.json\"; /usr/bin/jq -e '.LEDGER_CHECKSUM_MATCH == true and ((.ledger_sha256 // .ledgerDigest // .LEDGER_SHA256 // \"\") | tostring | test(\"^[a-f0-9]{64}$\"; \"i\"))' \"$EVID/parity-report.json\"",
      "assertion": {
        "kind": "exit_and_log_regex",
        "expected_exit": 0,
        "notes": "GATE-FIX-QA4: literal_cmd ends with jq -e which prints only the scalar true on success (exit 0). Domain tokens are proven by the frozen jq predicate + cmd_sha fidelity, not by stdout tokens that jq never emits. GATE-FIX-S28R3-QA5/H-2: assert-gate-run-id then read only .tmp/REDHAT-FIX-S28R3/${GATE_RUN_ID}/parity-report.json.",
        "require_all_regex": ["^true$"]
      }
    },
    {
      "n": 5,
      "text": "Restore blob storage on the fresh target \u2014 every object SHA-256 matches (BLOB_PARITY_PASS + matched_objects >= 1).",
      "type": "terminal",
      "method": "real-cli",
      "literal_cmd": "set -euo pipefail; /bin/bash scripts/assert-gate-run-id.sh; EVID=\".tmp/REDHAT-FIX-S28R3/${GATE_RUN_ID}\"; test -f \"$EVID/parity-report.json\"; /usr/bin/jq -e '.BLOB_PARITY_PASS == true and (.matched_objects // .blob_matched_objects // 0) >= 1' \"$EVID/parity-report.json\"",
      "assertion": {
        "kind": "exit_and_log_regex",
        "expected_exit": 0,
        "notes": "GATE-FIX-QA4: literal_cmd ends with jq -e which prints only the scalar true on success (exit 0). Domain tokens are proven by the frozen jq predicate + cmd_sha fidelity, not by stdout tokens that jq never emits. GATE-FIX-S28R3-QA5/H-2: assert-gate-run-id then read only .tmp/REDHAT-FIX-S28R3/${GATE_RUN_ID}/parity-report.json.",
        "require_all_regex": ["^true$"]
      }
    },
    {
      "n": 6,
      "text": "Point restore at an empty/corrupted backup chain \u2014 restore fails closed with a named chain error; never fake success; never sole-green on unknown flag: --pitr.",
      "type": "terminal",
      "method": "real-cli",
      "literal_cmd": "set -euo pipefail; /bin/bash scripts/assert-gate-run-id.sh; SCRATCH=\".tmp/REDHAT-FIX-H2/${GATE_RUN_ID}/step6-scratch\"; LOG=\".tmp/REDHAT-FIX-H2/${GATE_RUN_ID}/step6-restore.txt\"; /bin/mkdir -p \"$SCRATCH\" \"${LOG%/*}\"; EMPTY_PREFIX=\"pgbackrest-s28-gate-empty/${GATE_RUN_ID}\"; set +e; R2_PGBACKREST_PREFIX=\"$EMPTY_PREFIX\" /usr/local/bin/bun services/platform/src/cli/holo.ts restore --pitr 2024-01-01T00:00:00Z --scratch \"$SCRATCH\" --target-action promote > \"$LOG\" 2>&1; RC=$?; set -e; /bin/cat \"$LOG\"; test \"$RC\" -ne 0; ! /usr/bin/grep -q 'unknown flag: --pitr' \"$LOG\"; /usr/bin/grep -qiE 'no base backup available|backup chain missing|manifest checksum mismatch|WAL segment corrupted|backup chain integrity|outside available WAL|backup config missing secrets' \"$LOG\"; test \"$(/usr/bin/find \"$SCRATCH\" -type f 2>/dev/null | /usr/bin/wc -l | /usr/bin/tr -d ' ')\" -eq 0",
      "assertion": {
        "kind": "exit_and_log_regex",
        "expected_exit": 0,
        "notes": "Outer script exits 0 only when restore domain fail-closed is proven. must_not_observe unknown-flag-only. GATE-FIX-S28R3-QA3: assert-gate-run-id preflight; no :-manual.",
        "require_all_regex": [
          "no base backup available|backup chain missing|manifest checksum|WAL segment corrupted|outside available WAL|backup config missing secrets"
        ],
        "must_not_observe": ["unknown flag: --pitr"],
        "must_not_sole_success_on": "unknown flag: --pitr"
      }
    }
  ],
  "oracle_suite": {
    "command": "PLATFORM_IT=1 pnpm vitest run services/platform/tests/integration/sprint28-human-gate-oracles.test.ts",
    "capability_inventory": "pnpm vitest run services/platform/tests/integration/sprint28-capability-inventory.test.ts",
    "empty_chain_full": "PLATFORM_IT=1 pnpm vitest run services/platform/tests/integration/sprint28-restore-fails-closed.test.ts"
  }
}
