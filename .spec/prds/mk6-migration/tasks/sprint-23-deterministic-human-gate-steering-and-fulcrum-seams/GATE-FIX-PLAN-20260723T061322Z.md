# GATE-FIX plan from fresh QA `20260723T061322Z` (truthful FAIL)

**Source:** `gate-results.json` verdict `fail`, run_id `20260723T061322Z`  
**Stance:** Do **not** claim sprint/human-gate complete until a **final quiescent** fresh QA pass (no mid-run merges, ledger fixtures consistent with plan).

## Caveats → tasks

| Caveat | Defect | Task |
|--------|--------|------|
| Hard-coded `RUN_ECHO` / `RUN_CYCLE` 404 after nonprod reseed | Steps 1/3/4/5 not exercising handlers | **GATE-FIX-003** (landed `be7d52ac`) |
| Step 3 regex matches `# literal_cmd` echo; real output `STEP3_VERDICT=other` | False-positive pass (also: bare `PROBE_REQUIRED_FOR_VALIDATED` / `"ok":true` self-match class) | **GATE-FIX-004** (landed `be7d52ac`) |
| Step 3 research create then **immediate** 3b advance — no bounded poll / probe-ready marker | GATE-FIX-003 **AC-4 residual** (task lines 27, 70–74, 97): create-only then advance is insufficient; luck on fast fleets is not proof | **GATE-FIX-005** (planned) |
| Mid-gate run-stage merges advanced HEAD | Process noise, not a gate-plan code fix | Operational: land GATE-FIX then **quiescent** `/kb-run-human-tests` |

## Task order

1. **GATE-FIX-003** (P0, CONFIG) — rewrite steps 1/3/4/5 (+ method notes) to create/discover fresh runs at gate time (`test.echo` unprobed + `research` for probe/steer/cycle). **Poll for real plan probe before 3b.** Gate-plan only. Supersedes GATE-FIX-001 AC-2 only for UUID→variable on step 5. **Landed** on main (`be7d52ac` via merge `f135f2b6`) — UUID discovery + dual-create done; **AC-4 poll not implemented in landed literal_cmd**.
2. **GATE-FIX-004** (P0, CONFIG) — step 3 body-level dual claim; split success token; fail branch `exit 1`; no self-matching `expect_log_regex` on echo-only. Gate-plan only. **Landed** with 003.
3. **GATE-FIX-005** (P0, CONFIG) — close 003 AC-4 residual: between research create and 3b advance, **bounded real API status poll** (`GET /api/missions/:id`) + **read-only** observation of committed `research.plan@1` (trigger-equivalent SELECT via psql/holo CLI — no inserts), log split-constructed `STEP3_PROBE_READY`, **`exit 1` on timeout** before 3b. Gate-plan step 3 + evidence only. Depends on 003/004 land. **Do not implement until this plan is validated and landed.**

## Out of scope

- Product handlers, migrations, fleet product code (including widening GET status to embed stage_runs)
- Rewriting historical `.gate-evidence/**` (including `20260723T061322Z`)
- Claiming complete on GATE-FIX-001…004 alone (AC-4 residual + final quiescent QA still required)
- Implementing GATE-FIX-005 in this planning pass (task file + plan note only)

## Done definition (human)

1. GATE-FIX-003 + GATE-FIX-004 dual-lens reviewed and **landed on main** ✅ (`f135f2b6`)
2. GATE-FIX-005 dual-lens reviewed and **landed on main** (plan → implement → review → merge)
3. Service on local fleet `:4546` with templates aligned to `/tmp/holocron-fleet-local-valid.json` if reseed is used
4. Fresh `/kb-run-human-tests` with **verdict pass**, all steps genuine, **no** data-drift 404 caveats, **no** step3 self-match caveat, **probe-ready poll evidence** in step 3 log
5. Quiescent tree (no concurrent sprint merges into main mid-gate)

## Files added / validated

- `GATE-FIX-003-reseed-safe-fresh-run-discovery.md` (landed)
- `GATE-FIX-004-step3-assertion-no-self-match.md` (landed)
- `GATE-FIX-005-step3-bounded-probe-ready-poll.md` (**this plan wave**)
- This plan note (updated for 005)
- `SPRINT.md` task table row + status note for GATE-FIX-005
