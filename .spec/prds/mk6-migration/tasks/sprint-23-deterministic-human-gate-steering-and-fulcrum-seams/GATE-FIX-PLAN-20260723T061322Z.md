# GATE-FIX plan from fresh QA `20260723T061322Z` (truthful FAIL)

**Source:** `gate-results.json` verdict `fail`, run_id `20260723T061322Z`  
**Stance:** Do **not** claim sprint/human-gate complete until a **final quiescent** fresh QA pass (no mid-run merges, ledger fixtures consistent with plan).

## Caveats → tasks

| Caveat | Defect | Task |
|--------|--------|------|
| Hard-coded `RUN_ECHO` / `RUN_CYCLE` 404 after nonprod reseed | Steps 1/3/4/5 not exercising handlers | **GATE-FIX-003** |
| Step 3 regex matches `# literal_cmd` echo; real output `STEP3_VERDICT=other` | False-positive pass (also: bare `PROBE_REQUIRED_FOR_VALIDATED` / `"ok":true` self-match class) | **GATE-FIX-004** |
| Mid-gate run-stage merges advanced HEAD | Process noise, not a gate-plan code fix | Operational: land GATE-FIX then **quiescent** `/kb-run-human-tests` |

## Task order

1. **GATE-FIX-003** (P0, CONFIG) — rewrite steps 1/3/4/5 (+ method notes) to create/discover fresh runs at gate time (`test.echo` unprobed + `research` for probe/steer/cycle). **Poll for real plan probe before 3b.** Gate-plan only. Supersedes GATE-FIX-001 AC-2 only for UUID→variable on step 5.
2. **GATE-FIX-004** (P0, CONFIG) — step 3 body-level dual claim; split success token; fail branch `exit 1`; no self-matching `expect_log_regex` on echo-only. Gate-plan only. Depends on 003’s fresh runs for AC-2 live proof; may co-land if both only touch `gate-plan.json`.

## Out of scope

- Product handlers, migrations, fleet product code
- Rewriting historical `.gate-evidence/**` (including `20260723T061322Z`)
- Claiming complete on GATE-FIX-001/002 alone (061322Z still fail for fixture drift + step3 false positive)
- Implementing the gate-plan rewrites in this planning pass (task files only)

## Done definition (human)

1. GATE-FIX-003 + GATE-FIX-004 dual-lens reviewed and **landed on main**
2. Service on local fleet `:4546` with templates aligned to `/tmp/holocron-fleet-local-valid.json` if reseed is used
3. Fresh `/kb-run-human-tests` with **verdict pass**, all steps genuine, **no** data-drift 404 caveats, **no** step3 self-match caveat
4. Quiescent tree (no concurrent sprint merges into main mid-gate)

## Files added / validated

- `GATE-FIX-003-reseed-safe-fresh-run-discovery.md` (validated + implementability corrections)
- `GATE-FIX-004-step3-assertion-no-self-match.md` (validated + self-match surface expanded)
- This plan note
- `SPRINT.md` task table rows + status note for gate FAIL remediation
