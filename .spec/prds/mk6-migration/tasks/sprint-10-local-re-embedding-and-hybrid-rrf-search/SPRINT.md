---
sprint: 10
title: Local Re-embedding and Hybrid RRF Search
sequence: 10
timeline: Phase 2 — Inference and Data
status: Planned
prd: ../../README.md
capability_coverage: [CAP-EMB-01]
planned_from_roadmap_sha: 014c6088ab7f9a16735a79146d4fba2e135581a0b894f06b9757cece98d14225
planned_from_source_sha: 13f93dc53c8516efd93368a9f0c0627aeb3af6f6
planned_at: 2026-07-17T02:47:04Z
---

# Sprint 10: Local Re-embedding and Hybrid RRF Search

**Sequence:** 10
**Timeline:** Phase 2 — Inference and Data
**Status:** Planned
**Proposed by:** mastra-planner
**Branch:** `mk6-local-search`
**Opened:** 2026-07-17 — generated JIT by /kb-sprint-tasks-plan

---

## Overview

A complete, decisive migration of holocron off Convex — cloud database and all services — onto a Mastra (Bun) + Postgres platform on the tailnet mini, with the RN app resyncing via Zero and all reasoning on the local inference fleet. This sprint supplies the local Qwen3 passage-embedding and Postgres hybrid-search substrate needed by the later ETL, research, and pipeline sprints: content beyond the old 8K truncation becomes retrievable, and pgvector HNSW plus FTS are fused by rank-based RRF in one Postgres round-trip.

## Human Test Deliverable

An operator can seed a document with its relevant answer beyond character 8,000, run the real fleet-backed embedding pass, and retrieve that answer through a single real-Postgres hybrid-search request. The operator can also inspect vector validity, RRF explain output, golden-set recall, resumable idempotency, and inline-HNSW semantic search without a cloud embedding dependency.

**Test Steps:**
1. Run `holo embed:run` against the real `:4545` embed route — every non-empty doc gets ≥1 passage, all vectors 1024-dim non-null.
2. Run `holo embed:verify` — zero passages carry a null or wrong-dimension vector; norms are ~unit.
3. Seed a golden doc with the answer past char-8000 and run `holo search '<span query>'` — the doc ranks in top-k.
4. Run `holo search '<q>' --explain` — shows pgvector KNN + FTS fused with RRF in one round-trip.
5. Run `holo search:recall --golden set.json` — new recall ≥ the recorded Convex hybridSearch baseline.
6. Re-run `holo embed:run`, interrupt it, and resume — no duplicate passages (`WHERE embedding IS NULL`, `SKIP LOCKED`).
7. Run `holo search --surface research_findings '<q>'` — inline-HNSW semantic search returns results with no Cohere/cloud call.

## Tasks

| ID | Title | Agent | Estimate |
|----|-------|-------|----------|
| search-1 | Shared embed() helper (query/document prefix asymmetry) + passage chunking (~512 tok) | mastra-implementer | 240 min |
| search-2 | Idempotent resumable re-embed job + optional contextual header | mastra-implementer | 180 min |
| search-3 | RRF hybrid search helper (pgvector HNSW + FTS, one round-trip) + 5 inline-HNSW surfaces | mastra-implementer | 240 min |
| search-4 | RED tests: 0 null/wrong-dim vectors, past-8K retrieval, recall≥baseline, idempotent re-embed | red-test-generator | 180 min |
| search-5 | Review embedding + search parity | mastra-reviewer | 90 min |

## Human Testing Gate

**Gate:** After `holo embed:run` chunks and embeds a seeded document whose relevant span sits past character 8,000, `holo search '<span-query>'` returns that document in top-k via one-round-trip pgvector+FTS RRF fusion against real Postgres — impossible under the old 8K truncation.

## Source Coverage

- UC-DATA-03, UC-DATA-04
- T-DATA-009, T-DATA-010, T-DATA-011, T-DATA-012, T-DATA-013, T-DATA-014, T-DATA-015
- `05-uc-data.md` — local re-embedding, passage chunking, query/document prefix asymmetry, resumable embedding, and Postgres hybrid search acceptance criteria.
- `10-technical-requirements/03-data-schema.md` — vector/search storage, HNSW/GIN indexes, 512-token chunks with overlap, contextual headers, and the Zero publication split.
- `10-technical-requirements/09-capability-chains.md` — CAP-EMB-01 local re-embedding pass and its boundary contracts.
- `10-technical-requirements/11-runtime-contracts.md` — Fleet Role Manifest and local embed-route runtime contract.

## Capability Coverage

- CAP-EMB-01: local Qwen3 chunk+embed pass (query/doc asymmetry, idempotent resumable), consumed by hybrid search.

## Blocks

- Sprint 14 (Big-Bang ETL and Content-Addressed File Storage)
- Sprint 17 (Deterministic pi-free Research Engine)
- Sprint 22 (All Agentic Pipelines as Templates/Agents)

**Dependent on:** Sprint 04 (Postgres domain schema, pgvector/FTS/HNSW substrate) · Sprint 01 (Fleet Role Manifest and local embed-route resolution).

---

## Task Detail Files

Generated by /kb-sprint-tasks-plan on 2026-07-17 (proposed by: mastra-planner).
Avg quality score: ~108/115 (115-point rubric, min 80). Fakeability audit: 0 CRITICAL — every behavioral AC carries a grounded scenario (real Postgres + real pgvector + real fleet `:4545`), independently re-verified on the rendered files.
Topological order: search-4 (RED suite, written first — proves the empty impl fails against the real seam) → search-1 (embed helper + chunker) ∥ search-2 (idempotent re-embed job) → search-3 (RRF hybrid search + inline surfaces) → search-5 (adversarial parity review of search-1/2/3/4).

- search-1-shared-embed-helper-querydocument-prefix-asymmetry-passage-c.md
- search-2-idempotent-resumable-re-embed-job-where-embedding-is-null-fo.md
- search-3-rrf-hybrid-search-helper-pgvector-hnsw-fts-one-round-trip-5.md
- search-4-red-tests-0-nullwrong-dim-vectors-past-8k-retrieval-recallba.md
- search-5-review-embedding-search-parity-vector-integrity-past-8k-reca.md
