---
stability: CONSTITUTION
last_validated: 2026-07-13
prd_version: 3.0.0
---

# Technical Risks

Consolidated from all three specialist reports plus the Operator decisions. Ordered by severity.

| # | Risk | Severity | Mitigation |
|---|------|----------|------------|
| R1 | **Big-bang data loss.** One-shot cutover, no dual-write; a missed table/blob or a late write is lost. | High | Durable Convex write fence + quiet interval + watermark before export; immutable archive + `legacy_convex_id` through the read-only soak; catalog-derived idempotent ETL; no write-enabled soak before recovery gates. |
| R2 | **App-rewrite volume.** ~105 Convex hook call-sites across ~47 files + provider swap; Zero semantics differ → genuine rewrite with no RN e2e net today. | High | Provision RN e2e (Maestro/Detox) as a leading INFRA task; migrate screen-by-screen behind the coexistence window; grep-gate zero `convex/react` imports. |
| R3 | **Embedding-dimension + regeneration.** New model + dims + chunking → results change; silent quality regression or partial embed failure. | High | Pin `vector(1024)`; golden-query eval (new recall ≥ old) + the past-8K-retrieval assertion; idempotent resumable re-embed (`WHERE embedding IS NULL`, `SKIP LOCKED`); dim + unit-norm checks. |
| R4 | **16 crons have no home.** All Convex-runtime-bound; a missed one silently rots a background pipeline (no CI to catch it). | High | Enumerated 16→(7 janitor sweeps + 4 Mastra workflows + 1 queue consumer + 3→1 backfill + 1 digest); an AC asserts each observably fires; add a CI/health check. |
| R5 | **Structured output reliability on local models.** json_schema honoring varies across fleet backends ("compatibility mess"). | High | Constrained decode + Zod re-validation + bounded repair loop + boot-time per-role capability probe; escape hatch for extraction-critical steps; TDD known-malformed→repair-or-fail. |
| R6 | **FK integrity across `_id`→uuid remap.** A missed mapping NULLs/mis-points a FK; the two merges (research 5→3, analysis 12→3) are highest-risk. | Med-High | Whole-graph id map before any load; enforce real FK constraints (fail closed) + NULL-FK audit; `legacy_convex_id` re-derivation; extra spot-checks on merged tables. |
| R7 | **Fulcrum PRD reconciliation.** This migration retires fulcrum ADR-001 (SQLite ledger) and ADR-002 (cloud embedder). Building fulcrum against stale ADRs, or ambiguous ledger ownership. | Med-High | **Decision recorded (AP-1): Postgres-only, ledger in Postgres append-only tables — no SQLite.** Fulcrum's PRD must be re-planned to build as a mission template on this platform; flag both PRDs for reconciliation. |
| R8 | **Zero replication gotchas.** pgvector/tsvector can't sync; missing replica identity stalls UPDATE/DELETE; large jsonb bloats the client; big initial snapshot/slot lag; DDL needs publication upkeep. | Med | Minimal `zero_pub` (reactive subset only, vectors excluded via the canonical passages split); uuid PK replica identity everywhere; declared query/mutate/offline contract; test end-to-end propagation pre-cutover. |
| R9 | **Losing Convex's transactional scheduler.** The 23-case tool switch + `runAfter` chaining relied on it. | Med | Mastra workflow durability + at-least-once queue with exactly-once observable effects; outbox/inbox/idempotency/fencing; kill-9 tests on commit + queue paths are mandatory. |
| R10 | **pg-boss/graphile-worker on Bun unverified.** | Med | Sprint-0 gate on real Bun; graphile-worker primary / pg-boss fallback; Mastra native `schedule` covers pure crons regardless. |
| R11 | **Public `/article/` endpoint drift.** The only public URL; `app/document/[id].tsx` hard-codes the `.convex.site` shape — miss the rehost and every shared link 404s. | Med | Port the converter verbatim; keep path + token compatible; byte-compare a sample; narrow, explicit public exposure only. |
| R12 | **Mini contention: perpetual missions vs interactive latency + thermals.** | Med | Queue priority lanes (interactive > background); thermal/duty-cycle breaker; degradation ceiling drops background to sense-only under load. |
| R13 | **Local quality gap for the 10 chat specialists + CHALLENGE** (prompts tuned for Claude). | Med | Versioned per-specialist datasets/baselines, deterministic invariants, judge scorer + prompt versions, CI thresholds, declared high-stakes escape, and block-don't-warn regression policy. |
| R14 | **Escape-hatch leakage** — silent cloud drift defeats local-first. | Med | Default-deny `resolveModel`; escape requires explicit flag + budget pre-check + telemetry; a test asserts no Anthropic call on the default path. |
| R15 | **jsonb query performance** — a hot query filtering *into* jsonb seq-scans. | Low-Med | Audit query patterns; promote hot keys to typed columns; GIN/expression indexes only where needed (most jsonb is read-whole). |
| R16 | **"Dev deployment IS production" + no recovery proof.** Deletion is irreversible; the export archive cannot restore post-flip data. | Low-Med | Encrypted off-mini Postgres base backup + continuous WAL/PITR, blob manifest backup, isolated restore environment, and fresh restore drill before deletion; first enabled Postgres write is the data-plane point of no return. |
| R17 | **Carrying forward a live stub.** `convex/db/agentActivity.ts` returns `null`. | Low | Implement for real on the new engine; do not port the stub silently. |
| R18 | **Chunking blowup** on pathological 50K-char docs. | Low | Cap chunk size/count (~512 tok / 64 overlap); ~15–40K passages estimated; resumable job; contextual-header generation deferrable. |
| R19 | **Standing disaster-recovery gap after decommission.** Once Convex is deleted, the mini's Postgres + blob store are the ONLY copy of the data; a drive failure, theft, or fire causes unrecoverable loss with no fallback path — and a silently-failing backup job is worse than none (false confidence). Distinct from R16: R16 is the one-time migration-cutover deletion gate; this is the forever-after operating risk once that gate has passed. | High | **UC-PLAT-06**: continuous off-mini WAL archiving + scheduled base backups (pgBackRest → R2) + scheduled blob mirror (restic) + failure/overdue alerting + a periodic real restore drill to fresh hardware — standing for the life of the system, not a one-time migration artifact. |

---

## Added 2026-08-07 — risks recorded during Sprint 31 remediation planning

The sprints 1–29 integrity audit surfaced findings that the dispatched specialist SET judged real but **not in scope as sprint work**. They are recorded here with an explicit promotion trigger rather than silently dropped or smuggled into Sprint 31. Each names its author.

| # | Risk | Severity | Mitigation / promotion trigger |
|---|------|----------|-------------------------------|
| R20 | **Sole-implementation Convex capabilities.** Decommission is permitted only after every in-scope capability has a named, resolving replacement outside `convex/`. A capability whose only implementation is a runtime-fenced Convex module is a functional regression disguised as a completed migration. Currently: the 10 chat specialists (`convex/chat/specialists.ts:38-48`) and the real implementations of the 16 legacy crons (`convex/taskCrons.ts`). | High | Sprint 31 S31-CX-05 inventories and blocks; S31-02/S31-04 implement. **Promote to a Sprint 32 blocker the moment `holo verify:decommission-inventory` reports any file classified as sole-implementation.** *(convex-planner)* |
| R21 | **Unprovenanced export archives.** An export archive hash proves only self-consistency — `etl/archive.ts:290` computes it over the on-disk file manifest with no binding to a deployment, snapshot, or export invocation. This is the mechanism that let a constructed 60-table / 104-row directory pass as a production export in Sprint 14. After source deletion the archive is the only remaining witness. | High | Required provenance sidecar validated on read (S31-CX-02). **Promote to blocking if the fresh export diverges from the retained archive, or if the retained archive directory cannot be located.** *(convex-planner)* |
| R22 | **Content-blind reconciliation.** Row-count parity cannot detect field-level corruption from null coercion (`etl/transform.ts:63,75,87,102`) or column-default substitution (`etl/run.ts:501`). A load with correct counts and wrong values reports zero variance. | High | Per-field content digests + defaulted-column reporting (S31-CX-03). **Promote to a blocking defect if any non-zero `fieldDigestMismatches` or `defaulted_column` entries appear against the Sprint 29 load.** *(convex-planner)* |
| R23 | **Client contract behaviours are declared, not proven.** The client-data contract declares offline, optimistic, conflict and rejection behaviour for 105 call sites, but its verifier is a static cross-check of two spec files and never executes the client. Sprint 31 proves one behaviour (airplane-mode reads) on a real simulator; queued writes, rejection rollback, duplicate replay and concurrent edit carry no executable evidence. | High | Treat the remaining four as design intent, not verified guarantees. **Promote to a blocking task on any user-visible data-loss or duplicate-write incident, or if a Sprint 32+ gate cites UC-SYNC-01 AC-5 as satisfied evidence for a write path.** *(react-native-ui-planner)* |
| R24 | **Gate harnesses reaching into production.** Sprint 29's PITR test overwrote Sprint 27's production `pgbackrest.conf` and captured the alert-sweep `DATABASE_URL`; Sprint 27's gate fixtures still sit in the production heartbeat table; Sprint 29's freeze armed the real Convex deployment. | High | Harness-scoped ephemeral config + credentials; a harness process may never read or write production config (S31-OPS-03). **Promote to a dedicated evidence-integrity sprint if any future gate evidence is found writing a production config/secrets path, or if a `gate-results.json` is rewritten post-hoc without a matching `gate-verification.json` re-run.** *(devops-engineer)* |
| R25 | **A migration sprint passed without a recorded gate.** Sprint 21 has no `.gate-evidence/` and no `gate-results.json`, and its own AC-5 CI-wiring requirement is unmet — `verify:client-contract` appears in zero workflows; its negative-fixture directory is empty so two declared test cases cannot run. | High | Sprint 31 records an explicit gate tombstone stating the gate was never run, rather than back-filling evidence. **Promote to a blocking task immediately if an audit finds any other pre-cutover sprint missing `gate-results.json`.** *(react-native-ui-planner)* |
| R26 | **Migration ordinal collisions.** Migration filenames are the only ordering mechanism and nothing validates uniqueness or contiguity. Two files currently share the `0030_` prefix (33 files, 29 applied rows); a collision causes a silently skipped migration and corrupts the restore path, since a restored database is rebuilt from these files. | High | Fail-closed ordinal check in the migration runner and in CI (S31-01). **Already triggered; stands as the standing risk that the class recurs if the check is not CI-enforced.** *(mastra-planner)* |
| R27 | **CI blind spots.** 12 committed test files execute in no lane — 8 excluded as "bun:test" despite importing from `vitest`, plus 4 genuine bun:test files with no runner; the lane guard checks only aggregate per-lane count bands and cannot detect a file in zero lanes. Root `tsconfig.json:20` excludes `services/platform/**/*` from typecheck entirely. | High | **Promote to a dedicated CI-blind-spot sprint once ci-fast is green and ci-integration schedulable (S31-OPS-05) and stable for one sprint:** fix the false exclusion, add a bun:test lane, include `services/platform` in typecheck, rewrite the guard to assert zero-orphan. *(devops-engineer)* |
| R28 | **Cutover verified against a hermetic replica.** Sprint 29's go/no-go ran with `HOLO_GO_NO_GO_DATABASE_URL` pointed at a local replica and Convex at loopback; the only positive rollback-repoint artifact was generated in a since-deleted worktree; every rollback report carries `acknowledgements: []`; drain covered 2 tables with a 30-second quiet window; Sprint 29 has no `gate-verification.json`. | High | **Must be closed before any cutover-execution sprint or Sprint 32 is scheduled:** re-run go/no-go against real production coordinates, regenerate and durably store a positive rollback artifact, widen drain coverage. *(devops-engineer)* |
| R29 | **Test-reachable-only production code.** A module imported by tests but with no production call site keeps its suite green while shipped behaviour is gone. Already occurred once: commit `c480051d` removed the Langfuse exporter call site one day after Sprint 12's gate, leaving `observability/mission-research.ts:156` reachable only from tests. | Med | For any AC of the form "the running system does X", the test must invoke the production entry point (CLI or HTTP route), never the implementing module. **Promote if a second capability is found with a passing suite and no production call site.** *(mastra-planner)* |
| R30 | **Non-gating integrity audits.** `etl/fk-audit.ts:150-155` reads the enforced-constraint count but `:158` derives `ok` from `issues.length` alone, so it returns `ok:true` against a database with zero FK constraints on migrated domain tables. Any audit that computes a metric it does not gate on is decorative. | Med | Make `ok` require every derived edge to have a matching enforced constraint (S31-CX-04). **Promote to High if the validation-time-only constraint decision lands without a durable record of which edges were validated.** *(convex-planner)* |
| R31 | **Dependency ordering asserted, not derived.** The "FK-ordered" ETL load is a hand-maintained 36-element constant with an alphabetical fallback; in a real 60-table load roughly 24 tables take the alphabetical path. | Med | Derive the order topologically from the referential edge set (S31-CX-04). **Promote to blocking if the derived order differs from the executed Sprint 29 order in a way that could have produced dangling references.** *(convex-planner)* |
| R32 | **Checkpoint theatre.** `etl_runs.checkpoint` is written at four points and read nowhere — "checkpointed replay" does not exist; recovery is idempotency-by-upsert over the whole corpus. | Med | Either implement and prove the resume path with an interrupted-load test, or remove the column and the claim. **Promote to High if any future load exceeds a duration where a wholesale re-run is not operationally acceptable.** *(convex-planner)* |
| R33 | **Skipped RED assertions are absent evidence.** The Convex half of the write-fence RED assertions was skipped in the accepted Sprint 29 run (`SKIP convex fence reset: loopback Convex HTTP not reachable`), so the accepted evidence proves the platform-side fence but not the Convex-side reset oracle. | Med | A cutover gate must report skipped assertions as a distinct non-pass outcome. **Promote to High if the Convex-side oracle cannot be executed before the deployment is deleted — after Sprint 32 it is permanently unprovable.** *(convex-planner)* |
| R34 | **MCP mutation idempotency is application-enforced, not DB-enforced.** The Postgres MCP executor implements each tool's declared idempotency key by reading before writing, with no unique constraint behind it, for every mutation tool except the two constrained in Sprint 31. Under the single-keyholder trust model the interleaving window is not reachable in practice. | Med | Accepted for cutover. **Promote when: a second concurrent MCP/Agent Client session is introduced, a Mission Engine workflow or pg-boss job invokes an MCP mutation tool on a schedule, or a duplicate row is observed for a single declared idempotency key.** *(mcp-planner)* |
| R35 | **External-dependency tools inside the cutover gate.** Two of the 44 MCP tools (`shop_products`, `findRecommendations`) reach a third-party search API on every invocation, so a vendor outage can turn the CAP-CUT-01 gate red for reasons unrelated to the migration. | Med | Named allowlist whose contents are themselves asserted, degrading the gate to 42/44 with a named cause. **Promote when the strict sweep fails twice consecutively on an allowlisted tool while every Postgres-only tool passes.** *(mcp-planner)* |
| R36 | **The MCP behavioural proof is opt-in; the always-on proof is static.** Every live MCP assertion is skipped without `PLATFORM_IT`, and the unconditional gate (`mcp:verify-rehost`) establishes only that a dispatch case exists by source regex — `case 'x': throw` still counts. | Med | The cutover gate lane must fail rather than skip when `PLATFORM_IT` is unset; the regex check is a fast pre-filter, never the parity proof. **Promote when the strict sweep is observed skipped in any lane reporting a cutover-gate verdict.** *(mcp-planner)* |
| R37 | **Phantom rollback surfaces.** `holocron-mcp/src/convex/client.ts:14` constructs a Convex browser client against `PLATFORM_URL`, not the Convex cloud deployment, and `rollback-repoint.ts` never references the package — so it is neither a working legacy path nor a migrated one, while presenting as a fallback. | Med | A component may only be counted as part of the rollback path if the rollback drill exercises it. Resolved by S31-05. **Promote to High if any rollback runbook names this package as a fallback.** *(convex-planner)* |
| R38 | **Scaffolded quantitative inputs in the business report template.** The four collapsed business pipelines derive market-sizing and DVF figures from a hash of their inputs while the surrounding narrative is genuine fleet output — readers cannot tell the two apart. | Med | Every scaffolded figure must be labelled as such in the rendered report until live sourcing is funded. **Promote if a business report is shared outside the operator, acted on for a financial or hiring decision, or consumed by fulcrum as evidence.** *(mastra-planner)* |
| R39 | **Read-error detection is deadline-based, not connection-based.** The RN client has no Zero connection-state signal, so an unreachable zero-cache is inferred from a row staying `undefined` past a wall-clock deadline; a slow first sync on a cold cache is indistinguishable from an outage. | Med | Deadline set generously (≥15s) and validated against a cold-launch/warm-cache run. **Promote if a spurious error is observed on a healthy cold launch, or if Zero ships a connection-state API.** *(react-native-ui-planner)* |
| R40 | **A gate fix exists only on an unmerged branch.** The Sprint 26 voice-cancel Maestro step is blocked by a simulator-harness limitation; its documented remediation sits on `task/GATE-FIX-S26-04` and is not in `main`. | Med | Merge or formally abandon the branch before Sprint 32. **Promote to a blocking Sprint 32 precondition if still unmerged when the decommission sprint opens.** *(react-native-ui-planner)* |
| R41 | **Type-only Convex imports block deletion as hard as runtime imports.** Three RN components import types from `@/convex/_generated/dataModel`, so `convex/` must survive for typecheck. | Med | Replaced in S31-FE-05. **Hard blocker for Sprint 32 as written.** *(convex-planner + react-native-ui-planner)* |
| R42 | **State outside the backup chain.** CAP-BAK-01 covers Postgres and the blob store only; filesystem-persisted state (currently the `extract:status` cache in `.tmp/`) is not restorable after total loss and is not exercised by the restore drill. | Low | Accepted while such state is ephemeral and reconstructible. **Promote if any filesystem-persisted state becomes non-reconstructible, or if a restore drill reveals an operator workflow that cannot resume.** *(mastra-planner)* |
| R43 | **MCP client setup documentation points at the decommission target.** `README.md:629-640` documents the legacy Convex-backed package as the way to attach an Agent Client; Sprint 32 deletes it. | Low | Repointing the README is a required Sprint 32 deliverable. **Promote to High the moment the decommission branch removing `holocron-mcp/` is opened.** *(mcp-planner)* |
| R44 | **The 47-files / 105-call-sites headline is a normalized target, not a measurement.** The measured pre-migration state recorded by Sprint 21 itself was 46 importing files and 152 lexical hook lines; the file count includes 2 already-migrated Zero files and 2 test files. | Low | Recorded in the contract freeze tombstone. **Promote if any Sprint 32+ gate asserts a literal count of 47 files or 105 call-sites against live source — that assertion cannot pass and would block decommission on a bookkeeping artifact.** *(react-native-ui-planner)* |
| R45 | **Frozen-contract / client-schema divergence.** Two client-data-contract entries target `subscription_links`, a `zero_pub` table absent from the RN client schema. The subscriptions surface works today, so this is spec drift in a frozen artifact. | Low | No remediation planned; recorded in the freeze tombstone. **Promote only if a subscriptions surface is observed failing to read link data on-device.** *(react-native-ui-planner)* |
| R46 | **Non-cryptographic share-token fallback.** `app/document/[id].tsx` falls back to a `Math.random`-derived share token when the server does not grant one; the branch is dead in the verified gate path and the app is tailnet-only. | Low | Not remediated in Sprint 31. **Promote the moment the fallback branch becomes reachable in a gate path, or if the trust boundary widens beyond the tailnet.** *(react-native-ui-planner)* |
| R47 | **Banner-consistency drift.** If the sync-failure and stall-timeout fixes land across multiple PRs without a shared component, the app could end up with several visually similar but subtly inconsistent "unavailable" banners without anyone intending a redesign. | Low | Sprint 31 acceptance criteria require a single shared banner component. **Promote to Med if the research-screen and chat-screen fixes are assigned to different engineers/PRs without that requirement in the AC.** *(frontend-designer)* |
| R48 | **Last live Anthropic call path.** `convex/research/intent.ts:17,37` still calls `claudeFlash`, conflicting with the standing zero-Anthropic-inference policy. It is runtime-fenced but present. | Low | Resolved by Sprint 32's deletion of `convex/`. Recorded so it is verified as removed rather than assumed. **Promote to High if the migration fence is ever lifted on `convex/research`, or if a Sprint 32 slip leaves `convex/` in place past the decommission window.** *(convex-planner)* |
