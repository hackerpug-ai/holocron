# E4-S09: Add feedback history to Settings

> Epic: epic-4-feedback | AC: "User can navigate to feedback history from Settings" | Files: 1

## Files Owned
- `screens/settings-screen.tsx` (MODIFY)

## Conventions to Inline
- Use router.push for navigation
- Import ThumbsUp icon from @/components/ui/icons
- Use ChevronRight for navigation indicator
- Add testID for E2E testing

## Draft Description

Add "Feedback History" section to Settings screen.
Import ThumbsUp and ChevronRight icons from @/components/ui/icons.
Add Pressable with testID="settings-feedback-history".
OnPress: router.push('/settings/feedback').
Display row with: ThumbsUp icon (left), "Feedback History" Text, ChevronRight (right).
Use existing Settings row pattern (rounded-2xl, border, bg-card, p-4).
Place in appropriate section (after existing rows, before footer).
Follow existing Settings styling conventions.

## Test Block

```typescript
describe('Settings - Feedback History Link', () => {
  it('renders Feedback History row in Settings', () => {
    // Row with icon, text, chevron visible
  });
  it('navigates to feedback history when tapped', () => {
    // router.push('/settings/feedback') called
  });
});
```

## Dependencies
E4-S08 (FeedbackHistoryScreen must exist first)

## Group
4
