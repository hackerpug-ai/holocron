# E4-S01: Integrate FeedbackButtons into VideoCard

> Epic: epic-4-feedback | AC: "User can tap thumbs up/down on video cards" | Files: 1

## Files Owned
- `components/subscriptions/VideoCard.tsx` (MODIFY)

## Conventions to Inline
- Import FeedbackButtons from `@/components/subscriptions/FeedbackButtons`
- Use named export for VideoCard component
- Add testID prop for E2E testing
- Pass onFeedback callback to parent via onPress prop

## Draft Description

Integrate FeedbackButtons into VideoCard component.
Import FeedbackButtons from `@/components/subscriptions/FeedbackButtons`.
Add `onFeedback?: (type: 'positive' | 'negative' | null) => void` prop to VideoCardProps interface.
Place FeedbackButtons in top-right corner of card (absolute positioning).
Use `testID={testID ? `${testID}-feedback` : 'video-card-feedback'}` for FeedbackButtons.
Connect FeedbackButtons onFeedback to parent onFeedback prop callback.
Minimum 44x44 hitbox already handled by FeedbackButtons component.
Do NOT modify FeedbackButtons component — only integrate it.

## Test Block

```typescript
describe('VideoCard - Feedback Integration', () => {
  it('renders FeedbackButtons in top-right corner', () => {
    // FeedbackButtons visible on card
  });
  it('calls onFeedback when thumbs up tapped', () => {
    // onFeedback('positive') called
  });
  it('calls onFeedback when thumbs down tapped', () => {
    // onFeedback('negative') called
  });
});
```

## Dependencies
None

## Group
1
