# E4-S08: Create FeedbackHistoryScreen

> Epic: epic-4-feedback | AC: "User can view feedback history in Settings" | Files: 2

## Files Owned
- `screens/FeedbackHistoryScreen.tsx` (NEW)
- `app/(drawer)/settings/feedback.tsx` (NEW)

## Conventions to Inline
- Use useQuery and useMutation from Convex
- Import Text from @/components/ui/text
- Use ScrollView with testID
- Named export for screen component

## Draft Description

Create FeedbackHistoryScreen in screens/FeedbackHistoryScreen.tsx.
Use useQuery(api.feedback.getHistory) to fetch feedback.
Use useMutation(api.feedback.undo) for undo action.
Display list with: finding title, feedback icon (ThumbsUp/ThumbsDown), relative time, undo button.
Show empty state: "No feedback yet" message when history empty.
Create route at app/(drawer)/settings/feedback.tsx: export default function FeedbackHistoryRoute().
Use ScrollView with testID="feedback-history-screen".
Format relative time: "Today", "Yesterday", "X days ago", "X weeks ago", "X months ago".
Import icons from @/components/ui/icons.
Use theme colors for feedback type (primary=up, destructive=down).

## Test Block

```typescript
describe('FeedbackHistoryScreen', () => {
  it('renders list of feedback items', () => {
    // Items with title, icon, time, undo button
  });
  it('shows empty state when no feedback', () => {
    // "No feedback yet" message visible
  });
  it('calls undo mutation when undo tapped', () => {
    // Mutation called with finding ID
  });
  it('formats relative time correctly', () => {
    // "Today", "Yesterday", "3 days ago"
  });
});
```

## Dependencies
E4-S05 (queries must exist first)

## Group
3
