# E4-S02: Integrate FeedbackButtons into SocialCard

> Epic: epic-4-feedback | AC: "User can tap thumbs up/down on social cards" | Files: 1

## Files Owned
- `components/subscriptions/SocialCard.tsx` (MODIFY)

## Conventions to Inline
- Import FeedbackButtons from `@/components/subscriptions/FeedbackButtons`
- Use named export for SocialCard component
- Add testID prop for E2E testing
- Pass onFeedback callback to parent via onPress prop

## Draft Description

Integrate FeedbackButtons into SocialCard component.
Import FeedbackButtons from `@/components/subscriptions/FeedbackButtons`.
Add `onFeedback?: (type: 'positive' | 'negative' | null) => void` prop to SocialCardProps interface.
Place FeedbackButtons in top-right corner of card (absolute positioning).
Use `testID={testID ? `${testID}-feedback` : 'social-card-feedback'}` for FeedbackButtons.
Connect FeedbackButtons onFeedback to parent onFeedback prop callback.
Follow same pattern as VideoCard integration.
Do NOT modify FeedbackButtons component.

## Test Block

```typescript
describe('SocialCard - Feedback Integration', () => {
  it('renders FeedbackButtons in top-right corner', () => {
    // FeedbackButtons visible on card
  });
  it('calls onFeedback when thumbs up tapped', () => {
    // onFeedback('positive') called
  });
  it('calls onFeedback when thumbs down tapped', () => {
    // onFeedback('negative') called
  });
});
```

## Dependencies
None

## Group
1
