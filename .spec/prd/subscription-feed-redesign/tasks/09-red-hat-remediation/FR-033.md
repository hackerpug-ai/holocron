================================================================================
TASK: FR-033 - Fix feed builder to group by creatorProfileId
================================================================================

TASK_TYPE: INFRA
STATUS: Backlog
TDD_PHASE: RED
CURRENT_AC: AC-1
PRIORITY: P0
EFFORT: M
TYPE: DEV
ITERATION: 1

--------------------------------------------------------------------------------
CRITICAL CONSTRAINTS
--------------------------------------------------------------------------------

MUST: Group subscriptionContent by creatorProfileId (not sourceId)
MUST: When creatorProfileId is unavailable, fall back to sourceId grouping
MUST: Set creatorProfileId on created feedItems (not undefined)
MUST: Update groupKey format to use creatorProfileId where available
NEVER: Set creatorProfileId: undefined when a profile exists
STRICTLY: One feed item per creator (across all their platforms)

--------------------------------------------------------------------------------
SPECIFICATION
--------------------------------------------------------------------------------

**Objective**: Fix the buildFeed internal action to group content by creatorProfileId so that a creator with multiple platforms (YouTube + Twitter + blog) produces one feed item, not three.

**Success looks like**:
- Creator with YouTube + Twitter subscriptions -> ONE feed item (not two)
- feedItem.creatorProfileId is populated (not undefined)
- groupKey uses creator:{creatorProfileId} format
- Fallback to source:{sourceId} when no creatorProfile linked

--------------------------------------------------------------------------------
PREREQUISITES
--------------------------------------------------------------------------------

| Phase | Document | Lines/Section | Purpose |
|-------|----------|---------------|---------|
| BEFORE_START | convex/feeds/internal.ts | buildFeed function | Current sourceId grouping logic |
| BEFORE_START | convex/schema.ts | subscriptionSources table | creatorProfileId field |
| BEFORE_START | convex/schema.ts | creatorProfiles table | Creator profile structure |

--------------------------------------------------------------------------------
ACCEPTANCE CRITERIA
--------------------------------------------------------------------------------

AC-1: Content grouped by creatorProfileId
  GIVEN: Two subscriptionSources with same creatorProfileId
  WHEN: buildFeed runs
  THEN: Content from both sources grouped into ONE feedItem
  VERIFY: pnpm tsc --noEmit

AC-2: creatorProfileId set on feedItem
  GIVEN: buildFeed creates a new feedItem
  WHEN: Source has a creatorProfileId
  THEN: feedItem.creatorProfileId = source.creatorProfileId (not undefined)
  VERIFY: grep "creatorProfileId:" convex/feeds/internal.ts | grep -v undefined

AC-3: Fallback for sources without creatorProfile
  GIVEN: subscriptionSource has no creatorProfileId
  WHEN: buildFeed runs
  THEN: Groups by sourceId as fallback, groupKey = "source:{sourceId}"
  VERIFY: pnpm tsc --noEmit

AC-4: groupKey uses creator prefix
  GIVEN: Source has creatorProfileId
  WHEN: feedItem created
  THEN: groupKey = "creator:{creatorProfileId}" (not "source:{sourceId}")
  VERIFY: grep "groupKey" convex/feeds/internal.ts

--------------------------------------------------------------------------------
GUARDRAILS
--------------------------------------------------------------------------------

WRITE-ALLOWED (explicit file list):
- convex/feeds/internal.ts (MODIFY — fix grouping logic in buildFeed)

WRITE-PROHIBITED:
- convex/schema.ts - Schema already has creatorProfileId field
- convex/feeds/queries.ts - Query changes in separate task

--------------------------------------------------------------------------------
AGENT ASSIGNMENT
--------------------------------------------------------------------------------

**Implementation Agent**: convex-implementer
**Rationale**: Convex internal action modification requiring data model understanding

**Review Agent**: convex-reviewer
**Rationale**: Validate data integrity of grouping logic

--------------------------------------------------------------------------------
DEPENDENCIES
--------------------------------------------------------------------------------

Depends On: None (can start immediately)

Blocks: None (independent backend fix)

--------------------------------------------------------------------------------
APPROVAL
--------------------------------------------------------------------------------

Approved By: [pending]
Date: [pending]

================================================================================
