================================================================================
TASK: FR-036 - Add morning digest cron job
================================================================================

TASK_TYPE: INFRA
STATUS: Backlog
TDD_PHASE: RED
CURRENT_AC: AC-1
PRIORITY: P1
EFFORT: S
TYPE: DEV
ITERATION: 1

--------------------------------------------------------------------------------
CRITICAL CONSTRAINTS
--------------------------------------------------------------------------------

MUST: Add cron job in convex/crons.ts that runs daily at 9 AM
MUST: Cron calls createDigestNotification or equivalent internal action
MUST: Follow existing cron pattern in crons.ts
NEVER: Create duplicate cron entries
STRICTLY: Use Convex crons.daily() or crons.cron() with proper schedule

--------------------------------------------------------------------------------
SPECIFICATION
--------------------------------------------------------------------------------

**Objective**: Add the missing morning digest cron job that sends a daily digest notification at 9 AM.

**Success looks like**:
- `convex/crons.ts` contains a daily 9 AM cron entry
- Cron calls the digest notification creation function
- Existing feed-builder cron (every 2 hours) is unchanged
- Type check passes

--------------------------------------------------------------------------------
PREREQUISITES
--------------------------------------------------------------------------------

| Phase | Document | Lines/Section | Purpose |
|-------|----------|---------------|---------|
| BEFORE_START | convex/crons.ts | ALL | Existing cron patterns |
| BEFORE_START | convex/feeds/mutations.ts | createDigestNotification | Function to call |

--------------------------------------------------------------------------------
ACCEPTANCE CRITERIA
--------------------------------------------------------------------------------

AC-1: Morning digest cron exists
  GIVEN: convex/crons.ts is deployed
  WHEN: Inspecting cron entries
  THEN: Daily 9 AM cron job for digest notification exists
  VERIFY: grep -i "digest\|morning\|9.*AM\|0 9" convex/crons.ts

AC-2: Cron calls digest function
  GIVEN: 9 AM cron fires
  WHEN: Handler executes
  THEN: createDigestNotification or equivalent is called
  VERIFY: pnpm tsc --noEmit

AC-3: Existing crons unchanged
  GIVEN: Existing feed-builder cron
  WHEN: New cron added
  THEN: Feed-builder 2-hour cron still present and unchanged
  VERIFY: grep "feed-builder\|buildFeed" convex/crons.ts

--------------------------------------------------------------------------------
GUARDRAILS
--------------------------------------------------------------------------------

WRITE-ALLOWED (explicit file list):
- convex/crons.ts (MODIFY — add daily digest cron)

WRITE-PROHIBITED:
- convex/feeds/mutations.ts - createDigestNotification already exists
- convex/feeds/internal.ts - No changes needed

--------------------------------------------------------------------------------
AGENT ASSIGNMENT
--------------------------------------------------------------------------------

**Implementation Agent**: convex-implementer
**Rationale**: Convex cron configuration

**Review Agent**: convex-reviewer
**Rationale**: Validate cron schedule correctness

--------------------------------------------------------------------------------
DEPENDENCIES
--------------------------------------------------------------------------------

Depends On: None (can start immediately)

Blocks: None

--------------------------------------------------------------------------------
APPROVAL
--------------------------------------------------------------------------------

Approved By: [pending]
Date: [pending]

================================================================================
