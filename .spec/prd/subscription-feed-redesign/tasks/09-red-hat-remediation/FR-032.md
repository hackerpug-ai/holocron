================================================================================
TASK: FR-032 - Implement real infinite scroll and pull-to-refresh
================================================================================

TASK_TYPE: FEATURE
STATUS: Backlog
TDD_PHASE: RED
CURRENT_AC: AC-1
PRIORITY: P0
EFFORT: M
TYPE: DEV
ITERATION: 1

--------------------------------------------------------------------------------
CRITICAL CONSTRAINTS
--------------------------------------------------------------------------------

MUST: Use FlatList with onEndReached for infinite scroll
MUST: Implement cursor-based pagination in useSubscriptionFeed hook
MUST: Add RefreshControl with real refetch mechanism
MUST: Backend getFeed query must support cursor/offset pagination
NEVER: Use console.log stubs for loadMore or handleRefresh
NEVER: Use ScrollView with .map() for the feed list
STRICTLY: Load items in batches of 20

--------------------------------------------------------------------------------
SPECIFICATION
--------------------------------------------------------------------------------

**Objective**: Replace the console.log stubs in loadMore and handleRefresh with working pagination and refresh implementations.

**Success looks like**:
- Scrolling to bottom loads 20 more items
- Pull-to-refresh fetches latest content
- Loading indicator shows during pagination
- No duplicate items after refresh

--------------------------------------------------------------------------------
PREREQUISITES
--------------------------------------------------------------------------------

| Phase | Document | Lines/Section | Purpose |
|-------|----------|---------------|---------|
| BEFORE_START | hooks/use-subscription-feed.ts | ALL | Current stub implementation |
| BEFORE_START | convex/feeds/queries.ts | getFeed function | Current query signature |

--------------------------------------------------------------------------------
ACCEPTANCE CRITERIA
--------------------------------------------------------------------------------

AC-1: Infinite scroll triggers loadMore
  GIVEN: Feed has more than 20 items
  WHEN: User scrolls to bottom of list
  THEN: Next 20 items load and append to list
  VERIFY: pnpm test -- use-subscription-feed (test pagination loads more)

AC-2: Loading indicator during pagination
  GIVEN: User scrolled to bottom, loadMore triggered
  WHEN: Next batch is loading
  THEN: Footer loading indicator visible
  VERIFY: pnpm test -- SubscriptionFeedScreen (test loading footer)

AC-3: Pull-to-refresh fetches latest
  GIVEN: Feed is displayed
  WHEN: User pulls down to refresh
  THEN: Feed data refetches from Convex
  VERIFY: pnpm test -- SubscriptionFeedScreen (test refresh)

AC-4: No duplicate items after operations
  GIVEN: User has scrolled and loaded multiple batches
  WHEN: Pull-to-refresh triggers
  THEN: No duplicate feed items in the list
  VERIFY: pnpm test -- use-subscription-feed (test no duplicates)

AC-5: Backend supports pagination cursor
  GIVEN: getFeed query called with cursor/offset
  WHEN: Query executes
  THEN: Returns next batch starting from cursor position
  VERIFY: pnpm tsc --noEmit (types match)

--------------------------------------------------------------------------------
GUARDRAILS
--------------------------------------------------------------------------------

WRITE-ALLOWED (explicit file list):
- hooks/use-subscription-feed.ts (MODIFY — real pagination)
- components/subscriptions/SubscriptionFeedScreen.tsx (MODIFY — wire FlatList props)
- convex/feeds/queries.ts (MODIFY — add cursor support to getFeed)
- convex/feeds/validators.ts (MODIFY — add cursor arg)

WRITE-PROHIBITED:
- convex/schema.ts - No schema changes needed

--------------------------------------------------------------------------------
AGENT ASSIGNMENT
--------------------------------------------------------------------------------

**Implementation Agent**: react-vite-ui-implementer
**Rationale**: Cross-cutting frontend + backend pagination implementation

**Review Agent**: react-native-ui-reviewer
**Rationale**: FlatList pagination patterns

--------------------------------------------------------------------------------
DEPENDENCIES
--------------------------------------------------------------------------------

Depends On:
- [FR-030] Unify SubscriptionFeedScreen (canonical screen must exist)

Blocks: None

--------------------------------------------------------------------------------
APPROVAL
--------------------------------------------------------------------------------

Approved By: [pending]
Date: [pending]

================================================================================
