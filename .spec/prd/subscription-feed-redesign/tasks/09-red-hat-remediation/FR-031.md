================================================================================
TASK: FR-031 - Wire SubscriptionFeedItem card component into live feed
================================================================================

TASK_TYPE: FEATURE
STATUS: Backlog
TDD_PHASE: RED
CURRENT_AC: AC-1
PRIORITY: P0
EFFORT: M
TYPE: DEV
ITERATION: 1

--------------------------------------------------------------------------------
CRITICAL CONSTRAINTS
--------------------------------------------------------------------------------

MUST: Import and render SubscriptionFeedItem as the FlatList renderItem
MUST: Pass onPress that opens content via useWebView hook
MUST: All 3 variants (video/blog/social) render based on item.contentType
NEVER: Render raw Pressable+Text for feed items (the current anti-pattern)
NEVER: Duplicate WebViewSheet per card (mount ONE at screen level)
STRICTLY: SubscriptionFeedItem receives data props, not Convex queries

--------------------------------------------------------------------------------
SPECIFICATION
--------------------------------------------------------------------------------

**Objective**: Replace the inline Pressable+Text feed item rendering with the SubscriptionFeedItem component that supports video/blog/social card variants.

**Success looks like**:
- FlatList renderItem uses SubscriptionFeedItem
- Video items show thumbnail with duration overlay
- Blog items show excerpt with read time
- Social items show engagement stats
- Tapping any card opens content in WebViewSheet

--------------------------------------------------------------------------------
PREREQUISITES
--------------------------------------------------------------------------------

| Phase | Document | Lines/Section | Purpose |
|-------|----------|---------------|---------|
| BEFORE_START | components/subscriptions/SubscriptionFeedItem.tsx | ALL | Understand card variant props |
| BEFORE_START | hooks/useWebView.ts | ALL | Understand WebView integration pattern |
| BEFORE_START | components/webview/WebViewSheet.tsx | ALL | Understand sheet mounting pattern |

--------------------------------------------------------------------------------
ACCEPTANCE CRITERIA
--------------------------------------------------------------------------------

AC-1: FlatList uses SubscriptionFeedItem as renderItem
  GIVEN: Feed screen renders with feed items
  WHEN: FlatList renders a row
  THEN: SubscriptionFeedItem component is used (not raw Pressable)
  VERIFY: grep "SubscriptionFeedItem" components/subscriptions/SubscriptionFeedScreen.tsx

AC-2: Video variant renders correctly
  GIVEN: Feed item has contentType: "video"
  WHEN: Card renders
  THEN: Shows thumbnail image with duration overlay badge
  VERIFY: pnpm test -- SubscriptionFeedItem (test video variant)

AC-3: Blog variant renders correctly
  GIVEN: Feed item has contentType: "blog"
  WHEN: Card renders
  THEN: Shows excerpt text with read time estimate
  VERIFY: pnpm test -- SubscriptionFeedItem (test blog variant)

AC-4: Social variant renders correctly
  GIVEN: Feed item has contentType: "social"
  WHEN: Card renders
  THEN: Shows engagement stats (likes, comments)
  VERIFY: pnpm test -- SubscriptionFeedItem (test social variant)

AC-5: Card tap opens WebViewSheet
  GIVEN: User taps a feed item card
  WHEN: onPress fires
  THEN: WebViewSheet opens with the item's content URL
  VERIFY: pnpm test -- SubscriptionFeedScreen (test WebView opens on press)

AC-6: Single WebViewSheet mounted at screen level
  GIVEN: Feed screen renders
  WHEN: Inspecting component tree
  THEN: ONE WebViewSheet at screen level (not per-card)
  VERIFY: grep -c "WebViewSheet" components/subscriptions/SubscriptionFeedScreen.tsx (expect 1)

--------------------------------------------------------------------------------
GUARDRAILS
--------------------------------------------------------------------------------

WRITE-ALLOWED (explicit file list):
- components/subscriptions/SubscriptionFeedScreen.tsx (MODIFY — update renderItem)

WRITE-PROHIBITED:
- components/subscriptions/SubscriptionFeedItem.tsx - Card component already built
- convex/** - Backend changes in separate tasks

--------------------------------------------------------------------------------
AGENT ASSIGNMENT
--------------------------------------------------------------------------------

**Implementation Agent**: react-vite-ui-implementer
**Rationale**: React Native component wiring with WebView integration

**Review Agent**: react-native-ui-reviewer
**Rationale**: Verify card variants render correctly

--------------------------------------------------------------------------------
DEPENDENCIES
--------------------------------------------------------------------------------

Depends On:
- [FR-030] Unify SubscriptionFeedScreen (must have canonical screen first)

Blocks:
- [FR-037] Fix hardcoded colors/spacing (cards must be wired before fixing tokens)

--------------------------------------------------------------------------------
APPROVAL
--------------------------------------------------------------------------------

Approved By: [pending]
Date: [pending]

================================================================================
