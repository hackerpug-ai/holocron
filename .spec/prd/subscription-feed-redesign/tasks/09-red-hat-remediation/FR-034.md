================================================================================
TASK: FR-034 - Initialize inFeed field on subscriptionContent records
================================================================================

TASK_TYPE: INFRA
STATUS: Backlog
TDD_PHASE: RED
CURRENT_AC: AC-1
PRIORITY: P0
EFFORT: S
TYPE: DEV
ITERATION: 1

--------------------------------------------------------------------------------
CRITICAL CONSTRAINTS
--------------------------------------------------------------------------------

MUST: Set inFeed: false on all new subscriptionContent inserts
MUST: Create backfill mutation to set inFeed: false on existing records where inFeed is undefined
MUST: Fix getUnprocessedContent filter to handle both undefined and false
NEVER: Delete or modify existing subscriptionContent records beyond setting inFeed
STRICTLY: Backfill must be idempotent (safe to run multiple times)

--------------------------------------------------------------------------------
SPECIFICATION
--------------------------------------------------------------------------------

**Objective**: Ensure the inFeed field is properly initialized on all subscriptionContent records so the feed builder's filter `q.eq(q.field("inFeed"), false)` matches them.

**Success looks like**:
- New subscriptionContent records always have `inFeed: false` set on insert
- Existing records with `inFeed: undefined` are backfilled to `inFeed: false`
- getUnprocessedContent returns both new and pre-existing content
- Feed builder produces output for all real data

--------------------------------------------------------------------------------
PREREQUISITES
--------------------------------------------------------------------------------

| Phase | Document | Lines/Section | Purpose |
|-------|----------|---------------|---------|
| BEFORE_START | convex/subscriptions/internal.ts | insert("subscriptionContent") calls | Find all insert points |
| BEFORE_START | convex/feeds/internal.ts | getUnprocessedContent | Current filter logic |
| BEFORE_START | convex/whatsNew.ts | insert("subscriptionContent") | Additional insert point |

--------------------------------------------------------------------------------
ACCEPTANCE CRITERIA
--------------------------------------------------------------------------------

AC-1: New inserts include inFeed: false
  GIVEN: New subscriptionContent is created via subscriptions/internal.ts
  WHEN: Record is inserted
  THEN: inFeed field is set to false
  VERIFY: grep -A5 "insert.*subscriptionContent" convex/subscriptions/internal.ts | grep inFeed

AC-2: whatsNew inserts include inFeed: false
  GIVEN: New subscriptionContent is created via whatsNew.ts
  WHEN: Record is inserted
  THEN: inFeed field is set to false
  VERIFY: grep -A5 "insert.*subscriptionContent" convex/whatsNew.ts | grep inFeed

AC-3: Backfill mutation exists
  GIVEN: Backfill mutation is available
  WHEN: Called from Convex dashboard
  THEN: All records with inFeed: undefined are updated to inFeed: false
  VERIFY: pnpm tsc --noEmit

AC-4: getUnprocessedContent handles undefined
  GIVEN: Mix of records with inFeed: undefined and inFeed: false
  WHEN: getUnprocessedContent runs
  THEN: Returns ALL unprocessed records (both undefined and false)
  VERIFY: grep "inFeed" convex/feeds/internal.ts (filter handles both cases)

--------------------------------------------------------------------------------
GUARDRAILS
--------------------------------------------------------------------------------

WRITE-ALLOWED (explicit file list):
- convex/subscriptions/internal.ts (MODIFY — add inFeed: false to inserts)
- convex/whatsNew.ts (MODIFY — add inFeed: false to inserts)
- convex/feeds/internal.ts (MODIFY — fix getUnprocessedContent filter)
- convex/feeds/mutations.ts (NEW function — backfillInFeed)

WRITE-PROHIBITED:
- convex/schema.ts - Field already defined as v.optional(v.boolean())

--------------------------------------------------------------------------------
AGENT ASSIGNMENT
--------------------------------------------------------------------------------

**Implementation Agent**: convex-implementer
**Rationale**: Convex data migration and mutation work

**Review Agent**: convex-reviewer
**Rationale**: Validate backfill safety and idempotency

--------------------------------------------------------------------------------
DEPENDENCIES
--------------------------------------------------------------------------------

Depends On: None (can start immediately)

Blocks: None (independent backend fix)

--------------------------------------------------------------------------------
APPROVAL
--------------------------------------------------------------------------------

Approved By: [pending]
Date: [pending]

================================================================================
