================================================================================
TASK: FR-035 - Implement settings persistence in Convex
================================================================================

TASK_TYPE: FEATURE
STATUS: Backlog
TDD_PHASE: RED
CURRENT_AC: AC-1
PRIORITY: P1
EFFORT: M
TYPE: DEV
ITERATION: 1

--------------------------------------------------------------------------------
CRITICAL CONSTRAINTS
--------------------------------------------------------------------------------

MUST: updateFeedSettings writes settings to Convex database (not console.log)
MUST: getFeedSettings reads settings from Convex database (not hardcoded defaults)
MUST: Settings survive app restart
NEVER: Store settings in AsyncStorage or local state only
NEVER: Return hardcoded defaults when real settings exist
STRICTLY: Use existing feedSettings field or create appropriate storage

--------------------------------------------------------------------------------
SPECIFICATION
--------------------------------------------------------------------------------

**Objective**: Replace the console.log stubs in updateFeedSettings and getFeedSettings with real Convex persistence so settings survive app restarts.

**Success looks like**:
- User changes a setting in SubscriptionSettingsModal
- Setting is written to Convex via updateFeedSettings mutation
- Closing and reopening the modal shows the saved setting
- getFeedSettings returns real values (not hardcoded defaults)

--------------------------------------------------------------------------------
PREREQUISITES
--------------------------------------------------------------------------------

| Phase | Document | Lines/Section | Purpose |
|-------|----------|---------------|---------|
| BEFORE_START | convex/feeds/mutations.ts | updateFeedSettings | Current stub implementation |
| BEFORE_START | convex/feeds/queries.ts | getFeedSettings | Current hardcoded defaults |
| BEFORE_START | convex/schema.ts | feedItems or userSettings | Storage location |

--------------------------------------------------------------------------------
ACCEPTANCE CRITERIA
--------------------------------------------------------------------------------

AC-1: updateFeedSettings persists to Convex
  GIVEN: User changes enableNotifications to false
  WHEN: updateFeedSettings mutation called
  THEN: Setting written to Convex database
  VERIFY: pnpm tsc --noEmit

AC-2: getFeedSettings reads from Convex
  GIVEN: Settings were previously saved
  WHEN: getFeedSettings query called
  THEN: Returns saved settings (not hardcoded defaults)
  VERIFY: pnpm tsc --noEmit

AC-3: Settings survive app restart
  GIVEN: User saved settings
  WHEN: App is restarted and settings modal opened
  THEN: Previously saved settings are displayed
  VERIFY: Manual test — change setting, refresh, verify

AC-4: Defaults used when no settings saved
  GIVEN: No settings have been saved yet
  WHEN: getFeedSettings called
  THEN: Returns sensible defaults (notifications: true, thumbnails: true, etc.)
  VERIFY: pnpm tsc --noEmit

--------------------------------------------------------------------------------
GUARDRAILS
--------------------------------------------------------------------------------

WRITE-ALLOWED (explicit file list):
- convex/feeds/mutations.ts (MODIFY — implement updateFeedSettings)
- convex/feeds/queries.ts (MODIFY — implement getFeedSettings)
- convex/schema.ts (MODIFY — add feedSettings table or field if needed)

WRITE-PROHIBITED:
- components/** - Frontend reads from query, no changes needed

--------------------------------------------------------------------------------
AGENT ASSIGNMENT
--------------------------------------------------------------------------------

**Implementation Agent**: convex-implementer
**Rationale**: Convex mutation/query implementation with persistence

**Review Agent**: convex-reviewer
**Rationale**: Validate data persistence patterns

--------------------------------------------------------------------------------
DEPENDENCIES
--------------------------------------------------------------------------------

Depends On: None (can start immediately)

Blocks: None

--------------------------------------------------------------------------------
APPROVAL
--------------------------------------------------------------------------------

Approved By: [pending]
Date: [pending]

================================================================================
