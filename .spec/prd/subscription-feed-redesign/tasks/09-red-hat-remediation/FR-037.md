================================================================================
TASK: FR-037 - Replace hardcoded colors and spacing with semantic tokens
================================================================================

TASK_TYPE: FEATURE
STATUS: Backlog
TDD_PHASE: RED
CURRENT_AC: AC-1
PRIORITY: P1
EFFORT: M
TYPE: DEV
ITERATION: 1

--------------------------------------------------------------------------------
CRITICAL CONSTRAINTS
--------------------------------------------------------------------------------

MUST: Replace ALL rgba() hardcoded colors with semantic theme tokens
MUST: Replace ALL hardcoded numeric spacing values with spacing tokens
MUST: Use useTheme() colors for dynamic values (not hardcoded hex/rgba)
MUST: Verify dark mode renders correctly after changes
NEVER: Use rgba(), hex colors, or numeric spacing in StyleSheet or inline styles
NEVER: Break existing visual appearance in light mode
STRICTLY: Every color from theme.colors, every spacing from theme.spacing

--------------------------------------------------------------------------------
SPECIFICATION
--------------------------------------------------------------------------------

**Objective**: Replace all hardcoded rgba() colors and numeric spacing values in feed components with semantic theme tokens for proper dark mode support.

**Success looks like**:
- Zero rgba() values in feed component files
- Zero hardcoded spacing numbers (use spacing.sm, spacing.md, etc.)
- Dark mode renders correctly (no invisible borders, no contrast issues)
- Light mode appearance unchanged

--------------------------------------------------------------------------------
PREREQUISITES
--------------------------------------------------------------------------------

| Phase | Document | Lines/Section | Purpose |
|-------|----------|---------------|---------|
| BEFORE_START | components/subscriptions/SubscriptionFeedItem.tsx | StyleSheet + inline styles | Find all hardcoded values |
| BEFORE_START | components/subscriptions/SubscriptionFeedFilters.tsx | StyleSheet | Find hardcoded values |
| BEFORE_START | components/subscriptions/SubscriptionSettingsModal.tsx | StyleSheet | Find hardcoded values |

--------------------------------------------------------------------------------
ACCEPTANCE CRITERIA
--------------------------------------------------------------------------------

AC-1: No rgba() in SubscriptionFeedItem
  GIVEN: SubscriptionFeedItem.tsx exists
  WHEN: Searching for hardcoded colors
  THEN: Zero rgba() or hex color strings found
  VERIFY: grep -c "rgba\|#[0-9a-fA-F]" components/subscriptions/SubscriptionFeedItem.tsx (expect 0)

AC-2: No rgba() in SubscriptionFeedFilters
  GIVEN: SubscriptionFeedFilters.tsx exists
  WHEN: Searching for hardcoded colors
  THEN: Zero rgba() or hex color strings found
  VERIFY: grep -c "rgba\|#[0-9a-fA-F]" components/subscriptions/SubscriptionFeedFilters.tsx (expect 0)

AC-3: No hardcoded spacing in feed components
  GIVEN: Feed component StyleSheets
  WHEN: Searching for numeric padding/margin/gap values
  THEN: All use spacing.* tokens
  VERIFY: grep -E "padding.*: [0-9]|margin.*: [0-9]|gap: [0-9]" components/subscriptions/SubscriptionFeedItem.tsx (expect 0)

AC-4: No hardcoded spacing in SubscriptionSettingsModal
  GIVEN: SubscriptionSettingsModal.tsx StyleSheet
  WHEN: Searching for numeric values
  THEN: All use spacing.* tokens
  VERIFY: grep -E "padding.*: [0-9]|margin.*: [0-9]|gap: [0-9]" components/subscriptions/SubscriptionSettingsModal.tsx (expect 0)

AC-5: Dark mode renders correctly
  GIVEN: App running in dark mode
  WHEN: Viewing feed screen
  THEN: All borders visible, text readable, overlays appropriate
  VERIFY: Manual test — toggle dark mode, check all feed components

AC-6: Type check passes
  GIVEN: All changes made
  WHEN: Running type check
  THEN: Zero TypeScript errors
  VERIFY: pnpm tsc --noEmit

--------------------------------------------------------------------------------
GUARDRAILS
--------------------------------------------------------------------------------

WRITE-ALLOWED (explicit file list):
- components/subscriptions/SubscriptionFeedItem.tsx (MODIFY — replace hardcoded values)
- components/subscriptions/SubscriptionFeedFilters.tsx (MODIFY — replace hardcoded values)
- components/subscriptions/SubscriptionSettingsModal.tsx (MODIFY — replace hardcoded values)

WRITE-PROHIBITED:
- convex/** - Backend not affected
- hooks/** - Hook logic not affected

MUST:
- Use useTheme() for all color references
- Use theme.spacing for all spacing values
- Maintain identical light mode appearance
- Test dark mode rendering

MUST NOT:
- Introduce new theme tokens (use existing)
- Change component structure or behavior
- Modify any logic (visual-only changes)

--------------------------------------------------------------------------------
AGENT ASSIGNMENT
--------------------------------------------------------------------------------

**Implementation Agent**: react-vite-ui-implementer
**Rationale**: Theme token replacement across React Native components

**Review Agent**: react-native-ui-reviewer
**Rationale**: Verify theme compliance and dark mode

--------------------------------------------------------------------------------
DEPENDENCIES
--------------------------------------------------------------------------------

Depends On:
- [FR-030] Unify SubscriptionFeedScreen (canonical screen must exist)
- [FR-031] Wire SubscriptionFeedItem (cards must be in live path before fixing tokens)

Blocks: None

--------------------------------------------------------------------------------
APPROVAL
--------------------------------------------------------------------------------

Approved By: [pending]
Date: [pending]

================================================================================
