================================================================================
TASK: US-X-012 - Cached card opens in WebViewSheet
================================================================================

TASK_TYPE: FEATURE
STATUS: To Do
TDD_PHASE: RED
CURRENT_AC: AC-1
PRIORITY: P1
EFFORT: S
TYPE: DEV
ITERATION: 1

--------------------------------------------------------------------------------
BACKGROUND
--------------------------------------------------------------------------------

**Problem:** Tapping a cached card while offline may crash or show an error.

**Why it matters:** Graceful degradation is better than a crash.

**Current state:** Card tap behavior when offline is undefined.

**Desired state:** Tapping a cached card opens WebViewSheet (may fail gracefully if offline).

--------------------------------------------------------------------------------
ACCEPTANCE CRITERIA (TDD Beads)
--------------------------------------------------------------------------------

AC-1: Cached card opens in WebViewSheet
  GIVEN: User taps cached card
  WHEN: Card opens
  THEN: Content opens in WebViewSheet (may fail gracefully)

  TDD_STATE: [ ] RED  [ ] VERIFY_RED  [ ] GREEN  [ ] VERIFY_GREEN  [ ] REFACTOR
  TEST_FILE: components/subscriptions/__tests__/FeedCard.offline.test.tsx
  TEST_FUNCTION: cachedCardOpensWebViewSheet

Quality Criteria:
- [ ] All tests pass
- [ ] Lint passes with zero errors
- [ ] Type check passes
- [ ] RED evidence in Linear comments

--------------------------------------------------------------------------------
READING LIST
--------------------------------------------------------------------------------

1. components/subscriptions/FeedCard.tsx
   - Lines: ALL
   - Focus: Card tap handler, WebViewSheet usage

--------------------------------------------------------------------------------
GUARDRAILS
--------------------------------------------------------------------------------

WRITE-ALLOWED (explicit file list):
- components/subscriptions/FeedCard.tsx (MODIFY) - offline tap handling
- components/subscriptions/__tests__/FeedCard.offline.test.tsx (NEW)

WRITE-PROHIBITED:
- convex/** - backend changes separate

MUST:
- [ ] Attempt to open WebViewSheet on tap
- [ ] Handle failure gracefully (no crash)

MUST NOT:
- [ ] Crash when opening offline content
- [ ] Block card tap interaction

--------------------------------------------------------------------------------
AGENT ASSIGNMENT
--------------------------------------------------------------------------------

**Implementation Agent**: frontend-designer
**Rationale**: React Native WebView offline handling

**Review Agent**: react-native-ui-reviewer
**Rationale**: Validates graceful degradation

--------------------------------------------------------------------------------
EVIDENCE GATES
--------------------------------------------------------------------------------

Gate 1: All Tests Pass
  Command: pnpm vitest run
  Expected: Exit 0

Gate 2: Type Check
  Command: pnpm tsc --noEmit
  Expected: Exit 0

--------------------------------------------------------------------------------
DEPENDENCIES
--------------------------------------------------------------------------------

Depends On:
- US-X-008 - cached items must display

Blocks:
- None

================================================================================
