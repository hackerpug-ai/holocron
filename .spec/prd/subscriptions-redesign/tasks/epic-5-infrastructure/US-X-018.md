================================================================================
TASK: US-X-018 - Filter chip accessibility
================================================================================

TASK_TYPE: FEATURE
STATUS: To Do
TDD_PHASE: RED
CURRENT_AC: AC-1
PRIORITY: P0
EFFORT: S
TYPE: DEV
ITERATION: 1

--------------------------------------------------------------------------------
BACKGROUND
--------------------------------------------------------------------------------

**Problem:** Filter chips lack screen reader labels for category name, count, and selection state.

**Why it matters:** Screen reader users can't filter the feed by category.

**Current state:** Filter chips may lack accessibility props.

**Desired state:** Screen reader announces "Video, 12 items, button, not selected" on focus.

--------------------------------------------------------------------------------
ACCEPTANCE CRITERIA (TDD Beads)
--------------------------------------------------------------------------------

AC-1: Filter chip accessibility
  GIVEN: User focuses filter chip
  WHEN: Screen reader announces
  THEN: "Video, 12 items, button, not selected" announced

  TDD_STATE: [ ] RED  [ ] VERIFY_RED  [ ] GREEN  [ ] VERIFY_GREEN  [ ] REFACTOR
  TEST_FILE: components/subscriptions/__tests__/FeedFilterChips.a11y.test.tsx
  TEST_FUNCTION: announcesFilterChipLabel

Quality Criteria:
- [ ] All tests pass
- [ ] Lint passes with zero errors
- [ ] Type check passes
- [ ] RED evidence in Linear comments

--------------------------------------------------------------------------------
READING LIST
--------------------------------------------------------------------------------

1. components/subscriptions/FeedFilterChips.tsx
   - Lines: ALL
   - Focus: Current chip structure, accessibility props

--------------------------------------------------------------------------------
GUARDRAILS
--------------------------------------------------------------------------------

WRITE-ALLOWED (explicit file list):
- components/subscriptions/FeedFilterChips.tsx (MODIFY) - accessibility props
- components/subscriptions/__tests__/FeedFilterChips.a11y.test.tsx (NEW)

WRITE-PROHIBITED:
- convex/** - no backend changes

MUST:
- [ ] accessibilityRole="button" on each chip
- [ ] accessibilityLabel with category name and count
- [ ] accessibilityState={{ selected: boolean }}

MUST NOT:
- [ ] Use color alone to convey selection state

--------------------------------------------------------------------------------
CODE PATTERN (Reference)
--------------------------------------------------------------------------------

```typescript
<Pressable
  onPress={() => onCategoryChange(cat)}
  accessible={true}
  accessibilityRole="button"
  accessibilityState={{ selected: selectedCategory === cat }}
  accessibilityLabel={`${cat}. ${categories[cat]} items`}
  testID={`filter-chip-${cat}`}
>
  <Text>{cat} ({categories[cat]})</Text>
</Pressable>
```

--------------------------------------------------------------------------------
AGENT ASSIGNMENT
--------------------------------------------------------------------------------

**Implementation Agent**: frontend-designer
**Rationale**: React Native chip accessibility

**Review Agent**: react-native-ui-reviewer
**Rationale**: Validates a11y compliance

--------------------------------------------------------------------------------
EVIDENCE GATES
--------------------------------------------------------------------------------

Gate 1: All Tests Pass
  Command: pnpm vitest run
  Expected: Exit 0

Gate 2: Type Check
  Command: pnpm tsc --noEmit
  Expected: Exit 0

--------------------------------------------------------------------------------
DEPENDENCIES
--------------------------------------------------------------------------------

Depends On:
- Epic 2 (Multimedia Card Stream) - filter chips must exist

Blocks:
- None

================================================================================
