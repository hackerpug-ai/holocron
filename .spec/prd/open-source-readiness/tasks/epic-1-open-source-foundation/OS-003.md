# OS-003: Remove Dead Directories

> Task ID: OS-003
> Type: INFRA
> Priority: P0
> Estimate: 20 minutes
> Assignee: backend-implementer
> Status: ✅ Completed
> Completed: 2026-04-06
> Commit: 233a5d8 (merged via d5edba6)
> Reviewer: orchestrator

## CRITICAL CONSTRAINTS

### MUST
- Remove `holocron-mcp.backup/`, `holocron-general-mcp.old/`, `.tmp/`, `.claude/worktrees/`
- Use `git rm -r` for tracked directories
- Verify no dead directories remain

### NEVER
- Remove active worktrees that are in use
- Delete the `.claude/` directory entirely (only remove `worktrees/` subdirectory)

### STRICTLY
- Check each directory's contents before deletion to ensure no active work is lost

## SPECIFICATION

**Objective:** Remove backup and temporary directories cluttering the repository

**Success looks like:** All specified directories are removed from git tracking and filesystem.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | Dead directories exist | Running `git rm -r holocron-mcp.backup/ holocron-general-mcp.old/ .tmp/ .claude/worktrees/` | Directories staged for removal | `git status \| grep -q "deleted:"` |
| 2 | After removal | Checking for dead directories | None exist in tree | `! ls -d holocron-mcp.backup/ holocron-general-mcp.old/ .tmp/ .claude/worktrees/ 2>/dev/null` |
| 3 | Active worktrees exist | Checking `.claude/worktrees/` | Only removes the directory, not active worktree functionality | `test -d .claude && ! test -d .claude/worktrees` |

## TEST CRITERIA

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | Dead directories are removed when checking filesystem | AC-3 | `! ls -d holocron-mcp.backup/ holocron-general-mcp.old/ .tmp/ 2>/dev/null` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- Use `git rm -r` to remove tracked directories

### WRITE-PROHIBITED
- Removing `.claude/` directory entirely (keep config, only remove worktrees/)

## DESIGN

### References
- PRD Section 2.1, AC-3

### Code Pattern
```bash
# Remove dead directories
git rm -r holocron-mcp.backup/ holocron-general-mcp.old/ .tmp/ .claude/worktrees/

# Commit removal
git commit -m "chore: remove dead backup and temporary directories"
```

## CODING STANDARDS

- **repo-hygiene**: Remove dead code and artifacts

## DEPENDENCIES

No dependencies.

## NOTES

- `.claude/worktrees/` is generated by the worktree system and should not be tracked
- Backup directories suggest incomplete refactoring - ensure the actual code is properly organized
