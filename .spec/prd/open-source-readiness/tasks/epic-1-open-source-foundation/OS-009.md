# OS-009: Rename Sensitive Environment Variable in Documentation

> Task ID: OS-009
> Type: INFRA
> Priority: P0
> Estimate: 15 minutes
> Assignee: backend-implementer
> Status: ✅ Completed
> Completed: 2026-04-06
> Commit: f11b03e
> Reviewer: orchestrator

## CRITICAL CONSTRAINTS

### MUST
- Rename `EXPO_PUBLIC_OPENAI_API_KEY` to non-public prefix in documentation
- Update `.env.example` with safe placeholder naming
- Search codebase for references and update documentation

### NEVER
- Break the app by changing runtime variable names without updating code
- Commit actual API keys during this task

### STRICTLY
- Keep runtime compatibility - only update documentation, not code

## SPECIFICATION

**Objective:** Use non-public naming conventions for sensitive environment variables in documentation

**Success looks like:** Documentation and `.env.example` use generic naming like `YOUR_API_KEY_HERE` instead of provider-specific names.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | EXPO_PUBLIC_OPENAI_API_KEY in .env.example | Updating .env.example | Changed to generic placeholder | `! grep -q "EXPO_PUBLIC_OPENAI_API_KEY=" .env.example \| head -1` |
| 2 | After update | Reading .env.example | Contains safe placeholder pattern | `grep -E "YOUR.*KEY.*HERE|API_KEY.*placeholder" .env.example` |
| 3 | Documentation references old name | Updating README and docs | References use generic naming | `! grep -r "EXPO_PUBLIC_OPENAI_API_KEY" README.md docs/ 2>/dev/null` |

## TEST CRITERIA

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | .env.example uses safe placeholder names when reading file | AC-9 | `grep -q "YOUR_API_KEY_HERE\|placeholder" .env.example` | [ ] TRUE [ ] FALSE |
| 2 | Documentation doesn't expose provider-specific key names when searching docs | AC-9 | `! grep -r "OPENAI_API_KEY\|ANTHROPIC_API_KEY" README.md .spec/ 2>/dev/null \| grep -v ".git"` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- `.env.example` (MODIFY) - Use safe placeholder names
- `README.md` (MODIFY) - Update documentation
- Documentation files (MODIFY) - Update references

### WRITE-PROHIBITED
- Changing runtime environment variable names in actual code
- Breaking the app configuration

## DESIGN

### References
- PRD Section 2.1, AC-9

### Code Pattern
```bash
# .env.example - Before
EXPO_PUBLIC_OPENAI_API_KEY=sk-your-key-here

# .env.example - After
EXPO_PUBLIC_LLM_API_KEY=your-api-key-here

# Documentation should reference:
# "Set your LLM provider API key in EXPO_PUBLIC_LLM_API_KEY"
# Instead of:
# "Set your OpenAI key in EXPO_PUBLIC_OPENAI_API_KEY"
```

## CODING STANDARDS

- **security**: Use provider-agnostic naming for sensitive keys
- **documentation**: Keep examples generic and reusable

## DEPENDENCIES

No dependencies.

## NOTES

- Goal is to avoid advertising which specific providers are used
- Generic naming makes the project more flexible for other users
- Consider adding a comment about which providers are supported
