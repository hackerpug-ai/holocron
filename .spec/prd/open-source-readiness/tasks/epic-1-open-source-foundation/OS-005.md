# OS-005: Remove .DS_Store Files and Update Gitignore

> Task ID: OS-005
> Type: INFRA
> Priority: P0
> Estimate: 20 minutes
> Assignee: backend-implementer
> Status: ✅ Completed
> Completed: 2026-04-06
> Commit: 1636cb3 (merged via b4fb1bb)
> Reviewer: orchestrator

## CRITICAL CONSTRAINTS

### MUST
- Find and remove all `.DS_Store` files from the repository
- Add `.DS_Store` pattern to `.gitignore`
- Add `._*` pattern for macOS resource forks

### NEVER
- Commit new .DS_Store files during cleanup

### STRICTLY
- Search recursively through all subdirectories

## SPECIFICATION

**Objective:** Remove macOS metadata files and prevent future commits

**Success looks like:** Zero `.DS_Store` files in the tree and `.gitignore` contains macOS patterns.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | .DS_Store files exist | Running `find . -name ".DS_Store" -delete` | All files deleted | `find . -name ".DS_Store" 2>/dev/null \| wc -l \| grep -q "0"` |
| 2 | .DS_Store not in gitignore | Adding macOS patterns to .gitignore | Patterns are added | `grep -q ".DS_Store" .gitignore` |
| 3 | After cleanup | Searching for macOS metadata files | None found | `find . -name "._*" -o -name ".DS_Store" 2>/dev/null \| wc -l \| grep -q "0"` |

## TEST CRITERIA

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | No .DS_Store files exist when searching filesystem | AC-5 | `find . -name ".DS_Store" 2>/dev/null \| wc -l \| grep -q "0"` | [ ] TRUE [ ] FALSE |
| 2 | .gitignore contains macOS patterns when reading file | AC-5 | `grep -E "(\.DS_Store|\.['_])*\*" .gitignore` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- `.gitignore` (MODIFY) - Add macOS metadata patterns

### WRITE-PROHIBITED
- Adding new .DS_Store files

## DESIGN

### References
- PRD Section 2.1, AC-5

### Code Pattern
```bash
# Remove all .DS_Store files
find . -name ".DS_Store" -type f -delete

# Add to gitignore
cat >> .gitignore << 'EOF'
# macOS
.DS_Store
._*
EOF

# Stage and commit
git add .gitignore
git find . -name ".DS_Store" -exec git rm {} \;
git commit -m "chore: remove .DS_Store files and update gitignore"
```

## CODING STANDARDS

- **repo-hygiene**: Remove OS-specific metadata files

## DEPENDENCIES

No dependencies.

## NOTES

- .DS_Store files are automatically generated by macOS Finder
- Consider adding a pre-commit hook to prevent future .DS_Store commits
