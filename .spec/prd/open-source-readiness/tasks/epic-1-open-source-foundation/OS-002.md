# OS-002: Remove Binary Files and Update Gitignore

> Task ID: OS-002
> Type: INFRA
> Priority: P0
> Estimate: 30 minutes
> Assignee: backend-implementer
> Status: ✅ Completed
> Completed: 2026-04-06
> Commit: 07ab9c0
> Reviewer: orchestrator

## CRITICAL CONSTRAINTS

### MUST
- Remove `build-1774590211242.ipa` from the repository
- Add `*.ipa` pattern to `.gitignore`
- Verify no other binary files remain in the tree

### NEVER
- Commit new .ipa files during this task
- Add binary files to LFS unless absolutely necessary

### STRICTLY
- Use `git rm` for tracked files, not just filesystem deletion
- Commit gitignore changes before verifying

## SPECIFICATION

**Objective:** Remove iOS build artifacts and prevent future commits

**Success looks like:** `find . -name "*.ipa"` returns zero results and `.gitignore` contains the `*.ipa` pattern.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | .ipa file exists in repo | Running `git rm build-1774590211242.ipa` | File is staged for removal | `git status \| grep -q "deleted: build-1774590211242.ipa"` |
| 2 | .ipa pattern not in gitignore | Adding `*.ipa` to .gitignore | Pattern is added to file | `grep -q "*.ipa" .gitignore` |
| 3 | After removal | Searching for .ipa files | Zero results found | `find . -name "*.ipa" 2>/dev/null \| wc -l \| grep -q "0"` |
| 4 | Other binary files checked | Running `file **/*` | No other executable binaries in repo | `find . -type f -executable 2>/dev/null \| grep -v node_modules \| wc -l \| grep -q "0"` |

## TEST CRITERIA

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | No .ipa files exist in repository when searching filesystem | AC-2 | `find . -name "*.ipa" 2>/dev/null \| wc -l \| grep -q "0"` | [ ] TRUE [ ] FALSE |
| 2 | .gitignore contains *.ipa pattern when reading file | AC-2 | `grep -q "*.ipa" .gitignore` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- `.gitignore` (MODIFY) - Add binary file patterns
- Use `git rm` for removing tracked files

### WRITE-PROHIBITED
- Adding new binary files to the repository
- Modifying build configuration to output binaries to repo root

## DESIGN

### References
- PRD Section 2.1, AC-2

### Code Pattern
```bash
# Remove tracked file
git rm build-1774590211242.ipa

# Add pattern to gitignore
echo "*.ipa" >> .gitignore

# Commit changes
git add .gitignore
git commit -m "chore: remove .ipa file and add to gitignore"
```

## CODING STANDARDS

- **git-workflow**: Use git rm for tracked files
- **repo-hygiene**: Keep binary artifacts out of version control

## DEPENDENCIES

No dependencies.

## NOTES

- .ipa files are iOS build outputs that should be generated in CI/CD, not committed
- Consider adding other build artifacts to gitignore if found (.apk, .app, etc.)
