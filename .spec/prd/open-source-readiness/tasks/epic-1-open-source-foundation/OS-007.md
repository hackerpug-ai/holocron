# OS-007: Remove Dead Supabase Infrastructure

> Task ID: OS-007
> Type: INFRA
> Priority: P0
> Estimate: 20 minutes
> Assignee: backend-implementer
> Status: ✅ Completed
> Completed: 2026-04-06
> Commit: ed4ba4c
> Reviewer: orchestrator

## CRITICAL CONSTRAINTS

### MUST
- Remove `supabase/` directory
- Remove `.github/workflows/deploy-supabase.yml`
- Use `git rm -r` for tracked directories

### NEVER
- Remove active GitHub workflows that are still in use
- Delete `.github/` directory entirely

### STRICTLY
- Verify no active Supabase deployment exists before removal

## SPECIFICATION

**Objective:** Remove unused Supabase infrastructure from the repository

**Success looks like:** No `supabase/` directory or Supabase deployment workflow exists in the repository.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | supabase/ directory exists | Running `git rm -r supabase/` | Directory staged for removal | `git status \| grep -q "deleted:.*supabase/"` |
| 2 | Supabase workflow exists | Running `git rm .github/workflows/deploy-supabase.yml` | Workflow staged for removal | `git status \| grep -q "deleted:.*deploy-supabase.yml"` |
| 3 | After removal | Checking for Supabase artifacts | None exist in tree | `! test -d supabase/ && ! test -f .github/workflows/deploy-supabase.yml` |

## TEST CRITERIA

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | supabase/ directory is removed when checking filesystem | AC-7 | `! test -d supabase/` | [ ] TRUE [ ] FALSE |
| 2 | Supabase workflow is removed when checking workflows directory | AC-7 | `! test -f .github/workflows/deploy-supabase.yml` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- Use `git rm -r` to remove supabase/ directory
- Use `git rm` to remove Supabase workflow

### WRITE-PROHIBITED
- Removing other active GitHub workflows
- Deleting `.github/` directory entirely

## DESIGN

### References
- PRD Section 2.1, AC-7

### Code Pattern
```bash
# Remove Supabase directory
git rm -r supabase/

# Remove Supabase workflow
git rm .github/workflows/deploy-supabase.yml

# Commit removal
git commit -m "chore: remove unused Supabase infrastructure"
```

## CODING STANDARDS

- **repo-hygiene**: Remove unused infrastructure code

## DEPENDENCIES

No dependencies.

## NOTES

- Supabase was replaced with Convex as the backend
- Ensure no code references Supabase before removal
- Check environment variables and remove any Supabase-related configs
