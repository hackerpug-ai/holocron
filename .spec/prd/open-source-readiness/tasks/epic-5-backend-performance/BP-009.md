# BP-009: Replace `ruleValue` `v.any()` with Proper Union

> Task ID: BP-009
> Type: REFACTOR
> Priority: P1
> Estimate: 15 minutes
> Assignee: convex-implementer

## CRITICAL CONSTRAINTS

### MUST
- Replace `ruleValue: v.any()` with `v.union(v.string(), v.number(), v.array(v.string()))`
- Target: `convex/schema.ts` line 301
- Match actual value types used in code

### NEVER
- Leave unvalidated `v.any()` for typed fields

### STRICTLY
- Schema must match actual usage patterns

## SPECIFICATION

**Objective:** Replace `v.any()` for `ruleValue` with a union that matches actual usage.

**Success looks like:** `ruleValue` field validates that values are string, number, or string array.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | `v.any()` for ruleValue | Replacing with union | Uses `v.union(v.string(), v.number(), v.array(v.string()))` | `grep "ruleValue:" convex/schema.ts \| grep -q "v.union"` |
| 2 | After replacement | Checking usage | Matches actual types | No validation errors |

## TEST CRITERIA

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | ruleValue uses v.union when checking | AC-55 | `grep "ruleValue:" convex/schema.ts \| grep -q "v.union"` | [ ] TRUE [ ] FALSE |
| 2 | Includes string, number, array when checking | AC-55 | `grep -A1 "ruleValue" convex/schema.ts \| grep -q "string.*number.*array"` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- `convex/schema.ts` (MODIFY) - Replace `v.any()` with union

### WRITE-PROHIBITED
- Leaving completely unvalidated `v.any()`

## DESIGN

### References
- PRD Section 2.9, AC-55
- Target: `convex/schema.ts` line 301

### Code Pattern
```typescript
// BEFORE — No validation
ruleValue: v.any(),

// AFTER — Validates against actual types used
ruleValue: v.union(
  v.string(),
  v.number(),
  v.array(v.string())
),

// If you also use boolean:
ruleValue: v.union(
  v.string(),
  v.number(),
  v.boolean(),
  v.array(v.string())
),
```

## CODING STANDARDS

- **schema**: Use unions to capture all valid types
- **validation**: Match schema to actual usage

## DEPENDENCIES

No dependencies.

## NOTES

- Line numbers may have shifted — search for `ruleValue`
- The PRD specifies string, number, or string array
- Check actual usage to confirm these types
- Search for where `ruleValue` is used in mutations/actions
- Add boolean if that's also used
- This prevents invalid data from being stored
- Union validation is much better than `v.any()`
- Consider adding comments explaining each type's purpose
