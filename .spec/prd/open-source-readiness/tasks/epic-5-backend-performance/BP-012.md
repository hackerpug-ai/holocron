# BP-012: Final Performance Verification

> Task ID: BP-012
> Type: VERIFY
> Priority: P1
> Estimate: 30 minutes
> Assignee: code-reviewer

## CRITICAL CONSTRAINTS

### MUST
- Verify all performance improvements are complete
- Check Convex dashboard for full table scan warnings
- Run `tsc --noEmit` to ensure no type errors
- Document any remaining issues

### NEVER
- Sign off if full table scans remain

### STRICTLY
- All acceptance criteria from Epic 5 must be verified

## SPECIFICATION

**Objective:** Final verification that all backend performance improvements are complete.

**Success looks like:** Zero full table scans, all queries use indexes, all types are correct.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | All Epic 5 tasks complete | Running verification | All AC verified | Checklist complete |
| 2 | Convex dashboard | Checking for warnings | Zero full table scan warnings | Dashboard shows no warnings |
| 3 | Codebase | Running tsc | No type errors | `pnpm tsc --noEmit` exits 0 |

## TEST CRITERIA

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | No unindexed collect when grepping | AC-46, AC-47 | `! grep -E 'query\(".*"\)\.collect\(\)' convex/queries/ convex/research/` | [ ] TRUE [ ] FALSE |
| 2 | Uses vectorSearch when checking | AC-47 | `grep -c "vectorSearch" convex/queries/ convex/research/ \| grep -q "[1-9]"` | [ ] TRUE [ ] FALSE |
| 3 | No as any on scheduler when grepping | AC-48 | `! grep -E "scheduler.*as any\|cron.*as any" convex/` | [ ] TRUE [ ] FALSE |
| 4 | confidenceLevel uses v.union when checking | AC-50 | `grep "confidenceLevel:" convex/schema.ts \| grep -q "v.union"` | [ ] TRUE [ ] FALSE |
| 5 | No collect().length for counts when grepping | AC-51 | `! grep -E '\.collect\(\)\.length' convex/` | [ ] TRUE [ ] FALSE |
| 6 | No ctx.db.get after ctx.db.patch when grepping | AC-52 | `! grep -B1 "ctx.db.get" convex/ \| grep -A1 "ctx.db.patch"` | [ ] TRUE [ ] FALSE |
| 7 | Uses withIndex for iterations when checking | AC-53 | `grep -q 'withIndex("by_session")' convex/research/queries.ts` | [ ] TRUE [ ] FALSE |
| 8 | v.any count reduced when counting | AC-13, AC-54 | `grep -c "v.any()" convex/schema.ts \| awk '{exit $1 < 10}'` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- Verification only — no code changes expected
- If issues found, create follow-up tasks

### WRITE-PROHIBITED
- Signing off on incomplete work

## DESIGN

### References
- PRD Section 2.9 (AC-46 through AC-57)
- All Epic 5 tasks

### Verification Checklist
1. **Query Performance**
   - [ ] No unindexed `.collect()` on large tables
   - [ ] Text search uses `withSearchIndex`
   - [ ] Vector search uses `ctx.vectorSearch`
   - [ ] Filtered queries use indexes

2. **Type Safety**
   - [ ] No `as any` on scheduler/cron refs
   - [ ] `confidenceLevel` uses `v.union(v.literal(...))`
   - [ ] `ruleValue` uses proper union
   - [ ] `subscriptionFilters.sourceType` matches `subscriptionSources.sourceType`

3. **Efficiency**
   - [ ] No `collect().length` for counts
   - [ ] No read-after-write `ctx.db.get()`
   - [ ] Iterations use `by_session` index

4. **Schema Quality**
   - [ ] `v.any()` reduced to <10
   - [ ] `toolArgs` uses `v.record()` at minimum

## CODING STANDARDS

- **verification**: Verify all AC before signing off
- **performance**: Zero tolerance for full table scans

## DEPENDENCIES

**Blocked by**: All Epic 5 tasks (BP-001 through BP-011)

## NOTES

- This is the gatekeeper task for Epic 5
- Run the full verification checklist
- Check Convex dashboard for query performance warnings
- Document any remaining issues
- Don't sign off if anything fails
- Consider running a load test to verify performance improvements
- Update INDEX.md to mark Epic 5 complete when done
