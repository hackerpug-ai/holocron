# BP-011: Verify Function References Exist

> Task ID: BP-011
> Type: VERIFY
> Priority: P1
> Estimate: 15 minutes
> Assignee: convex-implementer

## CRITICAL CONSTRAINTS

### MUST
- Verify `api.research.actions.startSmartResearch` exists in generated API
- Target: `convex/research/mutations.ts:431`
- If renamed or removed, update to correct function path

### NEVER
- Leave broken function references

### STRICTLY
- All function references must resolve

## SPECIFICATION

**Objective:** Verify that referenced functions actually exist in the Convex API.

**Success looks like:** All function references point to existing functions.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | Function reference in code | Checking API | Function exists | `grep -q "startSmartResearch" convex/_generated/api.ts` |
| 2 | If function doesn't exist | Finding correct path | Reference updated | Code uses correct function name |

## TEST CRITERIA

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | Function exists in generated API when checking | AC-57 | `grep -q "startSmartResearch" convex/_generated/api.ts` | [ ] TRUE [ ] FALSE |
| 2 | Reference is correct when comparing | AC-57 | `grep "api.research.actions" convex/research/mutations.ts \| grep -q "startSmartResearch"` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- `convex/research/mutations.ts` (MODIFY) - Update reference if needed

### WRITE-PROHIBITED
- Leaving broken function references

## DESIGN

### References
- PRD Section 2.9, AC-57
- Target: `convex/research/mutations.ts:431`

### Verification Steps
1. Check what the function is called:
   ```bash
   grep -r "export.*startSmartResearch\|export const.*SmartResearch" convex/research/
   ```

2. Check if it's in the generated API:
   ```bash
   grep "startSmartResearch" convex/_generated/api.ts
   ```

3. If the function was renamed, update the reference:
   ```typescript
   // If renamed to `startResearch`:
   api.research.actions.startResearch
   ```

## CODING STANDARDS

- **verification**: Ensure all references point to existing functions
- **maintenance**: Keep references in sync with refactoring

## DEPENDENCIES

No dependencies.

## NOTES

- Line numbers may have shifted — search for the reference
- The function might have been renamed or removed
- Check `convex/research/actions.ts` for the actual function
- Check `convex/_generated/api.ts` to see what's exposed
- Common issues:
  - Function renamed but reference not updated
  - Function moved to different module
  - Function not exported via index
- If function doesn't exist, find what replaced it
- This prevents runtime errors from missing functions
- Consider using TypeScript to catch these errors at compile time
