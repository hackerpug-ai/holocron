# CQ-008: Run Full Test Suite and Verify

> Task ID: CQ-008
> Type: VERIFY
> Priority: P1
> Estimate: 15 minutes
> Assignee: test-runner

## CRITICAL CONSTRAINTS

### MUST
- Run `pnpm vitest run` — all tests must pass
- Verify zero `toBeDefined()` tests remain
- Verify all new behavioral tests pass
- Document any failures

### NEVER
- Sign off if tests are failing
- Skip this verification step

### STRICTLY
- All tests must pass before Epic 3 is complete

## SPECIFICATION

**Objective:** Final verification that all test quality improvements are complete and passing.

**Success looks like:** Full test suite passes with zero `toBeDefined()` tests and all new behavioral tests working.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | All test tasks complete | Running vitest | All tests pass | `pnpm vitest run` exits 0 |
| 2 | Test suite | Checking for toDefined() | Zero remain | `! grep -r "\.toBeDefined()" tests/` |
| 3 | New tests | Verifying they run | All behavioral tests pass | Output shows chat, conversation, confidence, termination tests passing |

## TEST CRITERIA

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | vitest run exits 0 when running | AC-21 | `pnpm vitest run; echo $?` | [ ] TRUE [ ] FALSE |
| 2 | Zero toDefined() in tests when grepping | AC-16 | `! grep -r "\.toBeDefined()" tests/` | [ ] TRUE [ ] FALSE |
| 3 | Chat tests pass when running | AC-17, AC-21 | `pnpm vitest run tests/convex/US-052-chat.test.ts 2>&1 \| tail -1 \| grep -q "passed"` | [ ] TRUE [ ] FALSE |
| 4 | Conversation tests pass when running | AC-18, AC-21 | `pnpm vitest run tests/integration/conversations.test.ts 2>&1 \| tail -1 \| grep -q "passed"` | [ ] TRUE [ ] FALSE |
| 5 | Confidence tests pass when running | AC-19, AC-21 | `pnpm vitest run tests/research/confidence.test.ts 2>&1 \| tail -1 \| grep -q "passed"` | [ ] TRUE [ ] FALSE |
| 6 | Termination tests pass when running | AC-20, AC-21 | `pnpm vitest run tests/research/termination.test.ts 2>&1 \| tail -1 \| grep -q "passed"` | [ ] TRUE [ ] FALSE |
| 7 | No string-grep tests when searching | AC-22 | `! grep -r "readFileSync" tests/` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- Test files (MODIFY) - Fix any failures found
- If issues found, create follow-up tasks

### WRITE-PROHIBITED
- Signing off on failing tests

## DESIGN

### References
- PRD Section 2.3 (AC-16 through AC-22)

### Verification Checklist
1. **Test Quality**
   - [ ] Zero `toBeDefined()` tests
   - [ ] Zero string-grep tests
   - [ ] All behavioral tests pass
   - [ ] Chat flow tests pass
   - [ ] Conversation CRUD tests pass
   - [ ] Confidence edge case tests pass
   - [ ] Termination logic tests pass

2. **Test Coverage**
   - [ ] New test files created
   - [ ] Tests verify behavior, not existence
   - [ ] Edge cases are covered
   - [ ] Boundary conditions tested

3. **Test Execution**
   - [ ] `pnpm vitest run` exits 0
   - [ ] No skipped tests (unless intentional)
   - [ ] No test timeouts
   - [ ] Reasonable test execution time

## CODING STANDARDS

- **verification**: Verify all AC before signing off
- **quality**: Zero tolerance for failing tests

## DEPENDENCIES

**Blocked by**: CQ-001 through CQ-007 (all test quality tasks must be complete)

## NOTES

- This is the gatekeeper task for Epic 3's test work
- Run the full suite, not just individual test files
- Check the output for any skipped or todo tests
- If tests are failing, don't sign off — create follow-up tasks
- Document the test counts (before/after) if possible
- Consider test execution time — tests should be fast
- Update INDEX.md to mark Epic 3 test work complete when done
