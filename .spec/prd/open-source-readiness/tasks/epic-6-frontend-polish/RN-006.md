# RN-006: Convert `useQueryResults` to kebab-case

> Task ID: RN-006
> Type: REFACTOR
> Priority: P1
> Estimate: 20 minutes
> Assignee: react-native-implementer

## CRITICAL CONSTRAINTS

### MUST
- Rename `useQueryResults` to `use-query-results`
- Update all import statements
- Update hook documentation

### NEVER
- Leave camelCase hook names

### STRICTLY
- All custom hooks use kebab-case naming

## SPECIFICATION

**Objective:** Rename `useQueryResults` hook to `use-query-results` for consistency.

**Success looks like:** Hook file and all imports use kebab-case naming.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | `useQueryResults` camelCase | Renaming | File is `use-query-results.ts` | `ls hooks/use-query-results.ts` |
| 2 | Existing imports | Updating | All imports use kebab-case | `grep -r "useQueryResults" app/ components/` returns empty |
| 3 | After rename | Running build | No import errors | `pnpm tsc --noEmit` passes |

## TEST CRITERIA

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | No camelCase hook file when checking | AC-63 | `! ls hooks/useQueryResults.ts 2>/dev/null` | [ ] TRUE [ ] FALSE |
| 2 | No camelCase imports when checking | AC-63 | `! grep -r "useQueryResults" app/ components/ hooks/` | [ ] TRUE [ ] FALSE |
| 3 | Kebab-case file exists when checking | AC-63 | `ls hooks/use-query-results.ts` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- `hooks/useQueryResults.ts` (RENAME) → `hooks/use-query-results.ts`
- All files importing `useQueryResults` (MODIFY) - Update imports

### WRITE-PROHIBITED
- Leaving camelCase hook names

## DESIGN

### References
- PRD Section 2.10, AC-63
- Project naming convention: kebab-case for hooks

### Rename Pattern
```bash
# 1. Rename the file
git mv hooks/useQueryResults.ts hooks/use-query-results.ts

# 2. Update exports in the file
export function useQueryResults() { ... }
// becomes:
export function useQueryResults() { ... } // Keep function name camelCase (JS convention)
// but file is kebab-case

# 3. Update all imports
// Before:
import { useQueryResults } from '@/hooks/useQueryResults'

// After:
import { useQueryResults } from '@/hooks/use-query-results'
```

### Finding Imports
```bash
# Find all files that import this hook
grep -r "useQueryResults" app/ components/ screens/
```

## CODING STANDARDS

- **naming**: Hook files use kebab-case
- **imports**: Import paths match file names
- **functions**: Export names remain camelCase (JS convention)

## DEPENDENCIES

No dependencies.

## NOTES

- The function name stays `useQueryResults` (camelCase) — that's JS convention
- Only the file name and import paths change to kebab-case
- This is a project-specific convention for file naming
- Make sure to update both the file and all import statements
- Run typecheck to verify all imports are updated

