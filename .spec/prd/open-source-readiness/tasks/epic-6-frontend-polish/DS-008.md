# DS-008: Verify Storybook Play Functions for Interactive Components

> Task ID: DS-008
> Type: VERIFY
> Priority: P1
> Estimate: 30 minutes
> Assignee: react-native-reviewer

## CRITICAL CONSTRAINTS

### MUST
- Verify all interactive components have play functions in stories
- Check buttons, inputs, pressable components
- Ensure play functions test actual interactions

### NEVER
- Have interactive components without play tests

### STRICTLY
- All interactive components have play functions

## SPECIFICATION

**Objective:** Verify that interactive Storybook stories include play functions.

**Success looks like:** Every interactive component story has a play function testing user interactions.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | Interactive component stories | Reviewing | Have play functions | Play functions present |
| 2 | Play functions | Running | Test interactions | Interactions work in Storybook |
| 3 | After verification | Documenting | All interactive stories compliant | Checklist complete |

## TEST CRITERIA

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | Button stories have play functions when checking | AC-75 | `grep -A20 "Button" components/ui/*.stories.tsx | grep -q "play:"` | [ ] TRUE [ ] FALSE |
| 2 | Input stories have play functions when checking | AC-75 | `grep -A20 "Input" components/ui/*.stories.tsx | grep -q "play:"` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- Verification only — no code changes expected
- If issues found, create follow-up tasks

### WRITE-PROHIBITED
- Signing off on missing play functions

## DESIGN

### References
- PRD Section 2.11, AC-75
- Storybook interactions documentation

### Verification Checklist

1. **Button Components**
   - [ ] Has play function testing click
   - [ ] Tests disabled state (if applicable)

2. **Input Components**
   - [ ] Has play function testing input
   - [ ] Tests validation (if applicable)

3. **Pressable Components**
   - [ ] Has play function testing press
   - [ ] Tests pressed state

### Correct Pattern
```typescript
import { within, userEvent, expect } from '@storybook/test'

export const ClickButton: Story = {
  play: async ({ canvasElement }) => {
    const canvas = within(canvasElement)
    const button = canvas.getByRole('button')
    await userEvent.click(button)
    // Assert result
    await expect(button).toHaveFocus()
  },
}

export const TypeInput: Story = {
  play: async ({ canvasElement }) => {
    const canvas = within(canvasElement)
    const input = canvas.getByRole('textbox')
    await userEvent.type(input, 'Hello world')
    await expect(input).toHaveValue('Hello world')
  },
}

export const ToggleSwitch: Story = {
  play: async ({ canvasElement }) => {
    const canvas = within(canvasElement)
    const toggle = canvas.getByRole('switch')
    await userEvent.click(toggle)
    await expect(toggle).toBeChecked()
  },
}
```

### Finding Issues
```bash
# Find all story files
find components/ui -name "*.stories.tsx"

# Check for play functions
grep -L "play:" components/ui/*.stories.tsx

# Find interactive components
grep -l "Button\|Input\|Pressable" components/ui/*.stories.tsx
```

## CODING STANDARDS

- **testing**: Interactive components must have play functions
- **coverage**: Test all interaction paths

## DEPENDENCIES

No dependencies.

## NOTES

- Play functions make stories testable
- They also serve as documentation
- Test the happy path and error cases
- Use `@storybook/test` for testing utilities

