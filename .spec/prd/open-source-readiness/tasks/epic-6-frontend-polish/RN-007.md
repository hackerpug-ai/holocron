# RN-007: Convert `useSearchResults` to kebab-case

> Task ID: RN-007
> Type: REFACTOR
> Priority: P1
> Estimate: 20 minutes
> Assignee: react-native-implementer

## CRITICAL CONSTRAINTS

### MUST
- Rename `useSearchResults` to `use-search-results`
- Update all import statements
- Update hook documentation

### NEVER
- Leave camelCase hook names

### STRICTLY
- All custom hooks use kebab-case naming

## SPECIFICATION

**Objective:** Rename `useSearchResults` hook to `use-search-results` for consistency.

**Success looks like:** Hook file and all imports use kebab-case naming.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | `useSearchResults` camelCase | Renaming | File is `use-search-results.ts` | `ls hooks/use-search-results.ts` |
| 2 | Existing imports | Updating | All imports use kebab-case | `grep -r "useSearchResults" app/ components/` returns empty |
| 3 | After rename | Running build | No import errors | `pnpm tsc --noEmit` passes |

## TEST CRITERIA

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | No camelCase hook file when checking | AC-63 | `! ls hooks/useSearchResults.ts 2>/dev/null` | [ ] TRUE [ ] FALSE |
| 2 | No camelCase imports when checking | AC-63 | `! grep -r "useSearchResults" app/ components/ hooks/` | [ ] TRUE [ ] FALSE |
| 3 | Kebab-case file exists when checking | AC-63 | `ls hooks/use-search-results.ts` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- `hooks/useSearchResults.ts` (RENAME) → `hooks/use-search-results.ts`
- All files importing `useSearchResults` (MODIFY) - Update imports

### WRITE-PROHIBITED
- Leaving camelCase hook names

## DESIGN

### References
- PRD Section 2.10, AC-63
- Project naming convention: kebab-case for hooks

### Rename Pattern
```bash
# 1. Rename the file
git mv hooks/useSearchResults.ts hooks/use-search-results.ts

# 2. Update all imports
// Before:
import { useSearchResults } from '@/hooks/useSearchResults'

// After:
import { useSearchResults } from '@/hooks/use-search-results'
```

### Finding Imports
```bash
# Find all files that import this hook
grep -r "useSearchResults" app/ components/ screens/
```

## CODING STANDARDS

- **naming**: Hook files use kebab-case
- **imports**: Import paths match file names
- **functions**: Export names remain camelCase (JS convention)

## DEPENDENCIES

No dependencies.

## NOTES

- The function name stays `useSearchResults` (camelCase)
- Only the file name and import paths change to kebab-case
- This is similar to RN-006 (useQueryResults)
- Make sure to update both the file and all import statements
- Run typecheck to verify all imports are updated

