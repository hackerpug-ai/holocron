# RN-009: Remove or Fix Dead `useHydrated` Hook

> Task ID: RN-009
> Type: REFACTOR
> Priority: P1
> Estimate: 15 minutes
> Assignee: react-native-implementer

## CRITICAL CONSTRAINTS

### MUST
- Verify if `useHydrated` is used anywhere
- If unused: delete the file
- If used: verify it actually works for React Native

### NEVER
- Leave dead code in the codebase
- Leave SSR-only patterns in React Native (no SSR)

### STRICTLY
- No unused hooks

## SPECIFICATION

**Objective:** Remove or fix the `useHydrated` hook if it's dead code.

**Success looks like:** Either the hook is removed or verified to be working correctly.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | `useHydrated` exists | Checking usage | Either used or deleted | Decision documented |
| 2 | If unused | Deleting | File removed | `! ls hooks/useHydrated.ts` |
| 3 | If used | Verifying | Works correctly for React Native | Hook purpose is valid |

## TEST CRITERIA

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | No useHydrated file when checking | AC-58 | `! ls hooks/useHydrated.ts 2>/dev/null` | [ ] TRUE [ ] FALSE |
| 2 | No imports of useHydrated when checking | AC-58 | `! grep -r "useHydrated" app/ components/ screens/` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- `hooks/useHydrated.ts` (DELETE) - If unused
- Any files importing `useHydrated` (MODIFY) - Remove imports

### WRITE-PROHIBITED
- Leaving dead hooks

## DESIGN

### References
- PRD Section 2.10, AC-58
- Dead code elimination

### Investigation Steps
```bash
# 1. Check if the file exists
ls hooks/useHydrated.ts

# 2. Find all usages
grep -r "useHydrated" app/ components/ screens/ hooks/

# 3. If no results, delete the file
git rm hooks/useHydrated.ts
```

### Context
`useHydrated` is typically used in Next.js/React SSR to detect when the app has hydrated on the client. In React Native:
- There's no SSR — hydration is not a concept
- This hook is likely copied from web code and unused
- If it IS used, verify the use case is valid

## CODING STANDARDS

- **cleanup**: Remove dead code
- **platform**: Don't use web patterns in React Native

## DEPENDENCIES

No dependencies.

## NOTES

- React Native doesn't have SSR, so hydration hooks are usually unnecessary
- This is likely copied from a web project
- If it IS used, understand why — there might be a valid React Native use case
- Common valid use case: checking if layout is complete (use onLayout instead)
- Check grep results before deleting

