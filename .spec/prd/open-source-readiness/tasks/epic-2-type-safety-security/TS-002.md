# TS-002: Eliminate `any` Types in `convex/chat/index.ts`

> Task ID: TS-002
> Type: REFACTOR
> Priority: P1
> Estimate: 30 minutes
> Assignee: convex-implementer

## CRITICAL CONSTRAINTS

### MUST
- Replace `any` types in handler contexts (`MutationCtx`, `QueryCtx`, `ActionCtx`)
- Type all handler parameters with explicit interfaces or Convex types
- Ensure return values are typed
- Run `tsc --noEmit` after changes

### NEVER
- Use `@ts-ignore` to suppress type errors
- Change runtime behavior

### STRICTLY
- All handler signatures must be fully typed

## SPECIFICATION

**Objective:** Eliminate all `any` types in `convex/chat/index.ts` handler functions.

**Success looks like:** All handler functions have properly typed context, parameters, and return values.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | Handler contexts use `any` | Refactoring types | Replaced with proper Convex types | `! grep -E "ctx: any|context: any" convex/chat/index.ts` |
| 2 | Parameters untyped | Adding type annotations | All parameters have explicit types | `grep -q "MutationCtx\|QueryCtx\|ActionCtx" convex/chat/index.ts` |
| 3 | After refactoring | Running typecheck | File passes `tsc --noEmit` | `pnpm tsc --noEmit convex/chat/index.ts` exits 0 |

## TEST CRITERIA

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | Zero context `any` types in index.ts when grepping | AC-11 | `! grep -E "ctx: any|context: any" convex/chat/index.ts` | [ ] TRUE [ ] FALSE |
| 2 | All handlers use proper Convex context types when checking imports | AC-11 | `grep -q "from.*convex/server" convex/chat/index.ts` | [ ] TRUE [ ] FALSE |
| 3 | tsc --noEmit passes when running typecheck | AC-15 | `pnpm tsc --noEmit 2>&1 \| grep -E "error TS" \| wc -l \| grep -q "0"` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- `convex/chat/index.ts` (MODIFY) - Replace `any` with proper types

### WRITE-PROHIBITED
- Using `@ts-ignore`
- Changing handler logic

## DESIGN

### References
- PRD Section 2.2, AC-11

### Code Pattern
```typescript
// Before
import { mutation, query } from "./_generated/server";

export const sendMessage = mutation({
  handler: async (ctx: any, args: any) => {
    // ...
  }
});

// After
import { mutation, query, MutationCtx } from "./_generated/server";
import type { Id } from "./_generated/dataModel";

export const sendMessage = mutation({
  handler: async (ctx: MutationCtx, args: { conversationId: Id<"conversations">; content: string }) => {
    // ...
  }
});
```

## CODING STANDARDS

- **typescript**: Explicit types for all handler signatures
- **convex**: Use proper context types from `./_generated/server`

## DEPENDENCIES

No dependencies.

## NOTES

- Focus on mutation/query handlers first
- Check if there are action handlers that need `ActionCtx`
- Parameter types can use `Id<"tableName">` for document references
