# TS-006: Run `tsc --noEmit` and Fix All Type Errors

> Task ID: TS-006
> Type: VERIFY
> Priority: P1
> Estimate: 30 minutes
> Assignee: convex-implementer

## CRITICAL CONSTRAINTS

### MUST
- Run `pnpm tsc --noEmit` and capture all errors
- Fix every type error — zero errors remaining
- Do NOT use `@ts-ignore` to bypass errors

### NEVER
- Suppress type errors with comments
- Leave errors for "later"

### STRICTLY
- `tsc --noEmit` must exit with code 0

## SPECIFICATION

**Objective:** Verify that all type safety changes result in a fully type-checked codebase.

**Success looks like:** Running `pnpm tsc --noEmit` produces zero errors.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | Type errors in codebase | Running tsc --noEmit | All errors fixed | `pnpm tsc --noEmit` exits 0 |
| 2 | After fixing | Checking error count | Zero TS errors | `pnpm tsc --noEmit 2>&1 \| grep -c "error TS" \| grep -q "0"` |
| 3 | Codebase | Checking for @ts-ignore | No suppression comments | `! grep -r "@ts-ignore" --include="*.ts" --include="*.tsx" .` |

## TEST CRITERIA

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | tsc --noEmit exits with code 0 when running typecheck | AC-15 | `pnpm tsc --noEmit; echo $?` | [ ] TRUE [ ] FALSE |
| 2 | Zero TypeScript errors in output when grepping | AC-15 | `! pnpm tsc --noEmit 2>&1 \| grep -q "error TS"` | [ ] TRUE [ ] FALSE |
| 3 | No @ts-ignore in codebase when searching | AC-15 | `! grep -r "@ts-ignore" --include="*.ts" --include="*.tsx" convex/ hooks/` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- Any TypeScript files (MODIFY) - Fix type errors as needed
- Type definition files (MODIFY) - Add missing types

### WRITE-PROHIBITED
- Using `@ts-ignore`, `@ts-expect-error` to bypass errors
- Changing `tsconfig.json` to reduce strictness

## DESIGN

### References
- PRD Section 2.2, AC-15

### Workflow
1. Run `pnpm tsc --noEmit` and capture output
2. Categorize errors by file and type
3. Fix errors systematically:
   - Missing imports → add imports
   - Implicit any → add explicit types
   - Type mismatches → fix the types or the values
4. Re-run until zero errors

## CODING STANDARDS

- **typescript**: All code must pass strict type checking
- **quality**: No type errors, no suppressions

## DEPENDENCIES

**Blocked by**: TS-001, TS-002, TS-003, TS-004, TS-005 (must complete refactoring first)

## NOTES

- This is the final gate for Epic 2's type safety work
- Common error patterns to fix:
  - `Parameter 'xxx' implicitly has an 'any' type` → add type annotation
  - `Property 'xxx' does not exist on type 'YYY'` → fix the property or type
  - `Type 'XXX' is not assignable to type 'YYY'` → fix the assignment
- If an error seems wrong, check the types — don't suppress it
- Consider running `tsc --noEmit` after each individual task to catch errors early
