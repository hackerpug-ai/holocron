# TS-012: Final Type-Check and Security Audit

> Task ID: TS-012
> Type: VERIFY
> Priority: P1
> Estimate: 30 minutes
> Assignee: code-reviewer

## CRITICAL CONSTRAINTS

### MUST
- Run full typecheck with `pnpm tsc --noEmit` — zero errors
- Verify all `clearAll` mutations are guarded
- Confirm rate limiter uses database
- Check no `@ts-ignore` were added

### NEVER
- Sign off if any type errors remain
- Sign off if security fixes are incomplete

### STRICTLY
- All acceptance criteria from Epic 2 must be verified

## SPECIFICATION

**Objective:** Final verification that all type safety and security improvements are complete.

**Success looks like:** Zero type errors, all security fixes verified, Epic 2 complete.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | Epic 2 tasks complete | Running typecheck | Zero errors | `pnpm tsc --noEmit` exits 0 |
| 2 | All security fixes | Verifying implementation | All guards in place | `grep -A5 "clearAll" convex/**/*.ts \| grep -q "process.env"` |
| 3 | Codebase | Checking for suppressions | No `@ts-ignore` | `! grep -r "@ts-ignore" --include="*.ts" convex/` |

## TEST CRITERIA

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | Zero TypeScript errors when running tsc --noEmit | AC-15 | `! pnpm tsc --noEmit 2>&1 \| grep -q "error TS"` | [ ] TRUE [ ] FALSE |
| 2 | Zero `ctx: any` in convex/ when grepping | AC-10, AC-11, AC-49 | `! grep -r "ctx: any" --include="*.ts" convex/` | [ ] TRUE [ ] FALSE |
| 3 | All clearAll mutations guarded when checking | AC-23 | `! grep -B2 "clearAll" --include="*.ts" convex/ \| grep -v "ALLOW_CLEAR_ALL" \| grep -q "ctx.db.delete"` | [ ] TRUE [ ] FALSE |
| 4 | Rate limiter uses database when verifying | AC-24 | `grep -q "ctx.db" convex/research/rateLimiter.ts` | [ ] TRUE [ ] FALSE |
| 5 | No `@ts-ignore` in convex/ when searching | AC-15 | `! grep -r "@ts-ignore" --include="*.ts" convex/` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- Verification only — no code changes expected
- If issues found, create follow-up tasks

### WRITE-PROHIBITED
- Signing off on incomplete work
- Bypassing verification checks

## DESIGN

### References
- PRD Sections 2.2, 2.4
- All Epic 2 tasks

### Verification Checklist
1. **Type Safety**
   - [ ] `tsc --noEmit` passes
   - [ ] Zero `ctx: any` in handlers
   - [ ] Zero `as any` in hooks
   - [ ] Status fields use `v.union(v.literal(...))`
   - [ ] `v.any()` reduced to <10

2. **Security**
   - [ ] All `clearAll` mutations guarded
   - [ ] Rate limiter uses database
   - [ ] `process.env` merge is targeted
   - [ ] `searchResearch` uses actual status

3. **Code Quality**
   - [ ] No `@ts-ignore` added
   - [ ] All imports are correct
   - [ ] No new lint errors

## CODING STANDARDS

- **verification**: Verify all AC before signing off
- **quality**: Zero tolerance for type errors or security gaps

## DEPENDENCIES

**Blocked by**: All Epic 2 tasks (TS-001 through TS-011)

## NOTES

- This is the gatekeeper task for Epic 2
- Run the full verification checklist
- Document any issues found in follow-up tasks
- Don't sign off if anything fails
- Consider running the pre-commit hooks as a final check
- Update INDEX.md to mark Epic 2 complete when done
