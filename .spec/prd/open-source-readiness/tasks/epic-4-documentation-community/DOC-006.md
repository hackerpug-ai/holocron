# DOC-006: Add Architecture Diagram

> Task ID: DOC-006
> Type: DOCUMENTATION
> Priority: P1
> Estimate: 45 minutes
> Assignee: documentation-writer

## CRITICAL CONSTRAINTS

### MUST
- Add architecture diagram showing data flow: RN client → Convex → external APIs
- Can use Mermaid, ASCII art, or image file
- Include key components and their relationships
- Add to README or docs/

### NEVER
- Create a diagram that doesn't match reality

### STRICTLY
- Architecture must be accurate and helpful

## SPECIFICATION

**Objective:** Add an architecture diagram that shows how the system components connect.

**Success looks like:** README includes a clear diagram showing the data flow between React Native client, Convex backend, and external APIs.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | No architecture diagram | Adding diagram | Diagram present | `grep -q "## Architecture\|### System Design" README.md` |
| 2 | After adding | Checking diagram | Shows data flow | `grep -q "Convex\|RN client\|API" README.md` |
| 3 | README | Viewing diagram | Renders correctly | Mermaid or image displays |

## TEST CRITERIA

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | Architecture section in README when checking | AC-32 | `grep -q "## Architecture" README.md` | [ ] TRUE [ ] FALSE |
| 2 | Diagram embedded when checking | AC-32 | `grep -q "```mermaid\|![Architecture" README.md` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- `README.md` (MODIFY) - Add architecture section
- `docs/architecture.md` (NEW) - Optional detailed architecture doc
- Diagram file (NEW) - If using image instead of Mermaid

### WRITE-PROHIBITED
- Creating a diagram that doesn't match the actual system

## DESIGN

### References
- PRD Section 2.5, AC-32

### Mermaid Diagram Example
```markdown
## Architecture

\`\`\`mermaid
graph TB
    subgraph Client["React Native App"]
        RN[Expo Router]
        UI[Components]
        Hooks[Custom Hooks]
    end

    subgraph Backend["Convex Backend"]
        Auth[Authentication]
        DB[(Database)]
        Functions[Server Functions]
        Cron[Scheduled Jobs]
    end

    subgraph External["External APIs"]
        AI[Z.ai / OpenAI]
        Web[Web Search]
        Arxiv[ArXiv API]
    end

    RN --> Functions
    UI --> Hooks
    Hooks --> Functions
    Functions --> DB
    Functions --> Auth
    Functions --> AI
    Functions --> Web
    Functions --> Arxiv
    Cron --> Functions
    Cron --> DB

    style AI fill:#f9f,stroke:#333,stroke-width:2px
    style DB fill:#bbf,stroke:#333,stroke-width:2px
\`\`\`
```

## CODING STANDARDS

- **clarity**: Keep diagrams simple and readable
- **accuracy**: Match the actual system architecture

## DEPENDENCIES

No dependencies.

## NOTES

- Mermaid is supported on GitHub — use it for diagrams
- If Mermaid doesn't work, use ASCII art or an image
- Key components to show:
  - React Native client (Expo Router)
  - Convex backend (functions, database, cron)
  - External APIs (AI providers, search, arXiv)
  - MCP server (separate component)
- Show the data flow clearly
- Consider a high-level diagram + detailed component diagram
- Keep it updated as architecture evolves
- Add brief explanations for each component
