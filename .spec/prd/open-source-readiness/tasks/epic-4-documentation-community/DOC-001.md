# DOC-001: Update README Schema Table

> Task ID: DOC-001
> Type: DOCUMENTATION
> Priority: P1
> Estimate: 20 minutes
> Assignee: documentation-writer

## CRITICAL CONSTRAINTS

### MUST
- Update README schema table to reflect actual 25+ tables (or representative summary)
- Don't list every table if too many — use categories
- Verify against actual `convex/schema.ts`

### NEVER
- Leave outdated table counts or descriptions

### STRICTLY
- Schema documentation must match reality

## SPECIFICATION

**Objective:** Update the README schema table to accurately reflect the Convex schema.

**Success looks like:** README schema table matches the actual tables defined in `convex/schema.ts`.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | Outdated schema table | Updating README | Reflects actual tables | `grep -A50 "## Schema" README.md \| grep -q "chatMessages\|researchSessions"` |
| 2 | After update | Comparing to schema | Tables match reality | Count in README matches `grep -c "export const" convex/schema.ts` |
| 3 | README | Checking accuracy | No phantom tables | All listed tables exist in schema |

## TEST CRITERIA

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | Schema section exists in README when checking | AC-27 | `grep -q "## Schema" README.md` | [ ] TRUE [ ] FALSE |
| 2 | Major tables listed when reading | AC-27 | `grep -c "^\|" README.md \| grep -q "[2-9][0-9]"` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- `README.md` (MODIFY) - Update schema table

### WRITE-PROHIBITED
- Inventing tables that don't exist

## DESIGN

### References
- PRD Section 2.5, AC-27

### Schema Table Format
```markdown
## Database Schema

Holocron uses Convex for data persistence. Key tables include:

### Core
| Table | Description |
|-------|-------------|
| `users` | User profiles and preferences |
| `conversations` | Chat conversations |
| `chatMessages` | Individual chat messages |

### Research
| Table | Description |
|-------|-------------|
| `researchSessions` | Research session state |
| `researchIterations` | Individual research iterations |
| `findings` | Research findings and sources |

### ... (group by domain, don't list all 25+ if too many)

*Full schema defined in [`convex/schema.ts`](../convex/schema.ts)*
```

## CODING STANDARDS

- **documentation**: Keep docs in sync with code
- **clarity**: Group related tables together

## DEPENDENCIES

No dependencies.

## NOTES

- Run `grep "defineTable" convex/schema.ts` to get actual table list
- Don't list every single table if there are 25+ — use categories
- Focus on the most important tables users should know about
- Link to the actual schema file for complete reference
- Consider adding a brief description of what each table does
- Keep the table readable — don't overwhelm with too much detail
