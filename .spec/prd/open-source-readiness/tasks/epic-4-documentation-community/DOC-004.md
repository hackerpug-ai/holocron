# DOC-004: Correct Setup Commands

> Task ID: DOC-004
> Type: DOCUMENTATION
> Priority: P1
> Estimate: 10 minutes
> Assignee: documentation-writer

## CRITICAL CONSTRAINTS

### MUST
- Fix setup commands from `pnpm ios` to `pnpm dev`
- Update any other incorrect commands
- Test that commands actually work

### NEVER
- Leave commands that don't work

### STRICTLY
- All setup commands must be accurate

## SPECIFICATION

**Objective:** Fix incorrect setup commands in the README.

**Success looks like:** Setup commands work when developers follow them.

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | Wrong commands in README | Fixing to pnpm dev | Uses correct command | `grep -q "pnpm dev" README.md` |
| 2 | After fix | Checking for wrong commands | No pnpm ios | `! grep -q "pnpm ios" README.md` |
| 3 | README | Testing commands | Commands work | `pnpm dev` starts successfully |

## TEST CRITERIA

| # | Boolean Statement | Maps To AC | Verify | Status |
|---|-------------------|------------|--------|--------|
| 1 | pnpm dev in setup when checking | AC-30 | `grep -q "pnpm dev" README.md` | [ ] TRUE [ ] FALSE |
| 2 | No pnpm ios when searching | AC-30 | `! grep -q "pnpm ios" README.md` | [ ] TRUE [ ] FALSE |

## GUARDRAILS

### WRITE-ALLOWED
- `README.md` (MODIFY) - Fix setup commands

### WRITE-PROHIBITED
- Leaving commands that don't work

## DESIGN

### References
- PRD Section 2.5, AC-30

### Setup Section Format
```markdown
## Getting Started

### Prerequisites
- Node.js 18+
- pnpm
- iOS Simulator (Mac) or Android Emulator
- Expo Go app (for physical devices)

### Installation

\`\`\`bash
# Clone the repo
git clone https://github.com/yourusername/holocron.git
cd holocron

# Install dependencies
pnpm install

# Copy environment template
cp .env.example .env

# Add your API keys to .env
# EXPO_PUBLIC_LLM_API_KEY=your-key-here
# CONVEX_DEPLOYMENT=your-deployment
# CONVEX_ADMIN_KEY=your-admin-key
\`\`\`

### Running the App

\`\`\`bash
# Start Convex backend
pnpm dev

# In a new terminal, start the Expo dev server
pnpm start

# Scan the QR code with Expo Go (iOS) or press 'a' for Android emulator
\`\`\`
```

## CODING STANDARDS

- **accuracy**: Commands must work as written
- **clarity**: Include all prerequisites

## DEPENDENCIES

No dependencies.

## NOTES

- The PRD specifically mentions `pnpm ios` is wrong — should be `pnpm dev`
- Check package.json for actual scripts
- Common scripts: `dev`, `start`, `build`, `test`
- Make sure environment variables are documented
- Include Convex deployment setup if needed
- Keep it simple — don't overwhelm with options
- Consider adding a "Troubleshooting" section
