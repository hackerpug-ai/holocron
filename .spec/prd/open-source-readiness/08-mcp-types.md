# 2.8 MCP Server Type Safety (P2)

Improve compile-time safety of the MCP server's Convex integration.

## Acceptance Criteria

- [ ] AC-44: `as any` casts in `holocron-mcp/src/tools/` reduced by 50%+ through typed Convex client wrapper or function reference mapping
- [ ] AC-45: MCP server README updated with full tool catalog, required env vars, and Claude Desktop config
