# 2.6 Code Cleanup (P1)

Remove dead code, resolve confusing dual structures, clean up logging.

## Acceptance Criteria

- [ ] AC-35: `screens/` directory either documented as Storybook-only (with a README) or removed/consolidated
- [ ] AC-36: `console.log` calls in `convex/` replaced with structured logging or removed (target: <50 remaining, down from 490)
- [ ] AC-37: `console.time`/`console.timeEnd` and raw `console.log` in `hooks/use-voice-session.ts` replaced with structured logging
- [ ] AC-38: `storybookwork.md`, `PHASE1_VERIFICATION_REPORT.md`, and `RULES.md` either integrated into proper docs or removed
