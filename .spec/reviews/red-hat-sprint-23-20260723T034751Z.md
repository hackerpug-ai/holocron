# Red-Hat Review Report — Sprint 23 (Deterministic Human Gate, Steering and Fulcrum Seams)

**Report Date**: 2026-07-23T03:47:51Z
**Target**: Sprint 23 — Deterministic Human Gate, Steering and Fulcrum Seams (UC-SVC-05; T-SVC-017…020)
**Sprint folder**: `.spec/prds/mk6-migration/tasks/sprint-23-deterministic-human-gate-steering-and-fulcrum-seams/`
**Reviewed By**: `mastra-reviewer` (domain), `test-quality-reviewer` (standing test-reality lens)
**Test-reality lens**: ran (implemented mode — code + tests exist; oracle/seed/negative-path audits applied; **mutation probe NOT run** per STRICT REVIEW CONTRACT read-only mandate — refused at the standing seat and documented as not-run)
**Reviewed SHA (auditable land anchor)**: `3885e9100ea6999d8702e4a79fc3289278f44cd1` (sprint-23 completion tip; product code identical to sprint `main_tip` `193bb23148f026c341e40ba18351aa455b593cdc` — the only delta over `193bb231` is the metadata-only `chore(sprint-23): status sync` commit)
**Per-task landed SHAs**:
- gate-4 `4deb1e29…` · gate-1 `5e9dbb6ff3e3c0997be6a8b1ef1420b0b99742f6` · gate-2 `7fea20a12b5dc8236bb820ef58abc6a0db5d44db`
- gate-3 `1579b9aa3cf7a9f42f6ac61cc4b398bd7cd04b6a` · gate-5 `7eb88845b2e50462ec26dd4f45606baab1baf80f`

**Pass type**: independent re-review (second pass). A prior report (`red-hat-sprint-23-20260723T021406Z.md`, ~1.5h earlier) reached an APPROVED verdict; this pass independently re-verifies against the same SHAs under a STRICT read-only contract. AC verdicts recorded **in this report only** — no task file was edited in place, per the contract.

**Verdict (review/qa stage)**: **APPROVED WITH ONE HIGH POLICY-COMPLIANCE FINDING (advisory, non-blocking to code correctness)**. This verdict does not land work — the run stage merges the reviewed commit to `main` after approval.

---

## STRICT REVIEW CONTRACT — compliance log

| Constraint | Honored? | Evidence |
|---|---|---|
| Read-only; no mutation of tracked source / migrations / task files / evidence / Postgres | ✅ | No `write`/`edit` to any source, migration, task `.md`, or evidence file. AC verdicts live in this report only. |
| No mutation probes | ✅ | Standing `test-quality-reviewer` seat was instructed to skip the mutation probe; it was **refused and documented as not-run**. Oracle strength judged via static grep + existing real-service artifacts (RED output, green-gate logs, live Postgres trigger source). |
| Static inspection + existing real-service artifacts only | ✅ | All verdicts cite `file:line` from `git show`/`rg`/`Read` against the reviewed SHA. No commands were executed against live Postgres. |
| Never merge / push / move checkout to another branch | ✅ | No `git checkout`/`merge`/`push`/`rebase`. HEAD remains `1df9d37a` (sprint-24 planning); sprint-23 code inspected at its landed SHAs. |
| Verdict does not land work; cite exact SHA | ✅ | Reviewed SHA `3885e910` cited above; run stage merges on approval. |

---

## Executive Summary

Sprint 23's landed implementation is **functionally correct and well-evidenced**, independently confirmed in this second pass. All three deterministic human-gate rules are enforced by a Postgres `SECURITY DEFINER` trigger (`enforce_mission_human_gate()`, `0025_deterministic_human_gate.sql:10-60`) plus a partial unique index (`mission_runs_active_subject_wip_one_uidx`, `0025:4-7`) — **not** handler-only. Mid-run steering, ASSAY≠CHALLENGE distinct-instance enforcement (`cycle.ts:483` `MISSION_ASSAY_CHALLENGE_COLLISION` hard-fail), and the pure-TS evidence gate (grep for `generateText`/`streamText`/`@mastra/core/agent`/`openai/` → exit 1, zero LLM surfaces) are real. The fulcrum-seams capstone compiles against 5 real surfaces; fulcrum is an alias of `evidence-research` (`evidence-research.ts:15`) with zero `templateKey:'fulcrum'` anywhere. No Category 1-4 stubs in the changed source (the sole "placeholder" grep hit is inside an invariant *comment* at `cycle.ts:98`). The RED suite has no `vi.mock`/`:memory:` substitution (TC-4 clean).

The **one HIGH finding is a policy-compliance deviation, not a code defect**: the sprint's own `sprint-goal-state.json` and `.tmp/gate-3/runtime-model.txt` document that the required implementer model `grok-composer-2.5-fast` was **rejected by the harness** ("Unknown Task.model slug; only grok-4.5 valid") and gate-1/2/3 ran on the `grok-4.5` fallback. This is unchanged from the prior report and still requires a conscious human decision before land.

---

## Gate Pre-Check (skill [2.5] — deterministic, skill-emitted)

### Executability
All 6 Human Testing Gate steps' entry points resolve in real source — vitest test names (`gate-1 uncited-kill rejected`, `gate-2 steering-next-cycle`, …) exist in `mission-engine-red.test.ts`; `holo fulcrum:authorable-check` and `holo mission:cycle` exist in `holo.ts`. ✅ No auto-findings.

### Oracle provability
Every oracle resolves in real source — `UNCITED_KILL_REJECTED`/`PROBE_REQUIRED_FOR_VALIDATED` raised in `enforce_mission_human_gate()` (`0025:31,45`); `WIP_ONE_EXCEEDED` enforced via the partial unique index (`0025:4-7`); `SUFFICIENT`/`INSUFFICIENT` emitted by `fulcrum-authorable-check.ts`; `fleet:model:*:inst-*` minted by `mintFleetInstanceId` (`cycle.ts:99-104`); `MISSION_ASSAY_CHALLENGE_COLLISION` at `cycle.ts:483`. ✅ No auto-findings.

### Non-empty result
All 6 step logs non-empty (`log_bytes` 481-735 in `gate-results.json`) with real vitest output and matching oracles. ✅

### Evidence freshness (sprint claims `goal: ACHIEVED`)
`gate-results.json` verdict `"pass"`, `steps_executed == steps_total == 6`. Freshness inspection:
- `gate-results.json` file mtime `14:25:35` local; task `.md` files mtime `14:26:02-05` local — task files are ~30s **newer**.
- **Resolved as non-finding**: both were committed in the **same** status-sync commit `3885e910` (`git show --stat` confirms). The task-file delta is metadata-only (`> Status: ⬜ Pending` → `> Status: ✅ Completed` + a comment-close newline) — no source/spec body change. All **source** commits (gate-1 `5e9dbb6f` 13:56, gate-2 `7fea20a1` 13:52, gate-3 `1579b9aa` 14:09, gate-5 `7eb88845` 14:18) predate `gate-results.json`. The mtime gap is the expected causal ordering (gate ran → passed → status recorded in the same commit that created `gate-results.json`), not staleness. ✅ No auto-finding.

---

## HIGH Confidence Findings

- [ ] **FINDING H1: Implementer runtime-model policy violation — required `grok-composer-2.5-fast` never dispatched; gate-1/2/3 ran on `grok-4.5` fallback.** | Severity: **HIGH (policy compliance, advisory/non-blocking to code correctness)**
  - **Source**: `sprint-goal-state.json` → `runtime_model_policy`; `.tmp/gate-3/runtime-model.txt`
  - **Evidence (quoted)**:
    - `sprint-goal-state.json`: `"required_implementer": "grok-composer-2.5-fast"`, `"available_in_harness": ["grok-4.5"]`, `"composer_dispatch": "REJECTED — Unknown Task.model slug; only grok-4.5 valid"`, `"conductor": "grok-4.5 medium"`
    - `.tmp/gate-3/runtime-model.txt`: `spawn_attempt: model=grok-composer-2.5-fast → REJECTED (Unknown Task.model slug; Valid: grok-4.5)`; `effective_runtime: grok-4.5 (only available slug in this Grok Build session)`; `agent_frontmatter_model: grok-composer-2.5-fast (mastra-implementer.md)`
  - **Why HIGH, not blocking**: (1) transparently self-documented — not hidden; (2) the harness *physically could not* dispatch the slug (`Unknown Task.model slug`), an environment constraint, not a silent skip; (3) code output triple-verified (dual-lens review + 6/6 Human Gate + two independent red-hat passes). HIGH because a model-policy deviation on three P0 implementation tasks must be consciously accepted by the human before land.
  - **Remediation decision is the human's**: (a) accept `grok-4.5` output (verification stack backs it), (b) re-run under a Composer-capable harness, or (c) relax the sprint `required_implementer` field with documented rationale.

---

## MEDIUM Confidence Findings

- [ ] **FINDING M1: gate-5 TC-4 contract verify command points at the instance-ID *consumer* (`inspection.ts`), not the *producer* (`cycle.ts:104`).** | Severity: **MEDIUM (non-blocking, contract polish)**
  - **Agent**: `mastra-reviewer` (also flagged in landed `gate-5-seam-sufficiency-review.md`)
  - **Evidence**: TC-4 greps `inspection.ts` for `fleet:model:.*inst-`; the mint site is `cycle.ts:104` `mintFleetInstanceId`. AC-4's behavioral intent is satisfied (real fleet IDs, not hardcoded), but the literal verify command would fail against a future refactor. Repoint at `cycle.ts` or assert both mint + consumption.

---

## LOW Confidence Findings (Single Agent / Track)

- [ ] **FINDING L1: gate-3 added new platform code (`fulcrum-authorable-check.ts`, 329 lines) — but it is proof tooling, not a fulcrum product template.** `git show --stat 1579b9aa` confirms. The "zero new platform code" AC is about the fulcrum *runtime/template* surface (satisfied: alias-only at `evidence-research.ts:15`). Informational.
- [ ] **FINDING L2: Instance-id grammar divergence** — `runtime.ts` uses `${modelRevision}:assay|challenge:${run.id}` while `cycle.ts:104` uses `fleet:model:<model>:inst-<uuid>`. Both dynamic (not hardcoded). Track convergence if desired.
- [ ] **FINDING L3: Harness-reported agent frontmatter model disagrees with on-disk agent definition** — `.tmp/gate-3/runtime-model.txt` reports `grok-composer-2.5-fast (mastra-implementer.md)`, but on-disk `~/.claude/agents/mastra-implementer/agent.md:3` reads `model: sonnet`. Reconcile so future `runtime_model_policy` blocks are unambiguous.

---

## AC VERDICT TABLE (all tasks, all acceptance criteria — recorded in report only, per STRICT CONTRACT)

Legend: ✅ PASS · ❌ FAIL · ⚠️ PARTIAL

### gate-1 — Deterministic human-gate handlers
| # | AC Item | Verdict | Evidence | Notes |
|---|---------|---------|----------|-------|
| AC-1 | Uncited kill rejected deterministically | ✅ PASS | `0025_deterministic_human_gate.sql:31` raises `UNCITED_KILL_REJECTED`; trigger `mission_verdicts_human_gate_before_insert` (`0025:57-60`) BEFORE INSERT; error-code oracle present 5× in test file | SECURITY DEFINER, not handler-only |
| AC-2 | Second concurrent build refused (WIP=1) | ✅ PASS | `0025:4-7` partial unique index `mission_runs_active_subject_wip_one_uidx ON (template_key, goal) WHERE status IN ('pending','running','suspended')` | Postgres-enforced |
| AC-3 | Advance→validated refused without probe | ✅ PASS | `0025:45` raises `PROBE_REQUIRED_FOR_VALIDATED`; probe check queries `mission_stage_runs` (`0025:37-43`) | SECURITY DEFINER |
| AC-4 | Constraints survive handler restart | ✅ PASS | `0027_mission_human_gate_crash_boundary.sql` crash-boundary variant; `0025:28-30` `pg_sleep(30)` hook for crash-injection; gate-1 commit `5e9dbb6f` = real SIGKILL crash/replay stabilization | Real process crash |

### gate-2 — Mid-run steering + ASSAY≠CHALLENGE
| # | AC Item | Verdict | Evidence | Notes |
|---|---------|---------|----------|-------|
| AC-1 | Steering takes effect next cycle | ✅ PASS | `cycle.ts` reads `mission_steering` per cycle (no workflow restart); GREEN log in `.tmp/gate-2/green-output.txt` | Real cycle |
| AC-2 | ASSAY≠CHALLENGE distinct instances | ✅ PASS | `cycle.ts:99-104` `mintFleetInstanceId` → `fleet:model:${model}:inst-${randomUUID()}`; :450/:468 distinct assay/challenge calls; `MISSION_ASSAY_CHALLENGE_COLLISION` hard-fail at `:483` | Real fleet UUIDs |
| AC-3 | Refuting claims pass identical admission gate | ✅ PASS | `evidence-gate.ts` grep for `generateText\|streamText\|@mastra/core/agent\|openai/` → exit 1 (zero LLM surfaces) | Pure-TS, no LLM |
| AC-4 | CLI emits ASSAY vs CHALLENGE instance IDs | ✅ PASS | `holo mission:cycle` wired in `holo.ts` (gate-2 commit `+69` lines); concrete non-placeholder IDs in artifacts | |

### gate-3 — Fulcrum-seams capstone
| # | AC Item | Verdict | Evidence | Notes |
|---|---------|---------|----------|-------|
| AC-1 | Command compiles fulcrum against all 5 seams | ✅ PASS | `fulcrum-authorable-check.ts` (329 lines, `git show --stat 1579b9aa`); step-6 human log `Overall: SUFFICIENT` | Fail-fast on MISSING |
| AC-2 | Fulcrum alias uses existing template | ✅ PASS | `evidence-research.ts:15` `'fulcrum'` in instantiations; `rg templateKey.*fulcrum` → empty | Zero new template code |
| AC-3 | Seam citations concrete file:line | ✅ PASS | step-6 log cites mission.ts, evidence.ts, evidence-gate.ts, evidence-research.ts, documents.ts | No placeholders |
| AC-4 | Fails fast on missing seam | ✅ PASS | `authorable-check-fails-fast-missing-seam` green; `INSUFFICIENT` + exit 1 on deleted gate file | |

### gate-4 — RED test suite
| # | AC Item | Verdict | Evidence | Notes |
|---|---------|---------|----------|-------|
| AC-1 | RED tests for gate-1 rules | ✅ PASS | `.tmp/gate-4/red-output.txt` = 11,831 bytes, 8 `FAIL` lines | RED evidence captured |
| AC-2 | RED tests for gate-2 steering/instances | ✅ PASS | 4 gate-2 tests present; RED output appended | Real fleet in RED |
| AC-3 | Tests follow mission-engine-red pattern | ✅ PASS | `PLATFORM_IT=1`, `public_api` seed; `vi.mock`/`:memory:` grep clean (TC-4) | Pattern-compliant |
| AC-4 | RED output captured | ✅ PASS | red-output.txt 11,831 bytes (>1KB), concrete FAIL lines | |

### gate-5 — Review seam sufficiency
| # | AC Item | Verdict | Evidence | Notes |
|---|---------|---------|----------|-------|
| AC-1 | 5 seams validated w/ concrete citations | ✅ PASS | `gate-5-seam-sufficiency-review.md` cites 5 file:line pairs; re-verified here | |
| AC-2 | Deterministic rules Postgres-enforced | ✅ PASS | `SECURITY DEFINER` (`0025:13`) + partial unique index (`0025:4-7`) + trigger (`0025:57-60`) | Not handler-only |
| AC-3 | Zero new platform code for fulcrum | ✅ PASS | `rg templateKey.*fulcrum services/platform/src/mission/templates/` empty | Alias-only |
| AC-4 | Instance IDs real fleet values | ✅ PASS | `cycle.ts:104` `randomUUID()` mint | See M1 on contract grep path |

**Completion Gate**: ALL 20 ACs PASS → `reviewed` (code-level). H1 is a process/policy advisory that does not change any AC verdict.

---

## STUB FINDINGS

None. Static grep for Category 1-4 stubs against changed source (`missions.ts`, `cycle.ts`, `fulcrum-authorable-check.ts`, `evidence-gate.ts`, `mission-engine-red.test.ts`) returned clean. Specifically:
- No `TODO`/`FIXME`/`verify manually` in production paths.
- No `z.any()` on gate inputs.
- No hardcoded `assay-instance`/`challenge-instance` — mint uses `randomUUID()`.
- No handler-side re-implementation of the SECURITY DEFINER citation check (enforcement is trigger-authoritative).
- No `vi.mock`/`:memory:` in the RED suite (TC-4 clean).

---

## GAPS & RISKS
1. **Policy-deviation risk (H1)** — if the human requires Composer-grade output for mission-engine determinism surfaces, the `grok-4.5` fallback may not meet that bar despite passing functional gates.
2. **Contract-verify drift (M1)** — TC-4 points at consumer not producer; a future refactor could silently break the check while behavior stays correct.
3. **Instance-id grammar divergence (L2)** — two dynamic shapes across `runtime.ts`/`cycle.ts`; future consistency debt.
4. **Frontmatter provenance ambiguity (L3)** — harness-read frontmatter disagrees with on-disk agent file.

## ASSUMPTIONS (Unvalidated)
- **Assumption**: `grok-4.5` output ≈ `grok-composer-2.5-fast` quality for Mastra mission-engine TS. **Risk**: unvalidated — environment forced-choice, not studied equivalence; mitigated by triple-verification, not eliminated.
- **Assumption**: Human Testing Gate runner executed real shipped commands. **Risk**: low — step logs show real vitest timings + real Postgres row-count assertions.

## CONTRADICTIONS
- **Frontmatter model**: `.tmp/gate-3/runtime-model.txt` reports `grok-composer-2.5-fast`; on-disk `mastra-implementer/agent.md:3` reads `model: sonnet`. Harness was reading an overlaid definition. Recorded as L3.

---

## Agent Reports (Summary)

- **`mastra-reviewer` (domain)**: Independently re-verified Postgres enforcement (`0025` SECURITY DEFINER + partial unique index), fulcrum alias-only, real fleet instance IDs, pure-TS evidence gate, 5 seam citations. No stubs. Flagged M1 + L1 + L2.
- **`test-quality-reviewer` (standing test-reality lens, implemented mode, **mutation probe refused/not-run per STRICT CONTRACT**)**:
  - *Oracle strength*: strong — every behavioral AC asserts concrete row counts / HTTP codes / unequal UUIDs; a stub handler skipping checks would FAIL (verdict inserted → count=1). Negative controls explicit in every scenario. Mutation probe **not run** (read-only mandate); static probe + live-gate evidence confirm oracles bite.
  - *Seed/fixture reality*: real — `seed_method: public_api`, real Postgres, zero `vi.mock`/`:memory:`. Fixtures reference real columns.
  - *Negative-path coverage*: complete — every AC carries MUST_NOT_OBSERVE assertions. Flagged L3.

---

## Recommendations by Category
1. **Gaps**: Reconcile `runtime-model.txt` frontmatter with on-disk `mastra-implementer/agent.md` (L3).
2. **Risks**: Human must consciously accept H1 before land.
3. **Assumptions**: Repoint gate-5 TC-4 verify at `cycle.ts` mint site (M1).
4. **Contradictions**: Track instance-id grammar convergence (L2).

---

## Metadata
- **Agents**: `mastra-reviewer` (Glob/Grep/Read/Bash), `test-quality-reviewer` (standing seat, Glob/Grep/Read/Bash — mutation probe refused per contract)
- **Confidence Framework**: HIGH (corroborated ≥2 sources), MEDIUM (2 agents/records agree), LOW (single agent)
- **Report Generated**: 2026-07-23T03:47:51Z
- **Reviewed SHA**: `3885e9100ea6999d8702e4a79fc3289278f44cd1` (product code identical to sprint `main_tip` `193bb231`)
- **Read-only QA**: No product code / migration / task file / evidence modified; no branch checkout / push / merge. Mutation probe refused. Verdict does not land work — run stage merges after approval.
- **Next Steps**: [Human decision on H1] → [Run stage merges `193bb231` to `main` on approval] → [Optionally address M1/L2/L3 in a future polish sprint]
