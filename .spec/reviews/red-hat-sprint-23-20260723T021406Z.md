# Red-Hat Review Report — Sprint 23 (Deterministic Human Gate, Steering and Fulcrum Seams)

**Report Date**: 2026-07-23T02:14:06Z
**Target**: Sprint 23 — Deterministic Human Gate, Steering and Fulcrum Seams (UC-SVC-05; T-SVC-017…020)
**Sprint folder**: `.spec/prds/mk6-migration/tasks/sprint-23-deterministic-human-gate-steering-and-fulcrum-seams/`
**Reviewed By**: `mastra-reviewer` (domain), `test-quality-reviewer` (standing test-reality lens)
**Test-reality lens**: ran (implemented mode — code + tests exist; oracle/seed/negative-path audits applied; mutation probe bounded to static grep + live-gate evidence review, no source mutation per read-only QA mandate)
**Reviewed SHA (auditable land anchor)**: `3885e9100ea6999d8702e4a79fc3289278f44cd1` (HEAD; product code identical to sprint `main_tip` `193bb23148f026c341e40ba18351aa455b593cdc` — the only delta is the metadata-only `chore(sprint-23): status sync` commit)
**Per-task landed SHAs**:
- gate-4 `4deb1e299fac40d1ef47fa82b84b44a0d86c891f`
- gate-1 `5e9dbb6ff3e3c0997be6a8b1ef1420b0b99742f6`
- gate-2 `7fea20a12b5dc8236bb820ef58abc6a0db5d44db`
- gate-3 `1579b9aa3cf7a9f42f6ac61cc4b398bd7cd04b6a`
- gate-5 `7eb88845b2e50462ec26dd4f45606baab1baf80f`

**Verdict (review/qa stage)**: **APPROVED WITH ONE HIGH POLICY-COMPLIANCE FINDING (advisory, non-blocking to code correctness)**. This verdict does not land work — the run stage merges the reviewed commit to `main` after approval. All 5 tasks satisfy every acceptance criterion at the code level; the HIGH finding is a documented runtime-model-policy deviation that the human must consciously accept before the land proceeds.

---

## Executive Summary

Sprint 23's landed implementation is **functionally correct and well-evidenced**. All three deterministic human-gate rules are enforced by a Postgres `SECURITY DEFINER` trigger (`enforce_mission_human_gate()`) plus a partial unique index (`mission_runs_active_subject_wip_one_uidx`) — **not** handler-only. Mid-run steering, ASSAY≠CHALLENGE distinct-instance enforcement, and the pure-TS evidence gate (zero LLM surfaces) are all real and verified by 13/13 integration tests against real Postgres + real fleet (98s wall clock). The fulcrum-seams capstone compiles against 5 real surfaces with concrete file:line citations, and fulcrum is an alias of `evidence-research` (zero new template code). The Human Testing Gate passed 6/6 with fresh evidence newer than every source commit, and the gate pre-check (executability, oracle provability, non-empty result, evidence freshness) is clean.

The **one HIGH finding is a policy-compliance deviation, not a code defect**: the sprint's own `sprint-goal-state.json` and `.tmp/gate-3/runtime-model.txt` document that the required implementer model `grok-composer-2.5-fast` was **rejected by the harness** ("Unknown Task.model slug; only grok-4.5 valid") and all three implementation tasks (gate-1/2/3) ran on the `grok-4.5` fallback instead. Per the review mandate, this is treated as a policy-compliance finding because the execution evidence affirmatively proves the required Composer model was **not** used. The code output has been independently triple-verified (dual-lens review + human gate + this red-hat), so the finding is advisory/non-blocking to correctness — but it must reach the human before land.

---

## HIGH Confidence Findings

- [ ] **FINDING H1: Implementer runtime-model policy violation — required `grok-composer-2.5-fast` never dispatched; gate-1/2/3 ran on `grok-4.5` fallback.** | Severity: **HIGH (policy compliance, advisory/non-blocking to code correctness)**
  - **Source**: `sprint-goal-state.json` → `runtime_model_policy`; `.tmp/gate-3/runtime-model.txt`
  - **Evidence (quoted)**:
    - `sprint-goal-state.json`: `"required_implementer": "grok-composer-2.5-fast"`, `"available_in_harness": ["grok-4.5"]`, `"composer_dispatch": "REJECTED — Unknown Task.model slug; only grok-4.5 valid"`, `"conductor": "grok-4.5 medium"`
    - `.tmp/gate-3/runtime-model.txt`: `spawn_attempt: model=grok-composer-2.5-fast → REJECTED (Unknown Task.model slug; Valid: grok-4.5)`; `effective_runtime: grok-4.5 (only available slug in this Grok Build session)`; `note: Project policy requires Composer for *-implementer; this harness cannot dispatch that slug.`
  - **Policy basis**: The sprint's recorded `runtime_model_policy.required_implementer` field is the binding scope for this sprint. (Note: the project `RULES.md` "Codex Runtime Overrides" section governs a *different* harness — ChatGPT-account Codex sessions — and does not list `grok-composer-2.5-fast`; the Composer requirement originates in the sprint's own policy block plus the `mastra-implementer` frontmatter as recorded by the harness. The discrepancy between the harness-reported frontmatter `grok-composer-2.5-fast` and the on-disk `~/.claude/agents/mastra-implementer/agent.md` `model: sonnet` is itself worth a LOW note — see L3 — but does not weaken the sprint-scope policy violation.)
  - **Why HIGH, not CRITICAL/blocking**: (1) The deviation is transparently self-documented in sprint state — it was not hidden. (2) The harness *physically could not* dispatch the required slug (`Unknown Task.model slug`), so this is an environment constraint, not a silent choice to skip Composer. (3) The code output was independently verified three ways — dual-lens product+technical review at land time, the 6/6 Human Testing Gate, and this red-hat review — all of which confirm the implementation is real and correct. The finding is HIGH because a model-policy deviation on three P0 implementation tasks must be consciously accepted by the human before the land, not buried in a status field.
  - **Remediation decision is the human's**: (a) accept the `grok-4.5` output as-is (the verification stack backs it), (b) re-run the three implementation tasks under a harness that can dispatch `grok-composer-2.5-fast`, or (c) relax the sprint's `required_implementer` policy field and document the rationale. This review does not prescribe which.

---

## MEDIUM Confidence Findings

- [ ] **FINDING M1: gate-5 TC-4 contract verify command points at the instance-ID *consumer*, not the *producer*.** | Severity: **MEDIUM (non-blocking, contract polish)**
  - **Agent**: `mastra-reviewer` (also flagged in the landed `gate-5-seam-sufficiency-review.md` as "MEDIUM-note")
  - **Evidence**: gate-5 TC-4 verify greps `services/platform/src/research/inspection.ts` for `fleet:model:.*inst-`. The mint site is `services/platform/src/mission/cycle.ts:104` (`function mintFleetInstanceId(...)` → `return \`fleet:model:${model}:inst-${randomUUID()}\``). `inspection.ts:127-140` *consumes* fleet IDs from committed stage outputs; it does not contain the literal mint grammar.
  - **Assessment**: AC-4's **behavioral** intent is satisfied — instance IDs are real fleet values, not hardcoded. But the contract verify command would fail if run literally against `inspection.ts` in a future codebase where the consumer's regex surface changes. Point TC-4 at `cycle.ts` or assert both mint + consumption.
  - **Status**: non-blocking; does not affect seam sufficiency.

---

## LOW Confidence Findings (Single Agent / Track)

- [ ] **FINDING L1: gate-3 added new platform code (`fulcrum-authorable-check.ts`, 329 lines) — but it is proof tooling, not a fulcrum product template.**
  - **Agent**: `mastra-reviewer`
  - **Evidence**: `git show --stat 1579b9aa` → `services/platform/src/cli/commands/fulcrum-authorable-check.ts | 329 +++`. gate-3 AC "zero new platform code" is about the fulcrum **runtime/template** surface; that is satisfied (no `templateKey: 'fulcrum'`, alias-only in `EVIDENCE_RESEARCH_INSTANTIATIONS` at `evidence-research.ts:15`). The authorable-check is intentional gate-3 proof tooling.
  - **Status**: informational; aligns with the landed gate-5 review's LOW-1.

- [ ] **FINDING L2: Long mission executor uses a different instance-id shape than the gate-2 cycle path.**
  - **Agent**: `mastra-reviewer`
  - **Evidence**: `services/platform/src/mission/runtime.ts` uses `${modelRevision}:assay|challenge:${run.id}` while `cycle.ts:104` uses `fleet:model:<model>:inst-<uuid>`. Both are dynamic (not hardcoded equality tokens). Track convergence if a single fleet-trace grammar is desired. Aligns with landed gate-5 LOW-2.

- [ ] **FINDING L3: Harness-reported agent frontmatter model disagrees with on-disk agent definition.**
  - **Agent**: `test-quality-reviewer` (standing seat)
  - **Evidence**: `.tmp/gate-3/runtime-model.txt` reports `agent_frontmatter_model: grok-composer-2.5-fast (mastra-implementer.md)`, but `~/.claude/agents/mastra-implementer/agent.md:3` reads `model: sonnet`. This means the executing harness was reading a different/overlaid agent definition than the one on disk. Not a Sprint 23 code defect, but it complicates auditing the exact provenance of the Composer policy and should be reconciled so future sprint `runtime_model_policy` blocks are unambiguous.

---

## AC VERDICT TABLE (all tasks, all acceptance criteria)

Legend: ✅ PASS · ❌ FAIL · ⚠️ PARTIAL

### gate-1 — Deterministic human-gate handlers
| # | AC Item | Verdict | Evidence | Notes |
|---|---------|---------|----------|-------|
| AC-1 | Uncited kill verdict rejected deterministically | ✅ PASS | `0027_mission_human_gate_crash_boundary.sql:11` raises `UNCITED_KILL_REJECTED`; test `gate-1 uncited-kill rejected` (mission-engine-red.test.ts:3332) green vs real Postgres | SECURITY DEFINER, not handler-only |
| AC-2 | Second concurrent build refused (WIP=1) | ✅ PASS | `0025_deterministic_human_gate.sql:4` partial unique index `mission_runs_active_subject_wip_one_uidx` on `(template_key, goal) WHERE status IN ('pending','running','suspended')`; test `gate-1 WIP=1` (:3364) green | Postgres-enforced |
| AC-3 | Advance→validated refused without probe | ✅ PASS | `0027:14` raises `PROBE_REQUIRED_FOR_VALIDATED`; test `gate-1 unprobed-advance refused` (:3421) green | SECURITY DEFINER |
| AC-4 | Postgres constraints survive handler restart | ✅ PASS | Test `gate-1 crash/restart replay` (:3535) uses real `SIGKILL` + `HOLO_TEST_MISSION_VERDICT_CRASH_AT` hook; asserts zero partial rows + idempotent replay; 6-commit iteration history (0744c3f7→5e9dbb6f) proves real crash wrangling | Real process crash, not simulated |

### gate-2 — Mid-run steering + ASSAY≠CHALLENGE
| # | AC Item | Verdict | Evidence | Notes |
|---|---------|---------|----------|-------|
| AC-1 | Steering takes effect next cycle | ✅ PASS | Test `gate-2 steering-next-cycle` (:3774) green, 15.5s wall (real cycle); artifact `.tmp/gate-2/artifacts/gate-4-steering-next-cycle-real-cycle.json` | Real following cycle |
| AC-2 | ASSAY≠CHALLENGE distinct instances | ✅ PASS | `cycle.ts:99-104` `mintFleetInstanceId` → `fleet:model:${model}:inst-${randomUUID()}`; distinct assay/challenge calls at :450/:468; `MISSION_ASSAY_CHALLENGE_COLLISION` hard-fail at :483; real unequal UUIDs in artifacts | Real fleet, not hardcoded |
| AC-3 | Refuting claims pass identical admission gate | ✅ PASS | Test `gate-2 admission parity` (:3882) green; `evidence-gate.ts` has **zero** `generateText`/`streamText`/`@mastra/core/agent`/`openai/` (grep exit 1); admission reports `pureTs: true` | Pure-TS, no LLM |
| AC-4 | CLI emits ASSAY vs CHALLENGE instance IDs | ✅ PASS | `holo mission:cycle` wired in `holo.ts`; test `gate-2 CLI evidence surface` (:3955) green; JSON projection asserts concrete non-placeholder IDs | |

### gate-3 — Fulcrum-seams capstone
| # | AC Item | Verdict | Evidence | Notes |
|---|---------|---------|----------|-------|
| AC-1 | Command compiles fulcrum against all 5 seams | ✅ PASS | `cli/commands/fulcrum-authorable-check.ts` checks contract/ledger/gate/role-bindings/publish with live Postgres probes; step-6 human log shows `Overall: SUFFICIENT` | Fail-fast on MISSING |
| AC-2 | Fulcrum alias uses existing template | ✅ PASS | `evidence-research.ts:15` `'fulcrum'` in `EVIDENCE_RESEARCH_INSTANTIATIONS`; `holo.ts:3965` `case 'fulcrum'` → `instantiation='fulcrum'`; no `templateKey:'fulcrum'` anywhere | Zero new template code |
| AC-3 | Seam citations are concrete file:line | ✅ PASS | step-6 log: 5 `PASS — …:line` lines (mission.ts:14, evidence.ts:32-150, evidence-gate.ts:55, evidence-research.ts:88-89, documents.ts:31) | No placeholders |
| AC-4 | Command fails fast on missing seam | ✅ PASS | Test `authorable-check-fails-fast-missing-seam` green (e2e-integration.txt); `INSUFFICIENT` + exit 1 on deleted gate file | |

### gate-4 — RED test suite
| # | AC Item | Verdict | Evidence | Notes |
|---|---------|---------|----------|-------|
| AC-1 | RED tests for gate-1 deterministic rules | ✅ PASS | `.tmp/gate-4/red-output.txt` = 11,831 bytes, 8 `FAIL` lines; suite shipped 6 gate-1 tests (exceeds the 4-test minimum) | RED evidence captured |
| AC-2 | RED tests for gate-2 steering/instances | ✅ PASS | 4 gate-2 tests present (:3774/:3819/:3882/:3955); RED output appended per verify command | Real fleet in RED |
| AC-3 | Tests follow existing mission-engine-red pattern | ✅ PASS | GIVEN/WHEN/THEN/MUST_OBSERVE comments present; `public_api` seed; `PLATFORM_IT=1`; zero `vi.mock`/`:memory:` (TC-4 grep clean) | Pattern-compliant |
| AC-4 | RED output captured for all tests | ✅ PASS | red-output.txt > 1KB (11,831 bytes), 8 distinct FAIL lines with `Expected: 0, Actual: 1`-style details | |

### gate-5 — Review seam sufficiency
| # | AC Item | Verdict | Evidence | Notes |
|---|---------|---------|----------|-------|
| AC-1 | All 5 seams validated with concrete citations | ✅ PASS | Landed `gate-5-seam-sufficiency-review.md` cites 5 file:line pairs; independently re-verified in this review | |
| AC-2 | Deterministic rules are Postgres-enforced | ✅ PASS | `SECURITY DEFINER` function + `mission_runs_active_subject_wip_one_uidx` partial unique index + trigger (0025/0027); idempotency uidx on `(run_id, request_key)` | Not handler-only |
| AC-3 | Zero new platform code for fulcrum | ✅ PASS | `rg templateKey.*fulcrum services/platform/src/mission/templates/` empty | Alias-only |
| AC-4 | Instance IDs are real fleet values | ✅ PASS | `cycle.ts:104` mint with `randomUUID()`; real unequal IDs in `.tmp/gate-2/artifacts/*-cycle.json` | See M1 on contract grep path |

**Completion Gate**: ALL ACs PASS → `reviewed` (code-level). The HIGH finding H1 is a process/policy advisory that does not change any AC verdict.

---

## HUMAN TESTING GATE (skill [2.5] pre-check)

- **Executability**: All 6 steps' entry points resolve in source — vitest test names match (`gate-1 uncited-kill rejected`, `gate-2 steering-next-cycle`, etc.); `holo fulcrum:authorable-check` and `holo mission:cycle` exist in `holo.ts`. ✅ No auto-findings.
- **Oracle provability**: Every oracle resolves in real source — `UNCITED_KILL_REJECTED`/`WIP_ONE_EXCEEDED`/`PROBE_REQUIRED_FOR_VALIDATED` raised in `enforce_mission_human_gate()`; `SUFFICIENT`/`INSUFFICIENT` emitted by `fulcrum-authorable-check.ts`; `fleet:model:*:inst-*` minted by `mintFleetInstanceId`. ✅ No auto-findings.
- **Non-empty result**: All 6 step logs non-empty with real vitest output and matching oracles (e.g., step-5: 3 tests passed, 47.8s — real fleet latencies, not stubs). ✅
- **Evidence freshness (sprint claims complete)**: `gate-results.json` written 2026-07-22T20:24:22Z (mtime 14:25:35 local); newest task file mtime 14:20:14; newest sprint commit `3885e910` at 14:26:05 (metadata-only status sync). Gate evidence is fresh relative to all implementation commits. `verdict:"pass"`, `steps_executed == steps_total == 6`. ✅

---

## STUB FINDINGS (gate-1..4)

None. Static grep for Category 1-4 stubs against changed files (`services/platform/src/http/missions.ts`, `cycle.ts`, `fulcrum-authorable-check.ts`, `evidence-gate.ts`, `mission-engine-red.test.ts`) returned clean. Specifically:
- No `TODO`/`FIXME`/`verify manually` in production paths.
- No `z.any()` on evidence-gate or cycle gate inputs.
- No hardcoded `assay-instance`/`challenge-instance` strings — mint uses `randomUUID()`.
- No handler-side `SELECT … FROM beliefs` citation pre-check re-implementing the SECURITY DEFINER rule (enforcement is trigger-authoritative; handlers only map exceptions to HTTP codes).
- No mock/`:memory:` substitution in the RED suite (TC-4 grep exit 1).

---

## GAPS & RISKS

1. **Policy-deviation risk (H1 above)** — if the human requires Composer-grade output for mission-engine determinism surfaces, the `grok-4.5` fallback may not meet that bar even though it passed all functional gates. The verification stack mitigates but does not eliminate this.
2. **Contract-verify drift (M1)** — TC-4 points at consumer not producer; a future refactor could silently break the contract check while the behavior remains correct.
3. **Instance-id grammar divergence (L2)** — two different dynamic shapes across `runtime.ts` and `cycle.ts`; not a bug today but a future consistency debt.
4. **Frontmatter provenance ambiguity (L3)** — harness-read frontmatter disagrees with on-disk agent file; muddies the audit trail for any future model-policy dispute.

---

## ASSUMPTIONS (Unvalidated)

- **Assumption**: The `grok-4.5` fallback model produces output equivalent in quality to `grok-composer-2.5-fast` for Mastra mission-engine TypeScript. **Risk**: unvalidated — the sprint policy explicitly named Composer for implementer work; the fallback was an environment forced-choice, not a studied equivalence. Mitigated by triple-verification but not eliminated.
- **Assumption**: The Human Testing Gate runner (`tmux:kb-run-sprint-s23-human-tests`) executed the real shipped commands. **Risk**: low — step logs show real vitest process output with real timings and real Postgres row-count assertions.

---

## CONTRADICTIONS

- **Frontmatter model**: `.tmp/gate-3/runtime-model.txt` reports `grok-composer-2.5-fast` as the `mastra-implementer` frontmatter; `~/.claude/agents/mastra-implementer/agent.md:3` reads `model: sonnet`. Resolution: the executing harness was reading a different/overlaid agent definition. Does not affect code correctness; recorded as L3.

---

## CONFIDENCE SUMMARY

- **HIGH confidence findings**: 1 (H1 — policy compliance; corroborated by `sprint-goal-state.json` AND `runtime-model.txt` AND the conductor note)
- **MEDIUM confidence findings**: 1 (M1 — also independently flagged by landed gate-5 review)
- **LOW confidence findings**: 3 (L1, L2, L3 — single-agent, informational)
- **AC verdicts**: 20/20 PASS across gate-1..gate-5 (0 FAIL, 0 PARTIAL)
- **Gate pre-check**: clean (no auto-findings)

---

## Agent Reports (Summary)

- **`mastra-reviewer` (domain)**: Verified Postgres enforcement (SECURITY DEFINER + partial unique index), fulcrum alias-only, real fleet instance IDs, pure-TS evidence gate, 5 seam citations. Confirms the landed `gate-5-seam-sufficiency-review.md` APPROVE on all 4 ACs. Flagged M1 + L1 + L2.
- **`test-quality-reviewer` (standing test-reality lens, implemented mode)**:
  - *Oracle strength*: strong — every behavioral AC asserts concrete row counts / HTTP codes / unequal UUIDs; a stub handler that skips checks would FAIL the suite (verdict inserted → count=1). Negative controls explicit in every scenario. No mutation probe run (read-only QA mandate), but static probe + live-gate evidence confirm the oracles bite.
  - *Seed/fixture reality*: real — `seed_method: public_api`, real Postgres, zero `vi.mock`/`:memory:` (TC-4 grep exit 1). Fixtures reference real table columns.
  - *Negative-path coverage*: complete — every AC carries MUST_NOT_OBSERVE assertions (no partial rows, no HTTP 200, no hardcoded IDs, no unknown-command).
  - Flagged L3 (frontmatter provenance).

---

## Recommendations by Category

1. **Gaps**: Reconcile `runtime-model.txt`'s reported frontmatter with the on-disk `mastra-implementer/agent.md` (L3) so future `runtime_model_policy` blocks are auditable without ambiguity.
2. **Risks**: Human must consciously accept H1 before land — either accept `grok-4.5` output (verification stack backs it), re-run under a Composer-capable harness, or relax the sprint policy field with documented rationale.
3. **Assumptions**: Repoint gate-5 TC-4 verify at `cycle.ts` (the mint site) or assert both mint + inspection consumption (M1).
4. **Contradictions**: Track instance-id grammar convergence between `runtime.ts` and `cycle.ts` if a single fleet-trace grammar is desired (L2).

---

## Metadata

- **Agents**: `mastra-reviewer` (Glob/Grep/Read/Bash/Task), `test-quality-reviewer` (standing seat, Glob/Grep/Read/Bash)
- **Confidence Framework**: HIGH (corroborated by ≥2 sources), MEDIUM (2 agents agree), LOW (single agent)
- **Report Generated**: 2026-07-23T02:14:06Z
- **Reviewed SHA**: `3885e9100ea6999d8702e4a79fc3289278f44cd1` (product code identical to sprint `main_tip` `193bb231`)
- **Read-only QA**: No product code modified, no branch checkout, no push, no merge. Verdict does not land work — the run stage merges after approval.
- **Next Steps**: [Human decision on H1] → [Run stage merges `193bb231` to `main` on approval] → [Optionally address M1/L2/L3 in a future polish sprint]
