# Red-Hat Review — Sprint 24 (GATE-FIX-003 / 004 / 005), Independent Post-Remediation

**Report Date (UTC):** 2026-07-24T05:41:44Z
**Sprint:** `sprint-24-full-rn-app-rewrite-off-convex-onto-zero`
**Stage:** Independent final review/qa (post-remediation), NOT implementation, NOT merge
**Reviewed By:** code-reviewer (primary, single-handed) + test-quality-reviewer lens (implemented mode)
**Test-reality lens:** RAN — implemented mode (oracle-strength, seed/fixture reality, negative-path). No subagent fan-out (independent landed-tree review per request).

## Provenance (auditable land)

| Field | Value |
|-------|-------|
| **Reviewed SHA (exact)** | `3e7200fad5278fb3ba0377492e7408d04a54d765` |
| Subject | `Merge task/s24-GATE-FIX-005-whatsnew-share into main` |
| Merge parents | `23af362d7c79acee2ff7e45205eb3eb261f426c8` (GATE-FIX-004 merge) + `c5096dfac757490ca5dea46452758d435c755136` (GATE-FIX-005 impl) |
| Remediation commits in scope | `31975479` GATE-FIX-003, `3320ae24` GATE-FIX-004, `c5096dfa` GATE-FIX-005 |
| Prior clean review (baseline) | `.spec/reviews/red-hat-sprint24-gatefix002-20260724T052517Z.md` (tip `07aefe77`, HIGH-1..5) |
| Current `main` tip at review time | `202b7609d8bb5b8d3b373b32fa8fa352d31b14c8` (1 commit ahead of reviewed SHA — see MED-1) |
| On-disk tree vs reviewed SHA | All 8 in-scope product/test/oracle files are **byte-identical** at `3e7200fa` and on-disk HEAD; only `scripts/e2e/run-sprint24-human-gate.sh` differs (the follow-on TC-7 fix). On-disk product code faithfully represents the reviewed SHA. |
| `gate-results.json` verdict | `"fail"` (frozen, honest 5/7) — **untouched by this review** |
| `sprint-goal-state.json` `human_gate_verdict` | `"fail"` — sprint **does NOT claim complete** (claim-complete auto-finding dormant, correctly) |

**Landing contract honored:** No merge, no push, no branch move, no edit to product code / task files / `gate-results.json` / `sprint-goal-state.json`. The reviewed SHA was inspected in place via `git show`/`git diff`; the working checkout was never moved. This verdict does not land work — the run stage merges after approval.

> **Do NOT mark Sprint 24 complete. Do NOT write a `gate-results.json` pass.** The separate real seven-step QA stage remains authoritative for close. Full HTG 7/7 is **not** proven by this review.

---

## Verdict

| Field | Value |
|-------|-------|
| **Overall** | **CLEAN — zero CRITICAL/HIGH residuals** |
| **CRITICAL residuals** | **0** |
| **HIGH residuals** | **0** |
| **MEDIUM residuals** | **1** (MED-1: false-RED driver TC-7 scan at the reviewed SHA; safe direction; already remediated on current main) |
| Blocker A (whats-new Zero feed + non-empty oracle) | **REAL FIX** |
| Blocker B (share actions sheet + Mastra `/article/` URL) | **REAL FIX** |
| New weakening / theatre introduced? | **No** |
| Convex regression? | **No** (step-6 oracle holds) |
| Presentation / a11y regression? | **No** (net a11y improvement) |
| Sprint complete? | **NO** — gate honestly `fail` |
| Acceptable per landing contract? | **YES** (zero CRITICAL/HIGH) — routed note only; no blocker to return |

---

## Executive Summary

GATE-FIX-003/004/005 genuinely remediate both honest 5/7 blockers from the `c4fd6920` run. Blocker A is fixed at the substrate: `resolveHolocronRoute` (`lib/holocron-deep-link.ts`) now parses the hostname form `holocron://whats-new`, `_layout.tsx` routes via `router.navigate`, and the non-empty data oracle `whats-new-feed-finding-0` is bound to a real Zero-backed `useWhatsNewFeed` hook. Blocker B is fixed at the substrate: `buildArticleShareUrl` emits a real Mastra `${host}/article/${shareToken}` and rejects Convex hosts; the actions-sheet oracle moved to an inner `View` (iOS XCUITest fix) with `accessibilityViewIsModal`. No false-green oracle, no Convex client regression, no a11y regression, no new theatre was introduced. The single residual is a **false-RED** in the reviewed SHA's driver TC-7 scan (substring match flags `articles-route-layout-back-button` as the `articles-route` oracle); it is over-strict (cannot manufacture a green), is already fixed in the follow-on commit that is the current `main` tip, and is rated MEDIUM. Acceptance bar (zero CRITICAL/HIGH) is met; sprint remains open pending the authoritative real 7/7 QA.

---

## AC / Blocker enumeration (first section)

Independent re-verification of the two honest 5/7 blockers (steps 4 & 7) plus the cross-cutting hardening claims of GATE-FIX-003/004/005. Each verdict carries `file:line` evidence from the tree at `3e7200fa`.

### Blocker A — `holocron://whats-new` mounts the real Zero-backed feed + non-empty data oracle

| Check | Verdict | Evidence |
|-------|---------|----------|
| Hostname-form deep link resolves | **PASS** | `lib/holocron-deep-link.ts:18-26` `resolveHolocronRoute` returns `whats-new` for `holocron://whats-new` (host set, path empty); `tests/lib/holocron-deep-link.test.ts` 6/6 pass (run this review). |
| `_layout.tsx` routes the resolved route + remounts on sequential steps | **PASS** | `app/_layout.tsx:62` `resolveHolocronRoute(url)`; `:87-90` `router.navigate({ pathname: '/whats-new' })` (navigate, not push — GATE-FIX-005 remount fix). |
| Feed testID is real | **PASS** | `app/(drawer)/whats-new/index.tsx:34` `<NewsfeedScreen testID="whats-new-feed" />`. |
| **Substrate is genuinely Zero-backed (not Convex)** | **PASS** | `hooks/use-whats-new-feed.ts:1` `import { useQuery as useZeroQuery } from '@rocicorp/zero/react'`; `:4` `feedItemsByOwner, latestWhatsNewReports from '@/app/zero/queries'`. Zero react hook, no `convex/react`. |
| Non-empty data oracle resolves + is NON-optional | **PASS** | `components/whats-new/NewsfeedScreen.tsx:129` `testID={\`${testID}-finding-0\`}` → `whats-new-feed-finding-0` (hero card bound to real Zero data); `.maestro/subscriptions/whats-new-loads.yml:65-71` `extendedWaitUntil`+`assertVisible` finding-0, **no `optional:true`**. |
| Empty-shell negation present + bounded (fail-closed, no hang) | **PASS** | `NewsfeedScreen.tsx:178` `${testID}-empty`; `whats-new-loads.yml:74-77` `extendedWaitUntil notVisible timeout:10000` — **fails if empty persists**, just no longer hangs (GATE-FIX-003). |
| Drawer fallback is chrome-only; does not fake data | **PASS** | `whats-new-loads.yml:25-53` fallback taps are `optional:true`; the data proof (`whats-new-feed` + `finding-0`) stays non-optional afterward. |

**Blocker A: REAL FIX.** A shell-only or Convex-backed fake would fail `whats-new-feed-finding-0`. Oracle is mutation-real.

### Blocker B — Article share opens the real actions sheet + proves a Mastra `/article/` URL

| Check | Verdict | Evidence |
|-------|---------|----------|
| Actions-sheet oracle observable on iOS | **PASS** | `components/documents/DocumentActionsSheet.tsx:106-112` testID moved from `<Modal>` to inner `<View accessibilityViewIsModal accessibilityLabel="Document actions" collapsable={false}>` (GATE-FIX-005 iOS XCUITest fix). |
| Share row testID real | **PASS** | `DocumentActionsSheet.tsx:181` `testID={\`${testID}-share\`}` → `document-actions-sheet-share`. |
| Share URL targets Mastra `/article/`, rejects Convex | **PASS** | `app/zero/platform.ts:24-34` `buildArticleShareUrl` → `\`${host}/article/${shareToken}\``; `:31-33` throws if host contains `.convex.site`/`.convex.cloud`. |
| Share handler wired to real Share | **PASS** | `app/document/[id].tsx:438-439` `buildArticleShareUrl(document.shareToken)`; `:467/:478` `Share.share({ url: shareUrl })`. |
| Seed grants a real share_token | **PASS** | `services/platform/src/db/seed-e2e.ts:217-218` first public doc gets `share_token = e2eShareToken` (uuid-shaped). |
| Real public endpoint exists | **PASS** | `services/platform/src/http/hono-app.ts:111` `app.get('/article/:shareToken', …)`; scoped-key exempted at `middleware/scoped-key.ts:100`. |
| Flow asserts `/article/` + rejects Convex host | **PASS** | `.maestro/articles/share-url-mastra.yml:58-64` `extendedWaitUntil/assertVisible text: ".*(/article/).*"`; `:67-70` `assertNotVisible .convex.site/.cloud` (fail-closed if URL not rendered). |

**Blocker B: REAL FIX.** The `/article/` text oracle times out (fails) if the share sheet does not surface the Mastra URL; it cannot pass on a Convex host.

### GATE-FIX-003 cross-cutting hardening

| Check | Verdict | Evidence |
|-------|---------|----------|
| Articles multi-card oracle non-optional | **PASS** | `.maestro/articles/list-loads.yml` `article-card-pressable` index 0 + index 1, non-optional. |
| Articles empty-state bounded (no HIGH-3 hang) | **PASS** | `list-loads.yml` `extendedWaitUntil notVisible articles-empty-state timeout:10000` (replaces unbounded `assertNotVisible`). Still fails if empty persists. |
| Articles-route oracle resolves | **PASS** | `app/articles.tsx:207` `testID="articles-route"`; `:194` `articles-route-layout`. |
| Driver wall-clock bound per Maestro flow | **PASS** | `scripts/e2e/run-sprint24-human-gate.sh` `run_maestro_flow` wraps each flow with `MAESTRO_STEP_TIMEOUT_SEC` (default 180s); on timeout kills + exits 124 + preserves honest-fail log. |

---

## Human Testing Gate pre-check (deterministic, skill [2.5])

- **EXECUTABILITY (all 7 steps):** entry points resolve — `holo seed:e2e`/`holo verify:no-convex-client` CLI present; flows exist; routes `/whats-new` (`app/(drawer)/whats-new`) and `/articles` (`app/_layout.tsx:171` Stack.Screen + `:95` navigate) exist. **No wiring gap.**
- **ORACLE PROVABILITY:** every asserted oracle resolves in source — `whats-new-feed`/`-finding-0`/`-empty`, `articles-route`/`article-card-pressable`/`articles-empty-state`, `document-actions-sheet`/`-share`, `/article/` text (via `buildArticleShareUrl`), `.convex.site`/`.cloud` negation. **No fictional oracle.**
- **EVIDENCE (claim-complete clause):** sprint does **not** claim complete (`human_gate_verdict:"fail"`). The "claim-complete without fresh pass" auto-finding is **dormant** — correct, not a gap.

---

## Test-reality lens (implemented mode)

| Audit | Result |
|-------|--------|
| Oracle strength (would a fake pass?) | **Strong.** Both blockers require real Zero data (`finding-0`) / a real Mastra URL (`/article/` text). A shell-only or Convex-host build fails closed. |
| Seed / fixture reality | **Real.** `share_token` is a real seeded column read by a real Hono endpoint; feed findings come from `feedItemsByOwner` Zero query. |
| Negative-path (bounded negations) | **Fail-closed, not weakened.** Empty-state `notVisible` checks carry a 10s timeout and still fail when the empty state persists; they remove the HIGH-3 hang without removing the assertion. |

---

## Residuals

### MED-1 — `3e7200fa` driver TC-7 scan FALSELY REJECTS `whats-new-loads.yml` (false-RED; safe direction; already fixed on main)

- **Severity:** MEDIUM (not CRITICAL/HIGH). **Direction:** false-RED (over-strict), **not** false-green. It cannot manufacture a pass.
- **Evidence:** Ran the exact `3e7200fa` TC-7 python scan against the current oracle files. It flags `.maestro/subscriptions/whats-new-loads.yml:35` (`id: "articles-route-layout-back-button"`) because the scan uses substring matching (`oid in line`) and `articles-route-layout-back-button` contains the oracle id `articles-route`, sitting one line above an `optional:true` (line 36, the back-button chrome).
- **Impact at the reviewed SHA:** `static_audit || fail "static oracle audit failed"` (driver) would exit non-zero **before** running any Maestro flow. Net effect at `3e7200fa`: the driver cannot produce *any* result (green or honest-fail) via this path. The on-disk honest 5/7 `gate-results.json` was recorded by the earlier `c4fd6920` driver (pre-GATE-FIX-005 fallback chrome), so it is not invalidated.
- **This is the direct answer to the "fail-closed driver no longer rejects oracle files for false optionality" check:** At the reviewed SHA `3e7200fa`, the driver **still** falsely rejects for a false optionality reason. It is **already remediated** in the immediate follow-on commit `202b7609` (current `main` tip), which scopes the optional check to `assertVisible` blocks and ignores `when:/notVisible` branch chrome. Because current `main` carries the fix, the authoritative real-7/7 QA stage (which runs against `main`) is unaffected.
- **Does it block this verdict?** **No.** Acceptance bar is zero CRITICAL/HIGH; a safe-direction MEDIUM already fixed on main is a routed note, not a blocker. Listed for transparency and provenance.

### Non-blocking observations (do not reopen; carry from prior reviews)

- **MED-B (carry):** Articles list oracle is a multi-card floor (index 0 + 1), not a full count-12 assertion. Acceptable for the gate; optional follow-up.
- **LOW:** `app/zero/platform.ts:57` `export const platformUrl = getMastraHost()` evaluates host at module load (deprecated alias). Cosmetic; not on the gate path.

---

## "No new weakening or theatre" audit (explicit)

| Surface | Assessment |
|---------|------------|
| Empty-state negations (articles + whats-new) | Bounded, **still fail when empty persists** — removes hang, keeps assertion. NOT weakened. |
| Positive data oracles (`article-card-pressable` x2, `whats-new-feed-finding-0`, `articles-route`) | Remain **NON-optional**. GATE-FIX-003/004/005 did not mark any data oracle optional. |
| Drawer/deep-link fallback chrome | All `optional:true`; never substitutes for the non-optional data oracle that follows. |
| Evidence theatre | None introduced. `gate-results.json` honestly `fail`; dual-lens still `not_yet_this_cycle`; no stale PASS claims revived. |
| Share seed | Real `share_token`; real Hono endpoint. Not theatre. |

---

## Lens summaries

### Gate pre-check
| Question | Answer |
|----------|--------|
| Both blockers remediated at substrate on `3e7200fa`? | **Yes** |
| Any false-green / fictional oracle? | **No** |
| Sprint claims complete? | **No** (honest `fail`) |
| Sprint close allowed by this review? | **No** |

### Anti-stub / Convex / a11y
| Surface | Assessment |
|---------|------------|
| whats-new Zero substrate | **Real** (`@rocicorp/zero/react`) |
| share Mastra URL | **Real** (`/article/`, Convex-host-rejecting) |
| Convex client regression | **None** — 0 real `convex/react` imports in `app/components/hooks/screens` (8 substring hits are comments + 1 negative test); step-6 oracle holds |
| a11y | **Net improvement** — `accessibilityViewIsModal` + `accessibilityLabel` + `collapsible={false}` added |

---

## Recommendation

| Option | Choice |
|--------|--------|
| **CLEAN (zero CRITICAL/HIGH)** | **YES** |
| **NEEDS_GATE_FIX** | **No** for CRITICAL/HIGH. MED-1 already remediated on current `main`. |
| **NEEDS_REDHAT_FIX** | **No** |
| Mark Sprint 24 complete? | **NO** |
| Write `gate-results.json` pass? | **NO** |
| Next operator action | Run the authoritative real seven-step QA (`scripts/e2e/run-sprint24-human-gate.sh` with seed + simulator + Metro + Zero + platform) against current `main` (which carries the MED-1 TC-7 fix). Only on a true, fully-evidenced 7/7 may the run stage consider `WRITE_GATE_RESULTS=1` and sprint close. This review does not perform that run and does not certify it. |

---

## JSON summary (machine)

```json
{
  "verdict": "CLEAN",
  "critical_residuals": 0,
  "high_residuals": 0,
  "medium_residuals": 1,
  "medium": [
    {
      "id": "MED-1",
      "title": "3e7200fa driver TC-7 scan falsely rejects whats-new-loads.yml (false-RED, safe direction)",
      "severity": "MEDIUM",
      "direction": "false_red_overstrict",
      "evidence": "whats-new-loads.yml:35 articles-route-layout-back-button substring-matches articles-route oracle within 5 lines of optional:true",
      "status_at_reviewed_sha": "present",
      "remediated_in": "202b7609d8bb5b8d3b373b32fa8fa352d31b14c8 (current main tip)",
      "blocks_verdict": false
    }
  ],
  "blockers": {
    "A_whats_new_zero_feed": "REAL_FIX",
    "B_share_actions_sheet_mastra_article_url": "REAL_FIX"
  },
  "new_weakening_or_theatre": false,
  "convex_regression": false,
  "a11y_regression": false,
  "sprint_complete": false,
  "gate_results_verdict": "fail",
  "acceptable_per_landing_contract": true,
  "reviewed_sha": "3e7200fad5278fb3ba0377492e7408d04a54d765",
  "main_tip_at_review": "202b7609d8bb5b8d3b373b32fa8fa352d31b14c8",
  "report_path": ".spec/reviews/red-hat-sprint24-gatefix003-005-20260724T054144Z.md",
  "summary": "GATE-FIX-003/004/005 on main 3e7200fa genuinely fix both honest 5/7 blockers (Zero-backed whats-new feed with non-empty finding-0 oracle; real Mastra /article/ share URL + iOS-observable actions sheet). Zero CRITICAL/HIGH residuals. One MEDIUM false-RED in the reviewed SHA's driver TC-7 scan (substring match on articles-route-layout-back-button) is over-strict, cannot manufacture a green, and is already fixed in 202b7609 (current main tip). No Convex regression, no a11y regression, no new weakening/theatre. Sprint remains honestly fail; the authoritative real 7/7 QA stage is unchanged and still required for close. No merge/push/branch-move performed; product code and gate artifacts untouched."
}
```

---

*Independent review of the landed primary checkout. Reviewed SHA `3e7200fad5278fb3ba0377492e7408d04a54d765` cited for audit. This verdict does not land work; the run stage merges the reviewed commit to base after approval. Sprint 24 is NOT marked complete; no gate-results pass was written.*
