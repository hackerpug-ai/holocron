# Red-Hat Review Report — Cycle 2

**Report Date**: 2026-07-25T19:50:15Z
**Target**: Sprint 25 — Reactive Surfaces: SSE Streaming, Mission Progress, Degraded (`sprint-25-reactive-surfaces-sse-streaming-mission-progress-degraded`)
**Sprint Status Reviewed**: `In Progress` (SPRINT.md:15) · status line claims "5/5 tasks completed · updated 2026-07-25T19:33:16Z" · NO `sprint-goal-state.json` (sprint is not formally claiming `goal:complete`) · NO current `gate-results.json` (only `gate-results.prev.json` from the PRE-fix run at `2026-07-25T15:59:18Z`)
**Reviewed By**: `mastra-reviewer`, `react-native-ui-reviewer`, `test-quality-reviewer` (standing seat)
**Test-reality lens**: ran (IMPLEMENTED mode) — mutation probe re-executed against HEAD `eed11919`; 1 of 2 cycle-1 H3 mutants re-killed, **1 SURVIVED** (load-bearing finding)
**Prior cycle re-audited**: `.spec/reviews/red-hat-sprint25-reactive-20260725T165851Z.md` (cycle 1 — verdict NEEDS_FIXES, H1/H2/H3 + M1-M7 + L1-L9)
**Verdict**: **NEEDS_FIXES — do not re-close.** Cycle-1 H1 and H2 are genuinely closed in production code. **Cycle-1 H3 is NOT closed** — the REDHAT-FIX-03 "mutation test" tests a reimplementation, not the production hook, and the assemblyRef-reset mutant still ships green. Combined with a stale gate and a broken TDD evidence chain, the sprint's "5/5 pass" claim is unearned.

---

## Executive Summary

The three REDHAT-FIX tasks landed real production code: the 'Streaming' conversation seed exists (`seed-e2e.ts:546`), the `research_sessions.current_iteration` writer exists and is wired into the mission engine (`research/progress.ts:55` ← `mission/cycle.ts:613` + `observability/mission-research.ts:454`), and the Maestro reconnect flow now captures numeric oracles (`chat-stream-last-seq-at-least-3`, `chat-stream-token-count-at-least-3`, `chat-assistant-bubble-count-1`) non-optionally. **H1 and H2 are closed.**

**However, H3 — the sprint's PRIMARY gate claim ("zero duplicate tokens after reconnect") — is NOT closed.** The cycle-2 mutation probe (test-quality-reviewer, isolated worktree off HEAD `eed11919`) re-ran both documented mutants against the **production** `useResumableSSEStream` hook. Mutant A (header-drop) is now killed by the new suite. **Mutant B (assemblyRef-reset) SURVIVES** — the new test (`redhat-fix-03-sse-reconnect-wiring.test.ts`) reimplements the reconnect flow in a harness function (`runReconnectWiring:189-336`) and simulates the mutant by wiping a *local* variable, not the production `assemblyRef.current`. The self-annotated `[x] PASS` on REDHAT-FIX-03 AC-2 is misleading: the "mutation log" was generated by the harness's own simulation mode, not by mutating production code.

This compounds with two further HIGH findings: (1) **no fresh gate run exists** — `gate-results.json` is missing and `GATE-RESULTS.md` still cites the pre-fix `run_id:s25-ht-20260725T155918Z` (3h34m before the REDHAT-FIX completion); (2) **the TDD evidence chain is broken** — the `.tmp/sprint-25/redhat-fix-{01,02}-path.json` and RED evidence logs mandated by every REDHAT-FIX task contract do not exist in the primary checkout, so the TC-5 verify commands fail on any cold checkout. The sprint cannot claim completion on the current evidence.

---

## HIGH Confidence Findings (3+ Agents Agree, or Mutation-Backed)

- [ ] **H3-NOT-CLOSED: The assemblyRef-reset mutant still SURVIVES the new REDHAT-FIX-03 suite — the sprint's PRIMARY gate oracle is regression-blind.** | Severity: **Critical**
      The REDHAT-FIX-03 test file (`tests/integration/redhat-fix-03-sse-reconnect-wiring.test.ts`) creates the illusion of mutation testing. Its "assembly-reset mutant" (`mode: 'assembly-reset'` at `:247`) wipes a LOCAL test-harness variable (`assembly = { lastSeq: 0, text: '', tokenCount: 0 }`), not the production `assemblyRef.current`. The test never renders or invokes the production `useResumableSSEStream` hook. **Mutation probe** (test-quality-reviewer, isolated worktree off HEAD `eed11919`): added `assemblyRef.current = { lastSeq: 0, text: "", tokenCount: 0 };` before both reconnect calls at `hooks/use-resumable-sse-stream.ts:608,712` → `pnpm vitest run tests/integration/redhat-fix-03-sse-reconnect-wiring.test.ts` → **4 passed, 1 skipped (exit 0)**; `pnpm vitest run tests/integration/s-reactive-01-resumable-sse.test.ts` → **23 passed (exit 0)**. Both mutants guarantee token duplication in production; neither is caught. (For contrast, Mutant A — commenting out `headers['Last-Event-ID'] = String(args.lastSeq)` at `:323` — IS killed by the new suite because `buildSseResumeHeaders` was extracted to a pure function the test imports directly.)
      **Agents**: test-quality-reviewer (WEAK_ORACLE_IMPLEMENTED, CRITICAL, mutation-backed — the load-bearing evidence); mastra-reviewer (SF-3 LOW "baseline anomaly" in the self-generated mutation log independently corroborates the log is unreliable: the *unmutated baseline* also reports `failures=1 ... exit=0`, meaning the mutant-kill comparison is "1 failure vs 1 failure").
      **Contradiction resolved**: react-native-ui-reviewer initially marked H3 CLOSED, citing the self-generated `redhat-fix-03-mutation.log` (`assembly-reset failures=1`) and the `[x] PASS` self-annotation. The standing seat's production-code probe overrides this — the self-generated log tested a simulated mutant (local variable wipe in the harness), not a production-code mutation. See **Agent Contradictions** below.
      **Fix**: add a test that exercises the PRODUCTION hook's reconnect path (not a reimplementation). Either (a) render `useResumableSSEStream` with `@testing-library/react-hooks` against a real `http.createServer` SSE stub and assert assembly continuity after reconnect, or (b) extract `openEventSource` into a testable unit and import it directly. The current `runReconnectWiring` harness proves the LOGIC is correct but not that the PRODUCTION CODE implements it.

- [ ] **G-2: No fresh gate run after REDHAT-FIX — `gate-results.json` MISSING; `GATE-RESULTS.md` documents the PRE-fix run.** | Severity: **Critical (process)**
      `.spec/prds/.../sprint-25.../gate-results.json` does NOT EXIST (git status: `D ... gate-results.json`). The only `gate-results*.json` is `.prev.json` dated `2026-07-25T15:59:18Z` (`run_id:s25-ht-20260725T155918Z`) — the PRE-fix run that cycle 1 flagged as NEEDS_FIXES. The three REDHAT-FIX tasks completed at `2026-07-25T19:33:16Z` — **3h34m AFTER** the only gate run. `GATE-RESULTS.md` still opens with `verdict:pass · run_id:s25-ht-20260725T155918Z` and its step-1 text ("seeds the Streaming conversation") is FALSE for the run it documents (`step-1-seed.log:36-47` shows `conversations:4`, no Streaming row). Post-fix evidence exists only as scattered partial runs (`step-1-seed-ht2.log`, `redhat-fix-03-ac3-maestro-pass.log`) — there is NO consolidated 5-step gate re-run against HEAD. The sprint's "5/5 pass" claim is inherited from the run cycle 1 rejected.
      **Agents**: mastra-reviewer (SF-1 VERY HIGH), test-quality-reviewer (G-2 HIGH), react-native-ui-reviewer (gate-evidence documentation drift MEDIUM — agreed fresh per-fix evidence exists, but no consolidated `gate-results.json`).
      **Fix**: re-run the full 5-step human gate against HEAD; produce a fresh `gate-results.json` with `written_at ≥ 19:33:16Z`. Until then, the gate claim is unverified.

- [ ] **G-3: The TDD evidence chain is broken — `.tmp/sprint-25/redhat-fix-{01,02}-path.json` + RED logs MISSING in the primary checkout.** | Severity: **High**
      All three REDHAT-FIX tasks declare `tdd_mode: red_first`, `RED_GREEN_REQUIRED: yes`, `SEEDED_EVIDENCE_REQUIRED: yes`. Their TC-5 verify commands are `test -f .tmp/sprint-25/redhat-fix-0N-path.json && jq -e '.path=="A" or .path=="B"'`. On the primary checkout: `.tmp/sprint-25/redhat-fix-01-path.json` MISSING, `.tmp/sprint-25/redhat-fix-02-path.json` MISSING (a `path.json` exists at `.tmp/REDHAT-FIX-02/path.json` with `{"path":"A"}` but at the WRONG path — generated in the worktree, never copied home). The RED evidence logs (`redhat-fix-0N-red.log`) exist only in stale `.kb-run-sprint/worktrees/REDHAT-FIX-0{1,2,3}/.tmp/sprint-25/` directories. A cold checkout cannot reproduce any PASS verdict — the TC verify commands fail. The `redhat-fix-02-research-iteration-writer.test.ts:379` assertion `expect(existsSync(PATH_JSON)).toBe(true)` FAILS on cold checkout.
      **Agents**: mastra-reviewer (SF-2 VERY HIGH), test-quality-reviewer (TDD process-artifact audit HIGH), react-native-ui-reviewer (LOW — path.json drift, content correct at alt location).
      **Fix**: commit the `path.json` artifacts at the TC-5-mandated `.tmp/sprint-25/` locations OR amend TC-5 to point at the actual locations OR have the tests create them in `beforeAll` setup (REDHAT-FIX-01's test already does this; REDHAT-FIX-02's does not).

- [ ] **H1 CLOSED: The 'Streaming' conversation seed is REAL (PATH-A landed).** | Severity: (closure)
      `services/platform/src/db/seed-e2e.ts:544-576` inserts `id=E2E_STREAMING_CONVERSATION_ID`, `title='Streaming'`, `preview='Hello from Streaming'`, plus 2 `chat_messages` (user + assistant). `seed-e2e.test.ts:85` asserts `conversations===5`. Seed log `step-1-seed-ht2.log:37,48` confirms `seeded Streaming conversation + 2 messages` + `conversations: 5`. The `optional:true` greenwash mask is removed (`reconnect-exactly-once.yml:335-341`, `token-streaming.yml:175-178` — `visible: "Streaming"` with no `optional:`). Maestro `redhat-fix-03-ac3-maestro-pass.log:90-91` shows `Assert that "Streaming" is visible ... COMPLETED` (not WARN).
      **Agents**: mastra-reviewer (VERY HIGH), react-native-ui-reviewer (HIGH — independently verified), test-quality-reviewer (HIGH — seed log + test 3/3).

- [ ] **H2 CLOSED: The `research_sessions.current_iteration` production writer is REAL (PATH-A landed).** | Severity: (closure)
      `services/platform/src/research/progress.ts:55-153` is a real Drizzle parameterized UPDATE (`SET current_iteration = ${nextIteration} ... WHERE id = ${sessionId}::uuid AND COALESCE(current_iteration,0) = ${previousIteration} RETURNING ...`). Fail-closed guards are genuine (`RESEARCH_SESSION_NOT_FOUND`, `ITERATION_BOUNDS`, `RESEARCH_SESSION_UPDATE_FAILED`). The writer is imported by three production modules: `mission/cycle.ts:13,613`, `observability/mission-research.ts:26-28,442,454`, `cli/holo.ts:3844`. No `.maestro/` imports in the progress module. Zero `.maestro/advance-server.py` calls in the production path. Cycle-1 H2 ("zero production writers") is REFUTED.
      **Agents**: mastra-reviewer (VERY HIGH — full import chain traced), react-native-ui-reviewer (PASS — out of direct scope but corroborated), test-quality-reviewer (HIGH — production writer with greppable call sites).
      **Caveat (see M-H2-LIVE below)**: the writer EXISTS, but "live as the workflow reaches iteration 3/5" is still semantically unproven — `runResearchMission`'s terminal path loops 1→5 in a single synchronous burst at completion; `runMissionCycle` advances +1 per MANUAL `holo mission:cycle` CLI trigger (no auto-scheduler).

---

## MEDIUM Confidence Findings (2 Agents Agree)

- [ ] **M-H2-LIVE: "Live as the workflow reaches iteration 3/5" overclaims — the writer exists but no engine path delivers live mid-run progress.** | Severity: High
      REDHAT-FIX-02 chose PATH-A (land the writer) and retained "as the workflow reaches iteration 3/5" in `SPRINT.md:28,44`, `GATE-RESULTS.md:9`, `ROADMAP.md:1445`. But the two call paths are: (1) `runResearchMission` terminal path (`mission-research.ts:448-461`) — when `terminalAdmitted`, loops 1→2→3→4→5 in a SINGLE synchronous call after the model returns; the UI will see `1/5 → 5/5` in one WAL replay burst; (2) `runMissionCycle` path (`cycle.ts:613`) — advances +1 per call, but only invokable via `holo mission:cycle <run-id>` CLI; no automatic scheduler, no HTTP `/api/missions/:id/cycle` endpoint. "Live progress during a single running mission" requires a human to manually invoke the CLI 3 times while watching the UI. No integration test exercises `runMissionCycle → advanceResearchSessionIteration` end-to-end.
      **Agents**: mastra-reviewer (HIGH — sole assessor, but the call-site analysis is unambiguous and SINGLE-SOURCE STRONG); test-quality-reviewer (implicitly — flagged "live as the workflow reaches" semantic unproven for S-REACTIVE-02 AC-1).
      **Fix**: either add an integration test that invokes `runMissionCycle`/`runResearchMission` end-to-end and asserts `current_iteration` advances live, OR footnote S-REACTIVE-02 AC-1 + SPRINT.md:28,44 + GATE-RESULTS.md:9 + ROADMAP.md:1445 with "writer proven; live engine-driven advancement pending automatic cycle scheduling."

- [ ] **M3-OPEN: Duplicate `chat-degraded-banner` / `chat-degraded-message` testIDs across two components.** | Severity: Medium (compounds with M6)
      `[conversationId].tsx:651,656` AND `ChatThread.tsx:336,345` BOTH render `chat-degraded-banner`/`chat-degraded-message` conditionally on `streamPhase === 'degraded'` — they coexist in the tree simultaneously when degraded. Maestro's `assertNotVisible` resolves on whichever XCTest finds first; in a render race where one banner clears and the other persists, the recovery assertion flakes. Violates the testID uniqueness rule. No REDHAT-FIX addressed this.
      **Agents**: react-native-ui-reviewer (HIGH — verified duplicates still present), test-quality-reviewer (MEDIUM — GAP-5 carried from cycle 1).

- [ ] **M5-WORSE: `pnpm typecheck` regression — 153 errors (up from ~few in cycle 1); TC-6/TC-7 text still unconditional.** | Severity: Medium (process)
      `pnpm typecheck` now exits 2 with 153 TS errors. Errors are NOT in reactive code (zero matches in `hooks/use-resumable-sse-stream.ts`, `components/chat/ChatThread.tsx`, or any `*reactive*` path) — they cluster in `services/platform/src/uploads/service.ts` (pre-existing) and `services/platform/src/tools/schemas/*.ts` (NEW since cycle 1, likely from REDHAT-FIX-02 research-sessions work — `TS5097` `.ts` extension imports). S-REACTIVE-01's TC-6/TC-7 text still says `pnpm tsc --noEmit → Exit 0` unconditionally — literal FAIL by the letter of the TC.
      **Agents**: react-native-ui-reviewer (MEDIUM — ran typecheck, counted errors, zero in reactive code); mastra-reviewer (implicitly — flagged the new tools/schemas errors as likely from REDHAT-FIX-02).

- [ ] **M6-OPEN: Degraded-recovery oracle weakened by GATE-FIX-S25-ORACLES — compounds with M3.** | Severity: Medium
      The recovery oracle was weakened to `assertNotVisible: id: chat-degraded-banner` + post-restore stream success — it proves only that the *live banner element* unmounts, not that the user-visible message clears. Combined with M3 (duplicate banner testIDs), a regression where the footer duplicate persists post-recovery would not be caught. No REDHAT-FIX addressed this.
      **Agents**: react-native-ui-reviewer (MEDIUM — S-REACTIVE-04 AC-3 PARTIAL), test-quality-reviewer (MEDIUM — NEG_OBSERVE_WEAK carried).

- [ ] **G-7 / G-8: REDHAT-FIX-02 AC-4 (fail-closed) + REDHAT-FIX-03 AC-5 (durable row) UNVERIFIED — `itLive` tests skipped without `PLATFORM_IT=1`.** | Severity: Medium (probe-blocked)
      Both ACs have well-structured `itLive` tests gated on `PLATFORM_IT=1` (requires running Postgres). Neither was executed in this review. REDHAT-FIX-02 AC-4 test additionally fails on cold checkout due to the missing `path.json` (see G-3). The fail-closed guards LOOK correct in source (`progress.ts:86-138`) but are not verified.
      **Agents**: test-quality-reviewer (PROBE_BLOCKED — both ACs). mastra-reviewer did not reach these (out of direct scope).

---

## LOW Confidence Findings (Single Agent)

- [ ] **L-M1: "Stop the local fleet" gate step 7 still has no human-executable verb.** | Severity: Medium (ergonomics; signal is real)
      Cycle-1 M1 unresolved. `run-degraded-no-hang.sh:212` does `kill -9 $FLEET_PID` + reaper. SPRINT.md:46 step 7 says "Stop the local fleet" but there is no `holo fleet stop` / `holo stack stop fleet` verb. Behavior is real; gate-step doc is non-human-executable.
      **Agent**: react-native-ui-reviewer (carried from cycle 1).

- [ ] **L-M4: Real `EventSource` bypassed; `setRequestHeader` silent-swallow risk remains (testability gap closed).** | Severity: Low
      Production transport unchanged (custom XHR + `void WhatWgEventSource` to defeat tree-shaking). BUT testability gap closed: `buildSseResumeHeaders` extracted to a pure function (`use-resumable-sse-stream.ts:312-326`) that the integration test asserts against, with an `omitLastEventId` mutant hook. Header-drop mutant is now killed by runtime capture. The production swallow risk remains but is now provably correct via mutant kill (for the pure function — NOT for the production hook wiring; see H3-NOT-CLOSED).
      **Agent**: react-native-ui-reviewer.

- [ ] **L-M7: Deterministic chat-token stream masks real model path on `holocron_nonprod`.** | Severity: Medium (documented acceptance missing)
      `chat-runs.ts:270,347`: `isHolocronNonprodDatabaseUrl(databaseUrl) && HOLO_CHAT_FLEET_ONLY!=='1'` → `emitDeterministicTokenStream` (hardcoded "One two three four five..."). The gate runs on `holocron_nonprod`, so AC-1's "real SSE socket" is real but tokens are deterministic. chat-runs.ts untouched by REDHAT-FIX. Documented acceptance still missing.
      **Agent**: mastra-reviewer (SF-3 carried).

- [ ] **L-L5: `documents.updated_at` not bumped in `update_document`.** | Severity: Low
      `mcp/executor.ts:780,783,791` does `UPDATE documents SET title = ...` without `updated_at = now()`. WAL replication still fires, but downstream `ORDER BY updated_at` or caches keyed on `updated_at` will lie. executor.ts untouched.
      **Agent**: mastra-reviewer (Gap-8 carried).

- [ ] **L-L8: `isFleetUnavailableFailure` regex-over-prose (partially improved).** | Severity: Low
      `use-resumable-sse-stream.ts:196`: `/ROLE_UNAVAILABLE/i.test(blob)` is now the FIRST check — the typed code IS the primary signal. But it's still regex over a concatenated blob (`error+message+code+status+text`), not a structured `envelope.code === 'ROLE_UNAVAILABLE'` field comparison. 7 prose fallback patterns remain (`:197-203`). Opportunistic improvement during REDHAT-FIX-03, not a targeted L8 fix.
      **Agent**: mastra-reviewer.

- [ ] **L-SF3: Mutation log baseline anomaly (independent corroboration of H3-NOT-CLOSED).** | Severity: Low (but evidentiary)
      `.tmp/sprint-25/redhat-fix-03-mutation.log:3` (when it exists): `correct failures=1 details=["reconnect Last-Event-ID expected '3', got null"] exit=0`. The unmutated baseline ALSO reports a Last-Event-ID failure but exits 0 — meaning the mutant-kill comparison is "1 failure vs 1 failure", which is not a meaningful delta. The `resumeTransport correct=sse header-drop=sse` line shows the transport doesn't change between correct and mutant, so mutant detection relies on the fragile failure-count comparison.
      **Agent**: mastra-reviewer (SF-3 LOW). Independently corroborates test-quality-reviewer's CRITICAL finding that the self-generated mutation log is unreliable.

- [ ] **L-GAP67: Cycle-1 negative-path gaps still untested.** | Severity: Medium
      No new tests for: terminal-before-airplane, stale `Last-Event-ID`, mid-message fleet recovery (during streaming, not between turns). `degraded-recovery.yml` restores the fleet between turns; the streaming-mid-message recovery path is untested.
      **Agent**: test-quality-reviewer (GAP-6/7 carried).

- [ ] **L-STEP2: `step-2-4-reconnect-ht2.log` truncated mid-Maestro.** | Severity: Low (evidence hygiene)
      The post-fix reconnect evidence log is truncated at line 76 ("Launch app … COMPLETED / Wait for animation to end …") with no full assert sequence and no exit code. Gate step-2 credit rests entirely on `redhat-fix-03-ac3-maestro-pass.log`. If that one file is lost, the gate claim collapses.
      **Agent**: react-native-ui-reviewer.

- [ ] **L-S05-STALE: S-REACTIVE-05 AC-1/AC-2 annotations stale.** | Severity: Low (doc hygiene)
      Both still read `- [ ] PARTIAL [RED-TEAM 2026-07-25]` with reasons now closed by REDHAT-FIX-01/03 (Streaming fiction → closed; inferred-not-measured → closed for SOME oracles, NOT for assemblyRef-reset per H3-NOT-CLOSED). The annotations should be refreshed to either PASS or to PARTIAL-with-only-the-M3/H3-reasons-remaining.
      **Agent**: react-native-ui-reviewer.

---

## Agent Contradictions & Debates

| Topic | Agent A | Agent B | Assessment / Resolution |
|-------|---------|---------|------------|
| **H3 closure verdict (the cycle's load-bearing question)** | react-native-ui-reviewer: H3 CLOSED — cited `.tmp/sprint-25/redhat-fix-03-mutation.log` (`assembly-reset failures=1`) and the `[x] PASS` self-annotation on REDHAT-FIX-03 AC-2 as verified | test-quality-reviewer: H3 NOT CLOSED — re-ran the mutation probe against the PRODUCTION hook (`assemblyRef.current = {lastSeq:0,...}` at lines 608,712); NEW suite **4 passed, 1 skipped — SURVIVED**; root cause: the test reimplements wiring in `runReconnectWiring:189-336` and simulates the mutant by wiping a LOCAL variable, not `assemblyRef.current` | **test-quality-reviewer WINS — decisive.** It is the standing seat, mutation-backed (the skill's designated test-reality lens), and its evidence explains the contradiction: the self-generated mutation log tested a *simulated* mutant (harness local-variable wipe), not a production-code mutation. mastra-reviewer's independent SF-3 finding ("baseline anomaly — unmutated baseline also reports failures=1, exit=0") corroborates that the self-generated log is unreliable. The react-native-ui-reviewer's error was trusting the `[x] PASS` self-annotation and the self-generated log without re-probing production code. **H3-NOT-CLOSED stands as the cycle's CRITICAL finding.** |
| **H2 "live progress" semantic** | react-native-ui-reviewer: S-REACTIVE-02 AC-1 PASS (Zero binding real) | mastra-reviewer: PASS-with-caveat — writer exists but neither call path delivers live mid-run progress (terminal path loops 1→5 at completion; cycle path needs manual CLI triggers) | **No real contradiction — different scopes.** The reactive *binding* (Zero useQuery → research_sessions) IS real and the RN reviewer correctly verified it. The *engine-driven live advancement* is the backend concern mastra-reviewer surfaced. **Net: writer EXISTS (H2 closed); live engine-driven advancement UNPROVEN (M-H2-LIVE).** |
| **Gate credit / evidence freshness** | react-native-ui-reviewer: "documentation drift not behavior drift" — fresh per-fix evidence files exist (`redhat-fix-03-ac3-maestro-pass.log` at 19:33Z); the sprint produced NEW files rather than overwriting cycle-1 logs | test-quality-reviewer + mastra-reviewer: gate is STALE; `gate-results.json` MISSING; GATE-RESULTS.md cites pre-fix run_id; no consolidated 5-step re-run against HEAD | **Both partially right.** The per-fix behavioral evidence IS fresh (19:33Z, post-REDHAT-FIX) — react-native-ui-reviewer correctly refutes any implication of fabricated evidence. BUT there is no consolidated fresh `gate-results.json`, and the documented `GATE-RESULTS.md` step text ("seeds the Streaming conversation") is FALSE for the run it cites (`step-1-seed.log` shows conversations:4). **Net: evidence exists but is not consolidated; the "gate 5/5 pass" claim cannot stand without a fresh re-run. G-2 stands.** |
| **Mutation log reliability** | react-native-ui-reviewer: trusted the log (`assembly-reset failures=1` → mutant killed) | mastra-reviewer: SF-3 "baseline anomaly" — unmutated baseline also reports failures=1, exit=0 | **mastra-reviewer's anomaly + test-quality-reviewer's production probe converge on the same truth:** the self-generated mutation log is not a reliable record of production-code mutation testing. The log format conflates "harness simulation mode" with "production mutant." |

---

## Recommendations by Category

1. **Gaps (must-close blockers)**:
   - **H3-NOT-CLOSED**: replace the `runReconnectWiring` test harness with a test that renders the PRODUCTION `useResumableSSEStream` hook against a real `http.createServer` SSE stub (via `@testing-library/react-hooks` or equivalent). Re-run the assemblyRef-reset mutant against the new test to confirm it now fails. The pure-function `buildSseResumeHeaders` test is fine — keep it — but it does not substitute for production-hook coverage.
   - **G-2**: re-run the full 5-step human gate against HEAD; produce a fresh `gate-results.json` with `written_at ≥ 19:33:16Z`. Update `GATE-RESULTS.md` to cite the new run_id.
   - **G-3**: commit `.tmp/sprint-25/redhat-fix-{01,02}-path.json` at the contract-mandated paths (or amend TC-5 / add `beforeAll` setup). Copy the RED evidence logs home from the stale worktrees.

2. **Risks (should-fix)**:
   - **M-H2-LIVE**: either add an end-to-end integration test (`runMissionCycle → advanceResearchSessionIteration` with live Postgres assertion), OR footnote S-REACTIVE-02 AC-1 + SPRINT.md:28,44 + GATE-RESULTS.md:9 + ROADMAP.md:1445 with "writer proven; live engine-driven advancement pending automatic cycle scheduling."
   - **M3 + M6**: deduplicate the `chat-degraded-banner`/`chat-degraded-message` testIDs (`[conversationId].tsx:651,656` vs `ChatThread.tsx:336,345`) AND strengthen the recovery oracle together — the two compound.
   - **M5**: fix the 153 typecheck errors OR amend TC-6/TC-7 text in S-REACTIVE-01 to scope the check to reactive-surface files only.
   - **G-7 / G-8**: run the `PLATFORM_IT=1` suites for REDHAT-FIX-02 AC-4 and REDHAT-FIX-03 AC-5 against a live database to verify fail-closed + durable-row-diff.

3. **Assumptions (challenge accepted)**:
   - The prior S-REACTIVE-05 self-review's `[x] PASS` annotations on REDHAT-FIX-03 AC-1..AC-5 are partially misleading — AC-2 ("mutants killed") self-annotated PASS but the production-code mutant survives. Restate AC-2 as "pure-function mutant killed; production-hook mutant NOT killed until test exercises the hook."
   - The sprint title's "Mission Progress" remains defensible under PATH-A (writer exists), but "as the workflow reaches iteration 3/5" overclaims until an auto-scheduler exists.

4. **Contradictions (resolved above)**:
   - H3 verdict: test-quality-reviewer wins (mutation-backed).
   - H2 "live progress": real writer, unproven live advancement.
   - Gate credit: fresh per-fix evidence exists, no consolidated fresh gate.
   - Mutation log reliability: unreliable for production-code claims.

---

## Confirmed PASS Verdicts (where the sprint earned it)

These surfaces are backend-real and survived adversarial re-verification — they should not be re-litigated in cycle 3:

- **H1 (Streaming seed)** — `seed-e2e.ts:544-576` + `step-1-seed-ht2.log` + `seed-e2e.test.ts:85` (conversations==5). Three-reviewer consensus. Real PATH-A fix.
- **H2 (research-iteration writer)** — `services/platform/src/research/progress.ts:55-153` + traced import chain across `mission/cycle.ts:613`, `observability/mission-research.ts:454`, `cli/holo.ts:3844`. Three-reviewer consensus. Real PATH-A fix. (Live-advancement semantic flagged separately as M-H2-LIVE.)
- **S-REACTIVE-03 (cross-surface p95)** — carried from cycle 1 (strongest-validated surface); `executor.ts:780` untouched by REDHAT-FIX commits. Not re-litigated.
- **SSE backend contract** — `chat-runs.ts:621` `seq > afterSeq` replay intact; `finalizeChatRun` durable row intact; `git diff --stat` empty for chat-runs.ts across all REDHAT-FIX commits.
- **Pure-function dedup logic + buildSseResumeHeaders** — Mutant A (header-drop) IS killed by the new suite because the pure function is imported directly. The extraction is a genuine improvement.
- **M2 (poll greenwash)** — `disableStatusPollFallback` flag wired (`use-resumable-sse-stream.ts:249,720`), provenance tracked (`:744-748`). Design-level fix; the production poll loop itself remains untested (see H3-NOT-CLOSED root cause).

---

## AC Verdict TABLE (cross-reviewer consolidation)

| Task | AC | Verdict | Basis |
|------|----|---------|-------|
| **S-REACTIVE-01** | AC-1 streams token-by-token | PASS | H1 closed + SSE contract intact |
| | AC-2 [PRIMARY] mid-stream reconnect, 0 dup tokens | **FAIL** | assemblyRef-reset mutant SURVIVES (H3-NOT-CLOSED) |
| | AC-3 exactly one final message matching Zero row | PASS | Maestro bubble-count-1 COMPLETED |
| | AC-4 Last-Event-ID gap-fill | PARTIAL | pure-function strong; production wiring untested |
| | AC-5 cancel finalizes | PASS | carried |
| **S-REACTIVE-02** | AC-1 [PRIMARY] progress bar advances live to 3/5 | PARTIAL | writer real (H2 closed); live semantics unproven (M-H2-LIVE) |
| | AC-2 zero_pub binding | PASS | unchanged |
| | AC-3 mobile compliance | PASS | unchanged |
| **S-REACTIVE-03** | AC-1 [PRIMARY] MCP doc update within 5s | PASS | strongest surface; non-regressed |
| | AC-2 p95 over ≥5 iterations | PASS | L1 self-attestation nuance persists (Low) |
| **S-REACTIVE-04** | AC-1 [PRIMARY] fleet-down degraded msg, no hang | PASS | M1 ergonomics caveat |
| | AC-2 inferred from failure envelope | PASS | unchanged |
| | AC-3 [PRIMARY] recovery when fleet returns | PARTIAL | M3 + M6 compound |
| **S-REACTIVE-05** | AC-1 [PRIMARY] review artifact | PARTIAL | annotations stale; H3 not closed |
| | AC-2 [PRIMARY] streaming reconnect re-verified | PARTIAL | inferred not measured (assemblyRef mutant survives) |
| **REDHAT-FIX-01** | AC-1..AC-4 | PASS | H1 closed; all four verified |
| **REDHAT-FIX-02** | AC-1 production writer PATH-A | PASS | H2 closed |
| | AC-2 Zero binding non-regression | PASS | unchanged |
| | AC-3 source audit writer | PASS | greppable production UPDATE |
| | AC-4 fail-closed | PROBE_BLOCKED | itLive skipped (PLATFORM_IT); path.json missing causes test failure |
| | TC-5 path.json exists | **FAIL** | `.tmp/sprint-25/redhat-fix-02-path.json` MISSING |
| **REDHAT-FIX-03** | AC-1 reconnect sends Last-Event-ID:3 | PARTIAL | pure-function strong; production hook not exercised |
| | AC-2 [PRIMARY] mutants killed | **FAIL** | assemblyRef-reset mutant SURVIVES production code; self-annotation misleading |
| | AC-3 Maestro numeric oracles | PASS | genuine improvement |
| | AC-4 poll cannot greenwash | PARTIAL | flag exists; production poll loop untested |
| | AC-5 durable row diff==0 | PROBE_BLOCKED | itLive skipped |
| | (path.json/mutation.log at `.tmp/sprint-25/`) | **FAIL** | MISSING on cold checkout |

**Completion Gate**: ANY FAIL or PARTIAL → `needs-revision`. S-REACTIVE-01 AC-2 (PRIMARY) FAIL + REDHAT-FIX-03 AC-2 (PRIMARY) FAIL + TC-5 failures → **needs-revision**.

---

## Agent Reports (Summary)

- **mastra-reviewer**: REQUEST CHANGES — H1 + H2 substantially resolved in production code (refutes cycle-1 SF-1/SF-2). NEW HIGH findings: SF-1 (no fresh gate run — `gate-results.json` MISSING), SF-2 (TC-5 path.json files missing). M-H2-LIVE: "live as the workflow reaches" overclaims under PATH-A (writer exists, no auto-scheduler, no live-mid-run integration test). M7/L5/L8 carried. SSE backend non-regressed (chat-runs.ts untouched).
- **react-native-ui-reviewer**: APPROVED_WITH_FOOTNOTES — H1 + H3 (in scope) verified closed via self-generated mutation log + Maestro evidence. M2 closed. M3/M5/M6 remain. **NOTE**: the H3-closed verdict is OVERRIDDEN by the standing seat's production-code probe — see Contradictions. Initial error: trusted the `[x] PASS` self-annotation and the self-generated mutation log without re-probing production code.
- **test-quality-reviewer (standing seat)**: NEEDS_FIXES — mutation probe re-executed against HEAD `eed11919`. Mutant A (header-drop) KILLED by new suite. **Mutant B (assemblyRef-reset) SURVIVES** — the new test (`redhat-fix-03-sse-reconnect-wiring.test.ts`) reimplements wiring in a harness function and simulates the mutant by wiping a local variable, not the production `assemblyRef.current`. H3 NOT closed. TDD evidence chain broken (path.json/RED logs missing). Gate stale. Probe worktree cleaned up; primary checkout clean for probed files.

---

## Metadata

- **Agents**:
  - `mastra-reviewer` — backend/contract lens (Glob, Grep, Read, Bash, Edit, Write)
  - `react-native-ui-reviewer` — RN client/reactivity/a11y lens (Glob, Grep, Read, Bash, Edit, Write)
  - `test-quality-reviewer` — standing seat, IMPLEMENTED mode, mutation probe (full tool access)
- **Driver pre-check findings merged**: G-2 (gate stale, HIGH), G-3 (TDD artifacts missing, HIGH), gate step 7 ergonomics (M1, carried). Source: `gate-pre-check`. The driver's initial "step-1-seed.log shows OLD seed" finding was confirmed by mastra-reviewer for the documented run and refuted for the post-fix `step-1-seed-ht2.log` (which DOES show Streaming) — net: the documented GATE-RESULTS.md is stale, but fresh per-fix seed evidence exists.
- **`mcp-reviewer` SKIPPED**: not in available agent roster. S-REACTIVE-03 was the strongest-validated surface in cycle 1 ("non-fakeable"); `executor.ts:780` untouched by REDHAT-FIX commits. Not re-litigated.
- **Confidence Framework**: HIGH (3+ agents, or mutation-backed, or independently verified at file:line); MEDIUM (2 agents or single strong source); LOW (single agent). Mutation-backed findings carry an extra evidentiary tier and override self-annotation trust.
- **Report Generated**: 2026-07-25T19:50:15Z
- **Duration**: ~18m (3 reviewers in parallel; ~10m driver pre-check + evidence gathering)
- **Tree state at exit**: probe worktree removed; primary checkout clean for probed files (`hooks/use-resumable-sse-stream.ts`, `tests/integration/redhat-fix-0{1,3}*.test.ts`, `tests/integration/s-reactive-01-*.test.ts`, `services/platform/tests/integration/redhat-fix-02-*.test.ts`). Pre-existing dirty files unrelated.
- **Next Steps**:
  1. **Replace the `runReconnectWiring` harness with a production-hook test** (H3-NOT-CLOSED) — render `useResumableSSEStream` against a real SSE stub; re-probe assemblyRef-reset mutant to confirm it now fails.
  2. **Re-run the full 5-step human gate** against HEAD; produce fresh `gate-results.json` (G-2).
  3. **Commit `.tmp/sprint-25/redhat-fix-{01,02}-path.json`** at TC-5-mandated paths; copy RED logs home (G-3).
  4. **Footnote or fix** S-REACTIVE-02 AC-1 "as the workflow reaches" language (M-H2-LIVE).
  5. **Run `PLATFORM_IT=1`** suites for REDHAT-FIX-02 AC-4 + REDHAT-FIX-03 AC-5 (G-7/G-8).
  6. **Deduplicate degraded-banner testIDs** + strengthen recovery oracle (M3+M6).
