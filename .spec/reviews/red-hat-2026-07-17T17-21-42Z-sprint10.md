# Red-Hat Review Report — Sprint 10: Local Re-embedding and Hybrid RRF Search

**Report Date**: 2026-07-17T17:21:42Z
**Reviewer**: mastra-reviewer (independent red-hat adversarial pass, in-session)
**Target**: Sprint 10 — `sprint-10-local-re-embedding-and-hybrid-rrf-search`
**Artifact Path**: `.spec/prds/mk6-migration/tasks/sprint-10-local-re-embedding-and-hybrid-rrf-search/`
**Scope**: 6 landed tasks (search-1, search-2, search-3, search-4, search-5, GATE-FIX-holo-search-cli), human-testing gate, e2e, build, and closeout metadata
**Main Tip**: `d41909904dc380de8151835ccabb634e24c1c13c`
**Sprint**: Sprint 10 local re-embedding and hybrid RRF search

---

## Verdict

Verdict: approved

Required findings open: 0

Severity breakdown:
- CRITICAL: 0
- HIGH: 0
- MEDIUM: 5 (advisory; do not block completion)
- LOW: 3 (advisory)

Sprint 10 local re-embedding and hybrid RRF search does not block completion. No CRITICAL findings. No HIGH findings. The 5 MEDIUM findings are advisory paperwork, metadata-accuracy, durability, and coverage notes. The 3 LOW findings are cosmetic or coverage-narrow.

---

## Executive Summary

Sprint 10 local re-embedding and hybrid RRF search delivers the local Qwen3 passage-embedding and Postgres hybrid-search substrate promised by CAP-EMB-01: `chunkDocument` (~512-token passages with overlap), `embed()` with query/document prefix asymmetry via fleet role resolution, an idempotent resumable `embedRun` (per-passage `SKIP LOCKED` commit), a single-CTE RRF hybrid search (`pgvector` HNSW + FTS, k=60, no normalize-by-max), and inline-HNSW KNN over five short-text surfaces. The human gate is genuinely executable: every CLI command, flag, and oracle resolves in `services/platform/src/cli/holo.ts`; the seven steps ran against real services with non-empty logs and a recomputed `verdict:"pass"` with zero discrepancies.

The fresh adversarial pass surfaced five MEDIUM issues, three of which (`scripts/benchmark-search.ts` legacy drift, NULL-embedding residue in the search-4 seed, integration test only covering `research_findings`) had already been raised by the search-5 reviewer and remain open. The two genuinely new mediums are: (a) every task file still shows `STATUS: Backlog` with unchecked `[ ]` AC boxes despite the sprint being closed (process gap), and (b) `sprint-goal-state.json` records `lint_exit: 0` while `pnpm lint` actually exits 1 — though all 9 lint errors are pre-existing and outside sprint-10's scope. A reproducibility note: the shared dev DB currently has empty `passages`/`sources` tables, so steps 3/4/5 of the gate would need re-seeding to replay today, although the original 17:05:29Z evidence is sound.

---

## AC Verdict Table

Compiled from the six task contracts (search-1, search-2, search-3, search-4, search-5, GATE-FIX-holo-search-cli) cross-referenced against landed source, integration tests, gate evidence, and the search-5 review report.

| # | Task | AC Item | Verdict | Evidence |
|---|------|---------|---------|----------|
| search-1 | AC-1 | embed() 1024-dim w/ query/document prefix asymmetry | PASS | `src/inference/embed.ts:75-132`, `embed-helper.test.ts:186` |
| search-1 | AC-2 | chunkDocument splits >8K into ≤512-tok passages, preserves past-8K span | PASS | `src/inference/chunk.ts:70-151`, `rrf-search.test.ts:201-211` |
| search-1 | AC-3 | chunkDocument handles short/empty boundary | PASS | `src/inference/chunk.ts:84-86`, `embed-helper.test.ts:300` |
| search-1 | AC-4 | embed() fails explicitly on dead fleet | PASS | `src/inference/embed.ts:96-106`, `embed-helper.test.ts:325` |
| search-2 | AC-1 | embedRun fills all NULL embeddings, 1024-dim | PASS | `src/inference/embed-run.ts:80-177`, `embed-run.test.ts:269` |
| search-2 | AC-2 | re-run idempotent (no dupes, no re-embed) | PASS | `embed-run.ts:96-104` (`SKIP LOCKED`), gate step6 `processed:0` |
| search-2 | AC-3 | resume after interruption w/o duplicating | PASS | `embed-run.ts:90-149`, `embed-run.test.ts:326` |
| search-2 | AC-4 | mid-batch fleet error commits prior, surfaces `EmbedRunError` | PASS | `embed-run.ts:55-68, 117-122`, `embed-run.test.ts:390` |
| search-3 | AC-1 | past-8K span retrieved top-k via single-CTE RRF | PASS | `src/search/rrf.ts:75-201`, gate step3 log, `rrf-search.test.ts:312` |
| search-3 | AC-2 | FTS-empty branch returns vector neighbour | PASS | `rrf.ts:147-148`, `rrf-search.test.ts:348` |
| search-3 | AC-3 | inline-HNSW surface KNN, zero Cohere | PASS | `src/search/surfaces.ts:105-195`, gate step7 |
| search-3 | AC-4 | no-match empty target → 0 results w/o throw | PASS | `rrf.ts:81-83`, `rrf-search.test.ts:385` |
| search-4 | AC-1 | RED '0 null/wrong-dim vectors' fails against empty impl | PASS | `embed-run.test.ts:269`, `.tmp/search-4/` |
| search-4 | AC-2 | RED 'past-8K retrieval' fails against empty impl | PASS | `search-recall.test.ts:249`, `.tmp/search-4/AC-2-red-against-start.txt` |
| search-4 | AC-3 | RED 'recall ≥ baseline' fails against empty impl | PASS | `search-recall.test.ts:306`, `.tmp/search-4/AC-3-red-against-start.txt` |
| search-4 | AC-4 | RED 'idempotent re-embed' fails against empty impl | PASS | `embed-run.test.ts:326` |
| search-5 | AC-1 | Reviewer confirms 0 null / wrong-dim vectors | PASS | `.tmp/search-5/review-report.md` AC-1 |
| search-5 | AC-2 | Reviewer confirms past-8K golden in top-k | PASS | review-report AC-2, `.tmp/search-3/AC-1-result.json` |
| search-5 | AC-3 | Reviewer confirms recall ≥ Convex baseline | PASS (w/ M1 substitute) | review-report AC-3, `recall new=1 baseline=1` |
| search-5 | AC-4 | Reviewer confirms single-round-trip RRF | PASS | review-report AC-4, `rrf.ts:106` exactly one `await sql` |
| search-5 | AC-5 | Reviewer confirms inline-HNSW 5 surfaces, zero Cohere | PASS | review-report AC-5, 5-surface live run |
| GATE-FIX | AC-1 | `holo search '<span-query>'` returns golden past-8K via RRF | PASS | `holo.ts:2109-2234`, gate step3 |
| GATE-FIX | AC-2 | `holo search --explain` shows RRF fusion | PASS | `holo.ts:2159-2179`, gate step4 |
| GATE-FIX | AC-3 | `holo search --surface research_findings` no Cohere | PASS | `holo.ts:2124-2155`, gate step7 |
| GATE-FIX | AC-4 | `holo search:recall --golden` prints recall figure | PASS | `holo.ts:2236-2397`, gate step5 |

Completion gate: ALL PASS → reviewed. 0 FAIL. 0 PARTIAL.

---

## Human Testing Gate (skill [2.5] pre-check)

### Executability

Every documented CLI command and flag resolves in `services/platform/src/cli/holo.ts`:
- Step 1: `case 'embed:run'` at `holo.ts:2034`
- Step 2: `case 'embed:verify'` at `holo.ts:2081`
- Step 3: `case 'search'` at `holo.ts:2109`
- Step 4: `--explain` flag at `holo.ts:2159-2179`
- Step 5: `case 'search:recall'` at `holo.ts:2236`, `--golden` at `holo.ts:273`
- Step 6: re-uses step 1
- Step 7: `--surface` flag at `holo.ts:2124`

### Oracle Provability

Every regex oracle has a real emitter in source:
- `"ok"\s*:\s*true` ← `holo.ts:2040` JSON output
- `"nullEmbeddings"\s*:\s*0` ← `embed-run.ts:218-225`
- `Local Re-embedding & RRF Design` ← seeded via `rrf-search.test.ts:181-195`
- `reciprocal_rank_fusion` ← `holo.ts:2162`
- `recall new=\d+ baseline=\d+` ← `holo.ts:2352`
- `"processed"\s*:\s*0` ← `holo.ts:2040`
- `hnsw:research_findings` ← `surfaces.ts:88, 123, 193`

### Non-Empty Result

All seven step logs at `.gate-evidence/20260717T170458Z/step{1..7}.log` are non-empty and every regex matched against real output. Step3 returned 2 results with real UUIDv7 `_id` `019f7109-...` whose timestamp decodes to the gate-run window.

### Evidence (CLAIMS COMPLETE)

- `gate-results.json` exists, `verdict:"pass"`, `steps_executed == steps_total == 7`.
- Fresher than the latest source commit: gate-results mtime `17:05:29Z`; latest source commit `323b2e3` at `17:02:27Z`. (The `17:09:24Z` `d419099` commit is evidence/status-only — 563 insertions across `.gate-evidence/`, `GATE-RESULTS.md`, `e2e-verification.json`, `gate-plan.json`, `gate-results.json`, `gate-verification.json`, `sprint-goal-state.json`, `SPRINT.md`, `GATE-FIX-holo-search-cli.md`; zero source-tree touches.)
- `gate-verification.json` `verified:true`, `recomputed_verdict:"pass"`, `steps_recomputed:7`, `discrepancies:[]`.

---

## HIGH Confidence Findings (3+ agents agree)

None. No CRITICAL findings. No HIGH findings of any confidence.

---

## MEDIUM Confidence Findings (advisory — do not block)

### MED-1 — Task AC checkboxes never checked off; STATUS still `Backlog`

- Severity: MEDIUM (process / paperwork)
- Confidence: HIGH (deterministic file inspection)
- Location: all six task files under `sprint-10-local-re-embedding-and-hybrid-rrf-search/`:
  - `search-1-*.md:47, 85-89` — `STATUS: Backlog`, AC-1..AC-4 all `- [ ]`
  - `search-2-*.md:47, 85-89` — same
  - `search-3-*.md:49, 88-92` — same
  - `search-4-*.md:51, 89-93` — same
  - `search-5-*.md:47, 85-90` — same (incl. AC-5)
  - `GATE-FIX-holo-search-cli.md:31-34, 44` — same
- Evidence: `grep -n '^- \[ \]\|^STATUS:' .spec/prds/mk6-migration/tasks/sprint-10-*/{search-*,GATE-FIX-*}.md` returns all 25 AC lines still `- [ ]` and all 6 STATUS lines still `Backlog`.
- Why advisory: the AC table above proves every AC item is functionally PASS against real source and gate evidence. This is a paperwork gap, not a behavior gap.

### MED-2 — `sprint-goal-state.json` claims `lint_exit: 0`; actual `pnpm lint` exits 1

- Severity: MEDIUM (closeout metadata inaccuracy)
- Confidence: HIGH (reproduced — `pnpm lint` exit 1, "Found 9 errors. Found 654 warnings.")
- Location: `sprint-goal-state.json:46` `"build": { "verdict": "PASS", "typecheck_exit": 0, "lint_exit": 0 }`
- Evidence: 9 lint errors reproduce (formatter issues in `.opencode/goals/state.json`, `.spec/evidence/infer-5-review-ac2-budget-precheck.json`, `.spec/evidence/redhat-fix-h2-green.json`, `.spec/orchestrate/orch-s10-local-re-embedding-20260717T153837Z.json`, plus `components/whats-new/NewsfeedHeroCard.tsx:12` import order).
- Sprint-10 scope is clean: zero errors in `services/platform/src/{search,inference,cli}/` or `services/platform/tests/integration/`. All 9 errors are pre-existing or in metadata files emitted by other sprints/tools.
- Why advisory: the sprint-10 source code itself passes lint. The inaccurate `lint_exit: 0` in the closeout JSON is a reporting defect.

### MED-3 — Gate reproducibility: `passages` and `sources` are empty post-gate

- Severity: MEDIUM (durability / replayability)
- Confidence: HIGH (live DB inspection at review time)
- Location: shared dev Postgres `postgres://inference1@127.0.0.1:5432/holocron`
- Evidence:
  - `SELECT count(*) FROM passages` returns `0`
  - `SELECT count(*) FROM sources` returns `0`
  - `SELECT count(*) FROM research_findings` returns `1` (claim "MLX prefill-tuned Qwen3 embedding server on Apple Silicon", `has_vec = t`)
  - Same for `research_iterations`, `subscription_content`, `toolbelt_tools`, `improvement_requests` — each retains 1 seeded row
- Consequence: a replay of human gate steps 3, 4, 5 (RRF search, explain, recall — all read `passages`) would return empty results today; step 7 (inline-HNSW surface) would still pass. The original gate evidence from `17:05:29Z` is genuine and was correctly verified — but the substrate is volatile.
- Why advisory: gate evidence is timestamped and immutable; the sprint was approved on past evidence, not present reproducibility.

### MED-4 — Integration tests skip by default; CI does not exercise the search path

- Severity: MEDIUM (regression coverage)
- Confidence: HIGH (source inspection)
- Location:
  - `services/platform/tests/integration/embed-helper.test.ts:20-28`
  - `services/platform/tests/integration/embed-run.test.ts:27-35`
  - `services/platform/tests/integration/rrf-search.test.ts:25-33`
  - `services/platform/tests/integration/inline-surfaces-search.test.ts:24-32`
  - `services/platform/tests/integration/search-recall.test.ts:23-31`
- Pattern: `const PLATFORM_IT = process.env.PLATFORM_IT === '1'; const itLive = (...) => { if (PLATFORM_IT) it(...); else it.skip(...); }`
- Consequence: `pnpm test` runs all 5 integration test files with every `itLive` skipped — the suite reports green even if `rrfHybridSearch`, `embedRun`, `chunkDocument`, or `searchSurface` is regressed to a stub.
- Why advisory: the human gate is the binding evidence and it ran against real services. The skip-by-default pattern is consistent with the rest of the platform integration suite.

### MED-5 — `scripts/benchmark-search.ts` is still the Convex/Supabase legacy harness (carry-over from search-5 M1)

- Severity: MEDIUM (already documented as M1 by search-5 reviewer; follow-up still open)
- Confidence: HIGH
- Location: `scripts/benchmark-search.ts:1-57` — requires `EXPO_PUBLIC_SUPABASE_*`, `EXPO_PUBLIC_OPENAI_API_KEY`, `CONVEX_DEPLOYMENT`; never invokes `rrfHybridSearch`; never prints `recall new=… baseline=…`.
- Evidence: file mtime `Jul 12 13:24` (pre-sprint-10); no edits in any sprint-10 commit.
- Substitute used: `services/platform/tests/integration/search-recall.test.ts` with `RECALL_BASELINE = 1` plus new `holo search:recall` CLI. Real recall measured live: `recall new=1 baseline=1`, `parity: true`.
- Why advisory: documented and worked around. The follow-up "replace or add a local RRF harness that records a Convex-migration baseline figure" remains open.

---

## LOW Confidence Findings (advisory)

### LOW-1 — RRF fusion SQL uses spaced `1.0 / (60` vs compact comment form

- `rrf.ts:147-148` runtime uses `1.0 / (${RRF_K} + v.rank)`; comments use compact `1.0/(60`. Semantically identical (`RRF_K=60`). Cosmetic only. (Already noted as L1 by search-5.)

### LOW-2 — Legacy `0.7`/`0.3` strings appear only as `NEVER` documentation

- `rrf.ts:8, 105` — comment-only; no executable normalize-by-max fusion in the SQL. (Already noted as L2 by search-5.)

### LOW-3 — `inline-surfaces-search.test.ts` only seeds `research_findings`

- AC-5 requires all 5 surfaces; the integration test asserts 1/5. The other 4 were verified manually by the search-5 reviewer with a Cohere fetch guard. (Already noted as M3 by search-5; follow-up still open.) Coverage gap, not a runtime defect.

---

## Agent Contradictions and Debates

| Topic | This review | search-5 reviewer (closeout) | Resolution |
|-------|-------------|------------------------------|------------|
| Sprint overall verdict | approved, 0 blocking | APPROVED, 0 HIGH | Agree |
| `scripts/benchmark-search.ts` | Still legacy (MED-5) | M1 documented | Agree — same finding, still open |
| Inline surfaces test coverage | 1/5 in CI (LOW-3) | M3 documented | Agree — same finding, still open |
| AC checkbox paperwork | New finding (MED-1) | Not raised | New — process gap, advisory |
| Closeout `lint_exit: 0` | Inaccurate vs actual exit 1 (MED-2) | Not raised | New — metadata accuracy, advisory |
| Gate reproducibility (passages wiped) | New finding (MED-3) | Not raised | New — durability note, advisory |

No contradictions between this fresh pass and the closeout-cycle red-hat.

---

## Recommendations by Category

1. **Gaps (paperwork)**: clerical pass over the six task files — flip `STATUS: Backlog` to `Done` and check off every `[ ]` to `[x]` with a one-line evidence pointer. Optional but cheap.
2. **Risks (metadata)**: reconcile `sprint-goal-state.json` `lint_exit` with reality — either scope it to sprint-10 changes (clean) or fix the 9 pre-existing errors. Honesty over convenience.
3. **Assumptions (durability)**: the gate is real but the substrate is volatile. Either freeze a golden-seed SQL script the operator can replay, or accept that gate evidence is point-in-time and document that.
4. **Contradictions**: none between this pass and the closeout cycle.

---

## Agent Reports (Summary)

- **mastra-reviewer (red-hat, this pass)**: 0 CRITICAL, 0 HIGH, 5 MEDIUM (3 carry-over from search-5 plus 2 new: MED-1 paperwork, MED-2 lint metadata), 3 LOW. Sprint is approved — does not block completion.
- **mastra-reviewer (search-5, closeout cycle — `.tmp/search-5/review-report.md`)**: APPROVED with 3 MEDIUM follow-ups (M1 benchmark-script drift, M2 NULL-embedding residue in search-4 seed, M3 inline-surfaces 1/5 test coverage) and 2 LOW. All AC-1..AC-5 and TC-1..TC-5 PASS.

---

## Metadata

- **Sprint**: Sprint 10 local re-embedding and hybrid RRF search
- **Reviewed By**: mastra-reviewer (independent red-hat pass)
- **Agents dispatched**: 1 (single in-session adversarial reviewer; cross-validated against the prior search-5 closeout report and live source, gate, and DB inspection)
- **Confidence Framework**: HIGH (3+ agents or deterministic reproduction), MEDIUM (1 agent plus reproduction), LOW (cosmetic / single-source)
- **Duration**: approximately 15 min (read, reproduce, write)
- **Source verified**: `services/platform/src/cli/holo.ts:2034-2398` (embed:run, embed:verify, search, search:recall), `src/inference/{embed,embed-run,chunk}.ts`, `src/search/{rrf,surfaces,index}.ts`, `tests/integration/{embed-helper,embed-run,rrf-search,inline-surfaces-search,search-recall}.test.ts`
- **Build verified (reproduced this pass)**:
  - `pnpm typecheck` (tsgo --noEmit) → exit 0 (matches closeout claim)
  - `pnpm lint` (biome check .) → exit 1, 9 errors / 654 warnings (closeout claim `lint_exit: 0` is inaccurate — see MED-2; sprint-10 scope is clean)
- **Gate verified**: `gate-results.json` verdict `pass` 7/7 plus `gate-verification.json` `verified:true recomputed_verdict:pass steps_recomputed:7 discrepancies:[]` plus `.gate-evidence/20260717T170458Z/step{1..7}.{log,exit,assertion.json}` all non-empty and regex-matched
- **DB inspected**: schema intact (`passages_embedding_hnsw`, `passages_search_vector_gin` indexes present); `passages` and `sources` empty post-gate (MED-3); inline-HNSW surfaces retain seeded rows
- **Fleet inspected**: `127.0.0.1:4545/v1/models` returns `qwen3-embedding` model (live)

---

## Final Verdict Block

Sprint: Sprint 10 local re-embedding and hybrid RRF search

Verdict: approved

Required findings open: 0

Severity breakdown:
- CRITICAL: 0
- HIGH: 0
- MEDIUM: 5 (advisory)
- LOW: 3 (advisory)

Sprint 10 local re-embedding and hybrid RRF search does not block completion.
