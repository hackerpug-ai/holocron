# Red-Hat Review Report — Sprint 28 / GATE-FIX-S28R3-QA30

**Report date:** 2026-07-30T23:19:36Z  
**Review type:** independent, adversarial, read-only  
**Implementation reviewed:** `40fedb5b0931f060b804761716062dfe57ca5357` (`fix(GATE-FIX-S28R3-QA30): canonicalize live R2 policy and propagate restore prefix`)  
**Landed merge reviewed:** `ebc05db0f7e0bc24b4050971dbe8ecda5c438902`  
**Base reviewed:** `19dc64d7b8c6f2d443d5b031b9b6be9b766c4657`

## Scope and constraints

This review inspected the exact diff from the base to the implementation, plus the landed merge. Scope was limited to the Step 2/3 `gate-plan.json` and generated `HUMAN-GATE.md` changes, the canonical R2 object-read-only policy contract, explicit `pgbackrest` prefix propagation, and `sprint28-s28r3-qa30-gate-fix.test.ts`.

No product code, task checkboxes, checkout state, commits, merges, pushes, gate verdicts, or gate evidence were written. The pre-existing working tree was dirty, so all implementation observations below were made from immutable Git objects. The focused regression test and mutations were intentionally not run because the test writes `.tmp/GATE-FIX-S28R3-QA30/*` evidence.

## Review panel

- `security-reviewer` — R2 policy and least-privilege contract
- `code-reviewer` — implementation and generated-command fidelity
- `test-quality-reviewer` — mandatory test-reality lens (implemented-artifact mode; execution/mutation blocked by the read-only brief)

## Commands and evidence consulted

- `git rev-parse ebc05db0^1 ebc05db0^2` confirmed merge parents are `19dc64d7…` and `40fedb5b…`.
- `git diff --no-ext-diff --find-renames --find-copies 19dc64d7… 40fedb5b…` and `git diff --check` established the precise five-file change and found no whitespace errors.
- `git diff --quiet 40fedb5b… ebc05db0…` returned zero: the landed merge tree is identical to the implementation tree.
- `git show <sha>:<path> | nl -ba` was used for every cited line; `git ls-tree`, `git cat-file -e`, and `git grep` tested artifact presence and consumers.
- A read-only Python comparison recomputed the documented/plan command bodies and SHA-256s. Both Step 2 (`9736e372a26bcbc0d65720f7c16287598247e366818a9e439db3493e36453d4f`) and Step 3 (`7a6a985a7145becf4471092a5ee1b208f314abf91aec57d07e33c450b9da099b`) match exactly.
- Read-only entry-point checks confirmed all referenced repository scripts exist at `40fedb5b…`; this host resolves `/bin/bash`, `/usr/local/bin/bun`, `/usr/bin/tee`, `/usr/bin/grep`, `/usr/bin/jq`, and `/opt/homebrew/bin/docker`.
- The anti-stub protocol (`brain/docs/ANTI-STUB-REVIEW.md`) was applied to the changed test and contract artifacts. No explicit or semantic production-code stub was found in this change.

## Confirmed contract observations

- Step 2's policy in [`gate-plan.json`](../prds/mk6-migration/tasks/sprint-28-point-in-time-restore-and-fresh-hardware-fire-drill/gate-plan.json) and the generated [`HUMAN-GATE.md`](../prds/mk6-migration/tasks/sprint-28-point-in-time-restore-and-fresh-hardware-fire-drill/HUMAN-GATE.md) includes `HolocronRestoreList` and `HolocronRestoreGet`, and normalizes to the helper's canonical output at `scripts/lib/r2-ro-live.sh:474-497`.
- The canonical-context guard rejects a supplied noncanonical policy at `scripts/lib/r2-ro-live.sh:737-744`; this check was not weakened.
- Step 3 now fails closed when either restore key is absent, removes `ALLOW_PLACEHOLDER_R2_RO=1`, and provides both `R2_RESTORE_OBJECT_PREFIX="pgbackrest"` and `R2_PGBACKREST_PREFIX="pgbackrest"` to both restore invocations.
- The downstream scripts genuinely canonicalize, export, and forward both prefixes: `scripts/provision-fresh-restore-target.sh:302-322,488-492` and `scripts/run-fire-drill-on-fresh-target.sh:522-531,1110-1118`.
- The new test reads the real plan and invokes the real policy helper without mocks (`services/platform/tests/integration/sprint28-s28r3-qa30-gate-fix.test.ts:36-122`).

## Findings

### HIGH — QA30's acceptance specification is absent from the immutable reviewed history

**Confidence:** MEDIUM (two independent lenses agreed)  
**Location:** `.spec/prds/mk6-migration/tasks/sprint-28-point-in-time-restore-and-fresh-hardware-fire-drill/GATE-FIX-S28R3-QA30-canonical-policy-and-prefix-propagation.md` — absent from `19dc64d7…`, `40fedb5b…`, and `ebc05db0…`

`git cat-file -e 40fedb5b…:<path>` fails and `git ls-tree -r 40fedb5b…` contains the two QA30 `.tmp` artifacts but not the task/acceptance specification. The on-disk task file was readable only as untracked working-tree context, so its acceptance criteria cannot be immutably bound to the reviewed implementation or merge. For a security-sensitive policy/gate correction, that leaves the audit unable to prove the exact intended contract from the reviewed SHAs alone.

**Required remediation:** Add the exact QA30 task specification, including its acceptance criteria and regression-test requirement, to reviewable versioned history (or incorporate the same normative requirements into a committed contract artifact), then repeat the immutable review against the resulting SHA.

### HIGH — The new regression checks command tokens, not ambient-free execution of either restore consumer

**Confidence:** LOW (test-reality lens; confirmed by source inspection)  
**Location:** `services/platform/tests/integration/sprint28-s28r3-qa30-gate-fix.test.ts:125-153`; Step 3 command at `gate-plan.json:69`

The regression asserts string presence for both explicit prefix assignments and for the missing-key/placeholder branches. It does not invoke `scripts/provision-fresh-restore-target.sh` or `scripts/run-fire-drill-on-fresh-target.sh`, and the header explicitly limits it to static contracts with no provider or Docker execution (`:2-5`). A downstream regression that stops consuming one prefix, or a no-key branch that unexpectedly succeeds due to ambient state, can therefore leave this test green.

**Required remediation:** Add a separately credential-gated disposable integration check that begins with both prefix variables absent, invokes the real provision and fire-drill paths with the explicit `pgbackrest` tuple, and observes their bound restore result. Add a genuine no-key negative control that observes nonzero plus `DEPENDENCY-S28-R2-RO`, no placeholder fallback, and no successful restore artifact.

## Test-reality audit

| Behavioral contract | Existing test mapping | Result |
|---|---|---|
| Step 2 policy is canonical and retains required SIDs/resources | `sprint28-s28r3-qa30-gate-fix.test.ts:85-123` | Static helper/plan comparison is substantive; no mock is involved. Not executed under this review constraint. |
| Step 3 has explicit prefixes and fail-closed key handling | `sprint28-s28r3-qa30-gate-fix.test.ts:125-153` | Partial: command-text mutations are caught, but consumer behavior and ambient-free negative path are unobserved (HIGH finding above). |

## Conclusion

This is **not severity-clean**: there are zero CRITICAL findings and two HIGH findings. Do not treat `ebc05db0f7e0bc24b4050971dbe8ecda5c438902` as red-hat-approved until the two remediations above are complete and independently re-reviewed. This review itself does not merge, push, or change checkout state.

[goal:complete]
