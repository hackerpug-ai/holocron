# Red-Hat Review Report

**Report Date**: 2026-07-15T20:00:07Z  
**Target**: Sprint 7 — Evidence-Graph Substrate and Ledger Immutability (`sprint-07-evidence-graph-substrate-and-ledger-immutability`)  
**Artifact Path**: `.spec/prds/mk6-migration/tasks/sprint-07-evidence-graph-substrate-and-ledger-immutability/`  
**Branch / HEAD reviewed**: `main` @ `c19444beee4cff45ecaf2f203a69861f09d8af85`  
**Reviewed By**: `mastra-reviewer`, `security-reviewer`, `code-reviewer` (+ orchestrator gate pre-check + live re-probe)  
**Product code modified**: **None** (report-only)

---

## Explicit Severity Verdict

| Field | Value |
|-------|-------|
| **SEVERITY GATE** | **FAIL** |
| **CRITICAL** | **0** |
| **HIGH (open)** | **3** |
| **MEDIUM (open)** | **6** |
| **LOW (open)** | **4** |
| **Ship-as-complete** | **No** — do not treat ledger immutability as audit-trustworthy until HIGH findings are closed |
| **Mechanism quality** | SQL substrate is **real** (not stub theatre): REVOKE for `holocron_app`, `SECURITY DEFINER revise_belief`, as-of / net-support SQL, CLI wiring, PLATFORM_IT suite |
| **Control effectiveness** | **Incomplete** — immutability binds only when the session is `holocron_app`; default runtime uses table owner; audit history is forgeable via INSERT |

**Verdict one-liner:** Core Postgres machinery is solid; the sprint **claim** of an immutable, append-only ledger as an operator/runtime property **fails** on role-binding, history authenticity, and documented human-test product path.

---

## Executive Summary

Three independent adversarial reviewers plus a live re-probe on this host converge: Sprint 07 delivered **real** DB-enforced mechanisms for belief UPDATE/DELETE denial on `holocron_app`, atomic temporal supersession via `revise_belief(...)`, bi-temporal as-of lookup, validity-windowed net-support, and register-doc no-dup corpus checks. The human gate has a fresh 7/7 `verdict: "pass"` with real CLI logs under `/tmp/holocron-gate-sprint-07-…`.

That is **not** enough for **APPROVED / complete**. The restricted role is **not** the product connection path; the table owner (default `DATABASE_URL`) retains full DML; the app role can **INSERT closed historical beliefs** and hijack mid-window as-of; and the documented HT-1→HT-2 path fails without hidden `gate-setup` belief insertion. Prior ledger-5 / closeout “0 HIGH / APPROVED” understates these control gaps.

**Severity gate: FAIL** — **0 CRITICAL, 3 HIGH open**. Remediate H1–H3 before re-claiming goal-complete for audit integrity.

---

## Gate Pre-Check (skill-emitted, deterministic)

| Check | Result | Evidence |
|-------|--------|----------|
| Human Testing Gate present | Yes | `SPRINT.md` § Human Test Deliverable / Human Testing Gate |
| Step entry points executable as documented | **PASS** | All steps use `bun services/platform/src/cli/holo.ts` (`evidence:seed`, `evidence:belief`, `db:probe --raw`, `evidence:revise`, `evidence:register-doc`); cases present in `services/platform/src/cli/holo.ts` (~1005–1155, ~571+) |
| Migrations present | **PASS** | `0003_evidence_one_open_belief.sql`, `0004_beliefs_immutability_revise.sql`, `0005_belief_asof_net_support.sql` |
| Sprint claims complete | **Yes** | `sprint-goal-state.json` → `goal.met: true`; tasks 5/5 completed; gate/e2e/build PASS |
| `gate-results.json` | **PASS (artifact)** | `verdict: "pass"`, `steps_executed == steps_total == 7`, run_id `2026-07-15T19:52:38Z`, `main_sha` = HEAD `c19444b…` |
| Freshness vs ledger-*.md | **PASS** | Gate mtime after newest task file (ledger-5 @ 13:46; gate @ 13:53) |
| `gate-verification.json` | **PASS (exit/regex)** | `verified: true`, `discrepancies: []` — does **not** assert semantic completeness of seed→belief |
| Auto-findings (wiring gap / missing gate) | **None** | Deterministic pre-check does not emit HIGH for missing entry points or absent gate |

**Note (elevated by agents, not pre-check auto-HIGH):** Gate execution is real CLI against Postgres, but depends on **non-product scaffold** (belief insert with actor `gate-setup`; pre-stamped `document_id` for HT-7). Literal SPRINT steps 1→2 without scaffold fail (see H3 / live re-probe).

---

## Live re-probe (this run)

```text
$ psql holocron — current_user = inference1
  has_table_privilege(inference1, beliefs, UPDATE|DELETE) = t|t
  has_table_privilege(holocron_app, beliefs, UPDATE|DELETE|INSERT) = f|f|t
  revise_belief: prosecdef=t, owner=holocron_owner

$ bun … holo.ts evidence:seed --json
  → ok:true, claimId=019f675d-7b30-…, 2 passages, supports+contradicts (NO belief)

$ bun … holo.ts evidence:belief --claim-id <seed-claim> --as-of now --json
  → ok:false, beliefId=null, netSupport=-1
  → errors: "no belief for claim_id=… as-of …"

$ gate step2.log
  → statement "initial gate belief from seed claim", actor "gate-setup"

$ psql holocron_app — INSERT closed history row (tx_to set) RETURNING closed=t
  → INSERT 0 1  (rolled back)  — history forgery path open
```

Gate step logs under `/tmp/holocron-gate-sprint-07-evidence-graph-substrate-and-ledger-immutability/` present for steps 1–7; step3 shows `role: holocron_app` + exit 1; step5 shows true concurrent split `concurrent_exits=1,0`.

---

## HIGH Confidence Findings (3+ Agents Agree)

Severity gate: only **CRITICAL/HIGH** block. All three panel agents (`mastra-reviewer`, `security-reviewer`, `code-reviewer`) agreed on the items below.

- [ ] **H1 — As-of audit forgery via INSERT (closed history)** | Severity: **HIGH**  
      Agents: mastra-reviewer, security-reviewer, code-reviewer  
      Evidence: `0004_beliefs_immutability_revise.sql:46` grants `INSERT` to `holocron_app`; live insert of closed row (`tx_to` set) succeeds; `belief_as_of` orders by `tx_from DESC` (`0005:21-27`) and can return forged mid-window statements while an open real belief remains. Prior closeout M2 under-rated this as non-breaking.  
      Fix: REVOKE INSERT from app and expose seed/revise only via SECURITY DEFINER; **or** BEFORE INSERT trigger forbidding non-NULL `tx_to` / unauthorized closed rows for non-definer callers.

- [ ] **H2 — Default runtime / owner pool bypasses REVOKE** | Severity: **HIGH**  
      Agents: mastra-reviewer, security-reviewer, code-reviewer  
      Evidence: `connection.ts:15-19` returns raw `DATABASE_URL`; only `probe-raw.ts:30-31` rewrites to `holocron_app`. Live: `inference1` has UPDATE+DELETE on `beliefs`. Seed/revise/as-of/register-doc connect as owner. REVOKE is real **only** for a role the product path does not use.  
      Fix: Force app/CLI pool through `toAppRoleDatabaseUrl` (or `SET ROLE holocron_app` at connect) except migrate/admin; CI-assert default pool cannot UPDATE/DELETE beliefs; expand app grants via definer entry points so operability and immutability are not mutually exclusive.

- [ ] **H3 — Documented HT-1→HT-2 product path is broken / gate-scaffolded** | Severity: **HIGH**  
      Agents: mastra-reviewer, security-reviewer, code-reviewer  
      Evidence: Live `evidence:seed` creates claim+passages+relations only (no belief); subsequent `evidence:belief --as-of now` returns `ok:false` / `no belief for claim`. Gate step2 belief uses actor `gate-setup` / statement `initial gate belief from seed claim` (`step2.log`). SPRINT human steps present a continuous operator proof that the product surface does not deliver.  
      Fix: Seed an initial open belief (or document a required `evidence:seed-belief` step); remove dependency on gate-only inserts so HT-1→HT-2 is product-true.

---

## MEDIUM Confidence Findings (2 Agents Agree)

- [ ] **M1 — GREEN “concurrent” IT is sequential stale-reject** | Severity: MEDIUM  
      Agents: mastra, security, code  
      `immutability-concurrent-reject.test.ts` awaits T1 then T2; true race lives in RED + (this run’s) gate step5. e2e mapping overstates concurrency coverage.

- [ ] **M2 — Silent catch-all SQL-fn → inline fallback** | Severity: MEDIUM  
      Agents: mastra, security, code  
      `belief-asof.ts:117-160`, `:256-276` catch **any** error, not only undefined_function — masks privilege/migration drift.

- [ ] **M3 — Idempotency race lacks unique_violation handler** | Severity: MEDIUM  
      Agents: mastra, security, code  
      Unique index prevents doubles (`0004:72-74`); concurrent same-key first insert may surface raw `23505` instead of clean existing-id return.

- [ ] **M4 — Gate HT-5 assertion / sequential fallback soft** | Severity: MEDIUM  
      Agents: mastra, code  
      Assertion only `open_count=1`; script can fall back to sequential path that also satisfies the regex.

- [ ] **M5 — NULL `claim_id` multi-open hole** | Severity: MEDIUM  
      Agents: security (+ code partial)  
      Partial unique index treats NULL claim_ids as distinct; two open NULL-claim beliefs can coexist.

- [ ] **M6 — Drizzle schema drift vs migrations** | Severity: MEDIUM  
      Agents: mastra, code  
      `schema/evidence.ts` lags uuid FK / one-open unique / idempotency indexes; future `drizzle-kit push` risk.

---

## LOW Confidence Findings (Single Agent / lower weight)

- [ ] **L1 — SPRINT promised as-of index on `tx_from` not shipped** | Agent: code-reviewer  
- [ ] **L2 — Net-support ignores relation tx history (`tx_to IS NULL` only)** | Agents: mastra, code  
- [ ] **L3 — Migration naming drift (`0003_*` plan vs shipped `0004_*` immutability)** | Agents: mastra, code  
- [ ] **L4 — RED DML suite connects as owner without `toAppRoleDatabaseUrl` (false proof path)** | Agent: security-reviewer (HIGH for verification integrity if RED is cited as REVOKE proof; treated LOW if GREEN is the ship gate)  
- [ ] **L5 — `db:probe --raw` is intentional operator SQL capability** | Agent: security-reviewer (CLI-only; acceptable under AP-7 with residual INSERT forgery → H1)  
- [ ] **L6 — Beliefs-only immutability vs full-graph “ledger” wording** | Agent: code-reviewer (scope honesty)

---

## Agent Contradictions & Debates

| Topic | Agent A | Agent B | Assessment |
|-------|---------|---------|------------|
| Prior closeout APPROVED / 0 HIGH | ledger-5 / sprint-goal redhat: PASS | All three fresh agents: FAIL with 3 HIGH | **Fresh panel wins** — live owner DML, INSERT forge, seed→belief fail reclassified residuals |
| H1 severity (INSERT history) | Prior M2 = MEDIUM “append false past only” | security: HIGH as-of mid-window hijack | **HIGH** — live forge path confirmed this run |
| HT-2 verdict | mastra: FAIL as documented | code: PARTIAL | **Treat as HIGH product-path gap** (documented continuous proof false) |
| RED DML suite role | security: HIGH H3 verification integrity | code: did not elevate | **Record as L4** unless CI relies on RED for REVOKE proof; GREEN uses correct role rewrite |
| Concurrent step authenticity this run | All agree GREEN is sequential | Gate step5 this run showed true concurrent split | Mechanism real; GREEN + assertion still weak |

---

## Recommendations by Category

1. **Gaps**  
   - Bind default app/CLI DB pool to `holocron_app` (H2).  
   - Constrain belief INSERT authenticity (H1).  
   - Make seed→belief product-true for HT-2 (H3).  
   - Link seed passages to `document_id` for register-doc without external stamp (HT-7 honesty).

2. **Risks**  
   - Sprint 14 ETL / Sprint 17 research / Sprint 23 human gate will treat as-of chains as truth — forged mid-history breaks audit-grade decisions.  
   - Owner-pool deploy configs make REVOKE a demo-only property of `db:probe --raw`.

3. **Assumptions**  
   - “App role = production connection” is currently **false**.  
   - “Gate pass = product path” is currently **false** for HT-2 (and partially HT-7).  
   - AP-7 single-user tailnet does **not** waive audit-integrity goals stated in SPRINT overview.

4. **Contradictions**  
   - Reconcile `review-findings.md` / `sprint-goal-state.json` redhat PASS with this FAIL panel before re-stamping goal-complete.  
   - Fix e2e HT-5 coverage mapping to a true concurrent test or rename sequential suite.

---

## Agent Reports (Summary)

| Agent | Key findings | Overall |
|-------|--------------|---------|
| **mastra-reviewer** | 3 HIGH (role bind, INSERT forge, HT-2 path); 5 MEDIUM; revise/as-of/register-doc real | **FAIL** |
| **security-reviewer** | 3 HIGH (as-of forge, owner bypass, RED role theatre); 4 MEDIUM; SECURITY DEFINER search_path OK | **FAIL** |
| **code-reviewer** | 3 HIGH (role bind, INSERT forge, HT scaffolding); stub greps clean; SQL core solid | **FAIL** |
| **gate-pre-check** | Entry points exist; gate artifact pass + fresh; no wiring auto-HIGH | n/a (does not clear control gaps) |
| **live re-probe** | Confirmed owner DML, seed→no belief, app INSERT closed history, revise_belief prosecdef | elevates H1–H3 to **HIGH confidence** |

---

## What is proven solid (do not rewrite)

| Component | Evidence |
|-----------|----------|
| REVOKE UPDATE/DELETE for `holocron_app` | Migration `0004:39-46`; live privileges; gate step3 42501 |
| `revise_belief` SECURITY DEFINER + FOR UPDATE + stale | `0004:78-191`; owner `holocron_owner`; live prosecdef |
| As-of SQL predicates | `0005:12-28`; gate step6; IT chain |
| Net-support validity windows | `0005:31-52`; seed→`netSupport: -1` live even without belief |
| register-doc no new passages | `register-doc.ts`; gate step7; IT |
| One-open unique index (non-NULL claim_id) | `0003:5-7` |
| No classic execute→`{ok:true}` stubs on evidence surface | Greps clean; IT uses real Postgres when `PLATFORM_IT=1` |

---

## Must-fix before honest PASS

1. **H1** — DB-enforce belief INSERT authenticity (definer-only closed/history rows, or forbid app closed inserts).  
2. **H2** — Force runtime connection to least-privilege role that cannot UPDATE/DELETE beliefs; grant operability via SECURITY DEFINER entry points; CI-assert.  
3. **H3** — Product path: seed (or explicit seed-belief) creates queryable open belief so HT-1→HT-2 works without `gate-setup`.  
4. **Soon:** narrow belief-asof catch; GREEN concurrent race; idempotency `unique_violation` handler; fix NULL claim_id multi-open; align Drizzle schema.

---

## Metadata

- **Agents**: `mastra-reviewer` (backend/Postgres/Mastra), `security-reviewer` (privilege/SECURITY DEFINER/audit), `code-reviewer` (stubs/TDD/CLI wiring)  
- **Specialist resolution**: Auto-selected from `RULES.md` Local Domain Experts (`*-reviewer`); panel focused on platform-relevant reviewers for this backend ledger sprint (same pattern as sprint-06: mastra + security + code)  
- **Confidence Framework**: HIGH (3+ agents), MEDIUM (2 agents), LOW (1 agent); live probe elevates H1–H3  
- **Report Generated**: 2026-07-15T20:00:07Z  
- **Duration**: ~6m (3 parallel agents + orchestrator probes)  
- **Report path**: `.spec/reviews/red-hat-2026-07-15T20-00-07Z-sprint07.md`  
- **Next Steps**: [Remediate H1–H3 | Re-run /review-red-hat | Do not re-APPROVE on prior ledger-5 alone]

---

## Severity gate stamp

```
RED-HAT SEVERITY VERDICT
STATUS: FAIL
CRITICAL: 0
HIGH: 3 (H1 INSERT as-of forgery; H2 owner/DATABASE_URL bypass; H3 HT-2 product path / gate scaffold)
MEDIUM: 6
LOW: 4+
APPROVED FOR COMPLETE: NO
```
