# Red-Hat Review Report

**Report Date**: 2026-07-15T16:13:16Z  
**Target**: Sprint 6 — Headless Deployment and Dev/Prod Parity (`sprint-06-headless-deployment-dev-prod-parity`)  
**Artifact Path**: `.spec/prds/mk6-migration/tasks/sprint-06-headless-deployment-dev-prod-parity/`  
**Branch / HEAD reviewed**: `main` @ `2cf0ca4` (goal-complete stamp; RH-1 fix `ad2bfc3`; gate tip referenced `602fe35` / `b6e3441`)  
**Reviewed By**: `mastra-reviewer`, `security-reviewer`, `code-reviewer` (+ orchestrator gate pre-check + live re-probe)  
**Product code modified**: **None** (report-only)

---

## Executive Summary

Sprint 6’s headless stack supervisor, launchd units, consolidated secrets tooling, Convex-env gate, CAP-EMB-01 embed probe, and RH-1 remediation land as a **real, fail-closed ops surface** — not service-layer theatre. Independent multi-agent review plus a **live re-probe on this host** confirm: `holo stack status` is honest (`scheduler: pending`, `zero_cache: disabled`), RH-1 is **closed** for scoped keys (Bearer from `secrets.yaml` → HTTP 200, no `unknown API key`; secrets **not** written into LaunchAgent env), and the human gate has a fresh 6/6 `verdict: pass` with matching logs.

**Severity gate: PASS** — **0 CRITICAL, 0 HIGH open**. Residual issues are **MEDIUM/LOW advisory** (Human Gate wording still claims “four services healthy”; FLEET_URL hardcode; doctor placeholders; install log `DATABASE_URL`; module-level `FLEET_KEY` freeze; soft dual-host parity; weak gate step-6 KeepAlive race). Under the **honest contract** (postgres+mastra managed; scheduler Sprint 11; zero-cache Sprint 20), Sprint 6 is **approved to remain complete**. Do not re-open completion solely for doc/narrative lag.

---

## Gate Pre-Check (skill-emitted, deterministic)

| Check | Result | Evidence |
|-------|--------|----------|
| Human Testing Gate present | Yes | `SPRINT.md` § Human Test Deliverable / Human Testing Gate |
| Step entry points executable as documented | **PASS** | All steps use `bun services/platform/src/cli/holo.ts` (`stack up|down|status`, `secrets doctor`, `verify-no-convex-env`); CLI cases present in `services/platform/src/cli/holo.ts` |
| Sprint claims complete | **Yes** | `sprint-goal-state.json`: `met: true`, `goal_complete_sentinel: "GATE-GOAL: ACHIEVED"` |
| `gate-results.json` | **PASS** | `verdict: "pass"`, `steps_executed == steps_total == 6`, run_id `2026-07-15T16:07:40Z` |
| Freshness vs D01-*.md | **PASS** | Gate 10:07:40 > all D01-*.md (last D01-05 @ 09:48) |
| Freshness vs RH-1 **source** | **PASS** | Fix commit `ad2bfc3` @ 10:04:10; gate @ 10:07:40 after fix |
| `gate-verification.json` | **PASS** | `verified: true`, `discrepancies: []` |

**Auto-findings from pre-check:** none (no wiring gaps; no missing/stale/failing gate for claimed-complete).

**Note (advisory, not pre-check HIGH):** Gate step **text** still says “scheduler, zero-cache healthy,” while `gate-plan.json` assertions only require postgres+mastra healthy (and product status is intentionally pending/disabled). This is documentation theatre, not an unexecutable step.

---

## Live re-probe (this run)

```text
$ bun services/platform/src/cli/holo.ts stack status
  postgres: healthy | mastra: healthy | scheduler: pending
  zero_cache: disabled | embed: healthy | mode: launchd | status: OK

$ bun … secrets doctor     → keys absent: 0, status: OK
$ bun … verify-no-convex-env → zero Convex env aliases found (clean)

$ launchctl print gui/$(id -u)/holocron-mastra  # env names
  DATABASE_URL, PORT, HOLO_ROOT, FLEET_URL
  # NO HOLO_KEY_* / MASTRA_API_KEY / FLEET_KEY in launchd env (RH-1 AC-3)

$ curl -H "Authorization: Bearer <HOLO_KEY_RN from secrets.yaml>" \
    -X POST http://127.0.0.1:4111/api/missions/x/steer
  HTTP 200  body includes "scope":"rn"  # not "unknown API key"
```

Gate step logs under `/tmp/holocron-gate-sprint-06-headless-deployment-dev-prod-parity/` present for steps 1–6; step1/3/6 show honest pending/disabled slots.

---

## RH-1 remediation verdict

| Check | Result | Evidence |
|-------|--------|----------|
| Prior HIGH: doctor-green keys invisible to launchd Mastra | **CLOSED** | `startService` → `applyConsolidatedSecretsToEnv()` **before** `createHonoApp()` — `services/platform/src/index.ts:82–99`, `config/secrets.ts:172–208`, `http/hono-app.ts:38–43` |
| Bearer HOLO_KEY_RN accepts post-stack-up | **PASS (live)** | HTTP 200 on protected route this run |
| Secrets not written into 0644 plists | **PASS** | `holocron-mastra.plist:22–36`; installed `~/Library/LaunchAgents/holocron-*.plist` grep clean |
| Regression tests | **Present** | `cli/__tests__/apply-secrets-to-env.test.ts`, `mastra-secrets-runtime.test.ts` |
| Residual incomplete apply (not original HIGH) | Advisory | `mastra.ts:5–7` freezes `FLEET_KEY` at import; `compat/cells/agent.ts:14,76–77` consumes frozen export |

**Agents agree RH-1 closed for stated scoped-key failure mode:** mastra-reviewer, security-reviewer, code-reviewer (3/3). Live probe elevates live confidence to **HIGH**.

---

## HIGH Confidence Findings (3+ Agents Agree)

Severity gate: only CRITICAL/HIGH **block**. All items below are **advisory** unless marked blocking — **none are blocking**.

- [x] ~~**RH-1** Consolidated secrets not applied to launchd Mastra~~ | Severity: ~~HIGH~~ → **CLOSED**  
      Agents: prior closeout + this panel (mastra, security, code) + live probe  
      Evidence: `index.ts:86`; live Bearer HTTP 200; plists without auth keys

- [ ] **RH-DOC-1** Human Testing Gate / deliverable still claim **four services healthy**; product correctly reports `scheduler: pending`, `zero_cache: disabled` | Severity: **MEDIUM** (doc/theatre)  
      Agents: mastra-reviewer, security-reviewer (context), code-reviewer  
      Evidence: `SPRINT.md:31–41,59–60` vs `stack/probes.ts:261–269`, `supervisor.ts:173–176`; gate step1 text vs assertion greps only postgres+mastra

- [ ] **RH-A2 / SEC-M1** `FLEET_URL` hardcoded in `holocron-mastra.plist` (env already set → secrets.yaml cannot override under launchd) | Severity: **MEDIUM**  
      Agents: mastra, security, code  
      Evidence: `deploy/launchd/holocron-mastra.plist:34–35`; `secrets.ts:194–199` (env wins)

- [ ] **RH-A3 / SEC-M2** `install-launchd.sh` echoes full `DATABASE_URL` | Severity: **MEDIUM**  
      Agents: mastra, security, code  
      Evidence: `scripts/install-launchd.sh:109`

- [ ] **RH-A4 / SEC-M3** Secrets doctor accepts placeholders (`replace-me-*`, `sk-none`) as resolved | Severity: **MEDIUM**  
      Agents: mastra, security, code  
      Evidence: `secrets.ts:101–115`; live doctor green on placeholder-style keys

- [ ] **RH-PARITY-1** Laptop/mini parity is same-CLI / portable-env, not dual-host proof; stack-up **fast path** can no-op when already healthy | Severity: **MEDIUM**  
      Agents: mastra, code  
      Evidence: `supervisor.ts:292–303`; gate step3 same Darwin surface; no `hosts.{mini,laptop}.yaml`

---

## MEDIUM Confidence Findings (2 Agents Agree)

- [ ] **RH-FLEET-1 / SEC-M4** `mastra.ts` freezes `DATABASE_URL` / `FLEET_URL` / `FLEET_KEY` at **import time** before `applyConsolidatedSecretsToEnv`; fleet agent imports frozen `FLEET_KEY` | Severity: **MEDIUM**  
      Agents: mastra-reviewer, security-reviewer (code notes LOW–MEDIUM)  
      Evidence: `mastra.ts:5–9`; `index.ts:17` top-level import; `compat/cells/agent.ts:14,76–77`  
      Note: Launchd pre-sets `DATABASE_URL`/`FLEET_URL`, so those usually match. Non-default `FLEET_KEY` in secrets.yaml may not reach agent clients. **Does not reopen RH-1 scoped-key HIGH.**

- [ ] **RH-GATE-6** Gate step 6 can pass via KeepAlive race (`already healthy` after kill) without proving force-restart path | Severity: **MEDIUM** (gate rigor)  
      Agents: mastra-reviewer + orchestrator log inspection  
      Evidence: `/tmp/.../step6.log` shows `already healthy` / elapsed ~122ms; plist `KeepAlive=true` (`holocron-mastra.plist:39–40`); code path `forceRestart` exists at `supervisor.ts:336–344`

- [ ] **RH-EVID-1** e2e-suite transcript at 09:48 predates RH-1 fix (10:04); verification JSON claims “RH-1 secrets runtime green” without listing `mastra-secrets-runtime` in that transcript; `.tmp/REDHAT-FIX-RH-1` absent | Severity: **LOW–MEDIUM** (evidence hygiene)  
      Agents: mastra, code (artifact notes) + orchestrator  
      Mitigation: live probe this run + tests on disk close the behavior gap

---

## LOW Confidence Findings (Single Agent / residual)

- [ ] **RH-A5** zero_cache “healthy” = launchd PID only when enabled later | Severity: **LOW** | Agent: prior closeout / all acknowledge  
      Evidence: `probes.ts:272–277` — OK while `Disabled=true`

- [ ] **RH-A6** Convex env names may remain outside T-PLAT-017 roots (`cli/`, `docs/`, `scripts/`) | Severity: **LOW**  
      Evidence: scan roots `verify-no-convex-env.ts:44`; gate clean on required roots

- [ ] **SEC-L1** `secrets.yaml` mode not forced to `0600` | Severity: **LOW** (AP-7 single-user OK)

- [ ] **SEC-M5** `DATABASE_URL` still substituted into LaunchAgent plists (connection string on disk) | Severity: **MEDIUM if password URL; LOW for local peer auth** | Agent: security-reviewer  
      Auth keys correctly **not** embedded (RH-1 AC-3)

- [ ] **RH-CLOSE-1** `REDHAT-CLOSEOUT.md` still documents RH-1 as **FAIL** while `sprint-goal-state.json` marks redhat **closed** | Severity: **LOW** (stale artifact)  
      Superseded by this report + fix commit `ad2bfc3`

- [ ] Launchd “loadable” unit test does not bootstrap | Severity: **LOW** | Agent: code-reviewer  
      Evidence: `launchd-and-embed-health.test.ts:147–169`

---

## Agent Contradictions & Debates

| Topic | Position A | Position B | Assessment |
|-------|------------|------------|------------|
| RH-1 closed? | Prior `REDHAT-CLOSEOUT.md`: **FAIL HIGH** | All three agents + live probe: **CLOSED** | **Resolved closed** — closeout is a pre-remediation artifact |
| goal:complete earned? | Literal Human Gate sentence (4 healthy) | Honest contract (postgres+mastra; pending/disabled) | **Earned under honest contract**; rewrite gate text before advertising “four healthy” |
| Dual-host parity | Tasks claim mini + laptop | Tests/gate = same host | **PARTIAL** — portable CLI proven; mini≠laptop not proven |
| FLEET_KEY freeze severity | security: MEDIUM | code: LOW | **MEDIUM residual**, non-blocking; scoped keys unaffected |
| Live RH-1 confidence | Agents without shell: MEDIUM | This orchestrator live probe: **HIGH** | Prefer live evidence |

---

## AC Contract Summary (consolidated)

| Task | Overall | Notes |
|------|---------|-------|
| D01-01 RED suite | **PASS** (parity AC PARTIAL soft) | Real CLI + probes; no skip theatre under PLATFORM_IT |
| D01-02 launchd | **PASS** | Four plists; KeepAlive; zero-cache Disabled; scheduler Disabled |
| D01-03 stack supervisor | **PASS** (honest; AC-4 PARTIAL) | Real up/down/status; fail-closed; scheduler pending / zero_cache disabled |
| D01-04 secrets + convex | **PASS** (strict runtime PARTIAL) | Doctor + verify + gitignore + CI; RH-1 closes runtime keys for auth |
| D01-05 CAP-EMB-01 | **PASS** | Manifest-driven real HTTP embed probe in status |
| D01-06 security review | **PASS** | `security-review-D01-06.md` APPROVED (store confidentiality) |
| REDHAT-FIX-RH-1 | **PASS** | apply-before-middleware; live Bearer; no secrets in plists |

**Anti-stub clean:** postgres (`pg_isready`), mastra (`curl /health`), embed (real HTTP), scheduler never fake-healthy, zero_cache honest disabled, stack down fail-closed, verify-no-convex real `rg`, secrets apply real file load.

---

## Recommendations by Category

1. **Gaps (advisory)**  
   - Rewrite `SPRINT.md` Human Testing Gate / deliverable and gate-plan step **text** to: postgres+mastra healthy ≤60s; scheduler **pending**; zero_cache **disabled** (or healthy only if launched).  
   - Supersede or annotate `REDHAT-CLOSEOUT.md` so it does not contradict closed RH-1.  
   - Optionally retain RH-1 curl transcript under `.tmp/REDHAT-FIX-RH-1/` for future audits.

2. **Risks (advisory)**  
   - Substitute `FLEET_URL` from consolidated secrets (or drop hardcode so apply-at-boot can fill).  
   - Redact `DATABASE_URL` in `install-launchd.sh` logs (host/db only).  
   - Lazy-read `FLEET_KEY`/`FLEET_URL` after `applyConsolidatedSecretsToEnv` (or move apply earlier before any `mastra.ts` import).  
   - Optional `secrets doctor --strict` rejecting known sentinels.

3. **Assumptions**  
   - Single-user tailnet AP-7 (no multi-tenant RLS) remains the trust model.  
   - “Laptop” and “mini” gate steps ran on the same Darwin operator surface for this completion cycle.  
   - Fleet `:4545` available when embed reports healthy.

4. **Contradictions**  
   - Prefer **code honesty** over outdated Human Gate sentences when scoring completion.  
   - Prefer **this report** over pre-fix `REDHAT-CLOSEOUT.md` for RH-1 status.

---

## Agent Reports (Summary)

| Agent | Key findings | Blocking? |
|-------|--------------|-----------|
| **mastra-reviewer** | APPROVE honest contract; RH-1 closed (code HIGH); RH-DOC-1, parity, FLEET hardcode, gate-6 KeepAlive, import freeze advisories | No |
| **security-reviewer** | PASS; RH-1 HIGH closed; auth fail-closed; MEDIUM: FLEET_URL hardcode, DATABASE_URL log, doctor placeholders, FLEET_KEY freeze, URL-in-plist | No |
| **code-reviewer** | APPROVED with MEDIUM advisories; no CRITICAL stubs; goal:complete earned for honest contract; gate wording theatre | No |
| **gate-pre-check** | Executable steps; claimed-complete with fresh 6/6 pass | No auto-findings |
| **orchestrator live probe** | Status honest; doctor/verify OK; Bearer 200; no secrets in launchctl env | Confirms RH-1 closed |

---

## Metadata

- **Agents**: `mastra-reviewer` (read-only), `security-reviewer` (read-only), `code-reviewer` (read-only); project specialists auto-selected from `RULES.md` Local Domain Experts (`*-reviewer` matched to deployment/secrets scope)  
- **Confidence Framework**: HIGH (3+ agents agree), MEDIUM (2 agents), LOW (1 agent); live probe used to upgrade RH-1 live confidence  
- **Severity gate**: CRITICAL or HIGH → block completion; MEDIUM/LOW → advisory  
- **Report Generated**: 2026-07-15T16:13:16Z  
- **Report Path**: `.spec/reviews/red-hat-2026-07-15T16-13-16Z-sprint06.md`  
- **Duration**: ~12m (context load + parallel agents + live probes + consolidation)  
- **Next Steps**: **Approve / keep Sprint 6 complete** under honest contract; track MEDIUM advisories as follow-ups (gate text rewrite recommended soonest for narrative honesty)

---

## Machine Verdict

```json
{
  "verdict": "APPROVE",
  "blocks_completion": false,
  "blocking_findings": [],
  "critical_count": 0,
  "high_count": 0,
  "medium_count": 6,
  "low_count": 6,
  "rh1_status": "CLOSED",
  "rh1_live_probe": {
    "bearer_http": 200,
    "unknown_api_key": false,
    "secrets_in_launchctl_env": false
  },
  "gate_precheck": {
    "executable": true,
    "claimed_complete": true,
    "gate_results": "pass",
    "steps_executed": 6,
    "steps_total": 6,
    "fresh": true
  },
  "honest_contract": "postgres+mastra healthy; scheduler pending; zero_cache disabled; secrets doctor; verify-no-convex-env; embed CAP-EMB-01",
  "literal_human_gate_four_healthy": "NOT_MET_AND_SHOULD_NOT_BE — docs lag product"
}
```
