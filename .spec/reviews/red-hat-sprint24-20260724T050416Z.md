# Red-Hat Post GATE/REDHAT Land — Sprint 24

**Mode:** External adversarial consolidator (react-native-ui + code-reviewer/test-quality + gate pre-check)  
**Trunk tip reviewed:** `4f2545f425e18028906791a10c0165af571256cc` (main HEAD at review)  
**Sprint:** `sprint-24-full-rn-app-rewrite-off-convex-onto-zero`  
**Reviewed at (UTC):** 2026-07-24T05:04:16Z  
**Do NOT mark sprint complete. Do NOT write gate-results pass.**

---

## Verdict table (AC-oriented)

### Sprint-level human gate (7 steps) — still FAIL (expected / honest)

| Step | Criterion | Verdict | Evidence |
|------|-----------|---------|----------|
| 1 | `holo seed:e2e --reset` 3/12/5 | **PARTIAL** (historical seed log green; not re-proven under post-land full driver this cycle) | Prior `.tmp/sprint-24-.../human-test/step1-seed.log` shows 3 conv / 12 docs / 5 feed; no `.tmp/GATE-FIX-001/step1-seed.log` from full post-land run |
| 2 | Cold boot → `chat-screen` + drawer 3 rows non-optional | **PARTIAL** | Flows hardened; rename log shows chat-screen + rows 0..2 COMPLETED **warm** path; full cold-boot log (`step2-coldboot-drawer.log`) **absent** post-land |
| 3 | Articles 12 docs via Zero | **FAIL / incomplete** | No post-land Maestro log for `list-loads.yml`; oracle only requires **one** `article-card-pressable`, not 12 |
| 4 | What's New 5 feed items via Zero | **FAIL / incomplete** | No post-land Maestro log for `whats-new-loads.yml`; oracle only asserts shell `whats-new-feed` (passes on loading skeleton / empty list) |
| 5 | Rename "Sprint Planning" within 5s via Zero | **PARTIAL** | This-cycle Maestro log COMPLETED rename path; residual titleOverrides oracle risk (see HIGH-2) |
| 6 | `holo verify:no-convex-client` | **PASS** (live re-run) | Independent re-run: 0 `convex/react` imports under app/components/hooks/screens |
| 7 | Share URL Mastra `/article/` | **FAIL / incomplete** | No post-land Maestro share log; prior step7 was **source grep theatre**; seed `is_public=true` without `share_token` |

| Artifact | Verdict | Notes |
|----------|---------|-------|
| `gate-results.json` | **fail** (correct) | `verdict: fail`, `retracted: true`, steps_passed 0; written_at_commit still 4009dd97 context |
| `sprint-goal-state.json` | **FAIL** (correct) | all_completed false; human_test fail |
| Prior false-green | **Retracted** | `gate-results.INVALID-false-green.json` preserved; SPRINT.md In Progress |
| Full 7/7 this cycle | **NOT claimed** | dual-lens `full_htg_7_of_7: not_yet_this_cycle`; title-reflect-status: NOT claimed |

### GATE-FIX-001 ACs

| AC | Description | Verdict | Evidence |
|----|-------------|---------|----------|
| AC-1 | Deterministic cold boot → chat-screen | **PARTIAL** | Metro URL parameterized; optional chrome only; chat-screen non-optional in YAML + static audit PASS. Full cold-boot Maestro log not present under post-land GATE-FIX artifacts |
| AC-2 | Drawer 3 seeded rows non-optional | **PARTIAL** | Asserts non-optional in YAML; observed green in rename warm log; not re-proven from clearState cold boot this cycle as full 7-driver |
| AC-3 | Articles 12 + What's New 5 | **FAIL** | Flows exist; **not executed post-land**; oracles under-assert counts / allow empty shell |
| AC-4 | Full rename reflects within 5s | **PARTIAL** | `.tmp/GATE-FIX-001/step5-rename-sprint-planning-retry2.log` COMPLETED; real mutator call present; titleOverrides can green Maestro without proving durable Zero write |
| AC-5 | no-convex + share Mastra | **PARTIAL** | no-convex PASS live; share Maestro **not** re-run; seed lacks share_token |
| AC-6 | Fresh honest gate-results 7/7 | **FAIL** | gate-results still fail (honest); AC-6 success state not reached — correct |

### REDHAT-FIX-UI-001 ACs

| AC | Description | Verdict | Evidence |
|----|-------------|---------|----------|
| AC-1 | Hex count 0 outside theme | **PASS** | Independent: `grep #hex … \| grep -v theme. \| wc -l` → **0** |
| AC-2 | ScreenLayout content equality | **PASS** | CONTENT=13 WITH=13; `_layout.tsx` N/A documented |
| AC-3 | testID + a11y on chrome hotspots | **PASS** (scoped) | research/improvements/whats-new detail, error-retry, drawer retry, social back stamped; residual: drawer `onRetry={() => {}}` no-op |
| AC-4 | Banned numeric literals hotspots | **PASS** | static-oracle / review artifact + design of AC-4 verify |
| AC-5 | Fresh review-artifact theme+a11y+contract | **PASS** (presentation) | `review-artifact.json` overall_verdict PASS @ 151524fc; contract notes 105/105; no-convex PASS |

### SKIP_SEED never pass

| Check | Verdict | Evidence |
|-------|---------|----------|
| SKIP_SEED records skipped not pass | **PASS** | `scripts/e2e/run-sprint24-human-gate.sh` L25–38, L417–428, L634–641: skipped/blocked cannot yield `steps_passed==7` |
| Missing drizzle / all seed roots fail → fail not pass | **PASS** | L523–530 fail path; never pass |

### titleOverrides honesty

| Check | Verdict | Evidence |
|-------|---------|----------|
| Mutator actually invoked | **PASS (not UI-only-only)** | `app/(drawer)/_layout.tsx` L144–151 `zero.mutate.conversations.update({ title, title_set_by_user, … })` after override |
| Oracle proves Zero durability | **FAIL residual** | Override applied **before** mutate; kept on error (L157–158); Maestro text oracle within 5s can pass on local state alone |

### Prior false-green retracted

| Check | Verdict |
|-------|---------|
| Sprint status not complete | **PASS** — SPRINT.md In Progress; goal-state fail |
| gate-results not dishonest pass | **PASS** — verdict fail + retracted true |
| Residual false-green files elsewhere | **PARTIAL** — `.tmp/sprint-24-.../e2e-verification.json` still overall PASS @ 4009dd97 (stale tmp theatre) |

---

## HIGH confidence findings

### HIGH-1 — Full human gate 7/7 still not executed post-land (blocks sprint complete)

**Confidence:** HIGH  
**Lens:** gate pre-check  

GATE-FIX-001 code landed (Metro parameterization, fail-closed oracles, human-gate driver, rename path fixes) and dual-lens explicitly records:

- `.tmp/GATE-FIX-001/dual-lens-review-913926b6.json` → `"full_htg_7_of_7": "not_yet_this_cycle"`
- `.tmp/GATE-FIX-001/title-reflect-status.txt` → `full_7_of_7_gate: NOT claimed`

Post-land artifact inventory under `.tmp/GATE-FIX-001/` contains **only** rename evidence (`step5-rename-sprint-planning-retry2.log`), static oracle audit, and dual-lens stubs — **no** `step2-coldboot-drawer.log`, `step3-articles.log`, `step4-whats-new.log`, `step7-share-url.log`, or fresh seed log from the driver.

`gate-results.json` correctly remains **fail**. Sprint **must not** complete.

### HIGH-2 — titleOverrides can satisfy Maestro without proving Zero persist

**Confidence:** HIGH  
**Lens:** anti-stub / test reality  

```ts
// app/(drawer)/_layout.tsx
setTitleOverrides(... until: Date.now() + 5000);  // before mutate
await zero.mutate.conversations.update({ id, title: trimmed, ... });
// on catch: keep override so user still sees intended title
```

Maestro `.maestro/chat/rename-reflects.yml` only asserts visible text `"Sprint Planning"` within 5s. That window is exactly the override TTL. Negative control "stub — rename is local-only no-op" is **not** closed by the current oracle.

**Required remediation (GATE-FIX residual):** prove durability — e.g. kill/relaunch or wait past override expiry and re-assert title from Zero query; or fail rename UI if mutator throws (clear override + error).

### HIGH-3 — Articles / What's New / Share this-cycle incomplete + weak oracles

**Confidence:** HIGH  
**Lens:** gate pre-check + fakeability  

1. **Not run post-land** under full driver.
2. **What's New oracle** (`whats-new-loads.yml`) only asserts `id: whats-new-feed`.  
   `NewsfeedScreen` stamps `testID={testID}` on **loading skeleton** and on **FlatList even when empty** (`whats-new-feed-empty` is separate). Shell-visible ≠ 5 seeded items.
3. **Articles oracle** requires ≥1 `article-card-pressable`, not 12.
4. **Share:** seed inserts `is_public=true` **without** `share_token` (`seed-e2e.ts` L215–216). Runtime must `publishDocument` to mint token. Prior HTG step7 log is source-grep only (`buildArticleShareUrl` text), not Maestro share sheet. Residual HTG step 7 risk is material.

### HIGH-4 — GATE-FIX dual-lens / verify-manifest review theatre

**Confidence:** HIGH  
**Lens:** test-quality / false-green  

- `product-verdict.json` / `technical-verdict.json` mark only **AC-1..AC-3** (and TC-1..3) satisfied — GATE-FIX has **AC-1..AC-6** PRIMARY e2e ACs.
- `verify-manifest.json` points AC verifies at **Sprint 23** mission-cycle logs (`sprint-23-deterministic-human-gate-steering-and-fulcrum-seams`) — **wrong sprint, wrong evidence**.
- Lenses still `"verdict":"APPROVED"` while `full_htg_7_of_7: not_yet_this_cycle`.

This is dual-lens **false-green process** even though product gate-results honesty was preserved at the sprint artifact layer. Do not treat dual-lens APPROVED as GATE-FIX done.

### HIGH-5 — Stale e2e-verification.json still claims PASS @ false-green commit

**Confidence:** HIGH  
**Lens:** false-green residual  

`.tmp/sprint-24-full-rn-app-rewrite-off-convex-onto-zero/e2e-verification.json` still has `"overall":"PASS"` and `written_at_commit: 4009dd97…` with all 7 steps PASS. Authoritative `gate-results.json` was retracted, but this sibling artifact can mislead automation that globs `e2e-verification.json`.

---

## MEDIUM confidence findings

### MED-1 — Drawer loading/error/retry still semantic stubs

**Confidence:** MEDIUM  
**File:** `app/(drawer)/_layout.tsx`  

- `isLoading={false}`, `error = null`, `isRenaming = false`, `onRetry={() => {}}`  
- Retry button has a11y labels (REDHAT good) but **retry is a no-op**.  
Not a presentation-only issue if users hit load failure; residual CAP-SYNC honesty gap on drawer chrome.

### MED-2 — Share path depends on client mutator optimistic-only posture

**Confidence:** MEDIUM  
**Files:** `app/zero/mutators.ts`, `app/document/[id].tsx`, seed  

`mutators.ts` comment: server `/mutate` registration "expected once … wired; client runs optimistically". Share Maestro can pass if ActionSheet shows client-built URL even when server never got a durable `share_token`. CAP-PUB-01 HTG needs real share sheet + non-Convex host against live platform env (`EXPO_PUBLIC_PLATFORM_*`).

### MED-3 — REDHAT task status still Backlog despite land + PASS artifact

**Confidence:** MEDIUM  
**Process:** REDHAT-FIX-UI-001 + GATE-FIX-001 task frontmatter still `Status: Backlog` while code is on main and review-artifact claims PASS. Status parity drift (goal-state already notes parity false).

### MED-4 — Count oracles intentionally thin vs seed contract language

**Confidence:** MEDIUM  

Human-test deliverable text says "12 documents" / "5 feed items"; Maestro only proves route shell (+ ≥1 card for articles). Even after full HTG green, **seed-count** claim remains weakly oracled unless finding-0..finding-4 / card count asserts land.

### MED-5 — Historical human-test step2-5 log still shows optional oracles

**Confidence:** MEDIUM  

`.tmp/sprint-24-.../human-test/step2-5-ui.log` (pre-hardening) shows optional conversation-row / articles-route / whats-new-feed. Current YAML is fixed (static audit PASS) — good — but that log must never be re-cited as green evidence.

---

## LOW confidence findings

### LOW-1 — `whats_new_reports` seed findings_json empty

**Confidence:** LOW–MEDIUM  

Report row seeds counts but `findings_json` is `{seed:true…}` not findings array. Feed falls back via `feedItemsByOwner` — intentional and good — but depends on fallback staying wired.

### LOW-2 — brandColors hex hosted under theme may game hex-grep

**Confidence:** LOW  

AC-1 grep excludes `theme.` lines; brand palettes live in `lib/theme.ts`. Intentional and matches AC. Residual risk only if components reintroduce hex outside theme.

### LOW-3 — ConversationActionMenu uncontrolled input is Maestro-shaped

**Confidence:** LOW  

Uncontrolled `defaultValue` + remount key is a real RN/Maestro fix, not a stub. Acceptable; ensure unit tests cover save flush path.

---

## Lens summaries

### 1) react-native-ui-reviewer (theme / a11y / ScreenLayout / Maestro)

| Gate | Result |
|------|--------|
| Theme hex | **PASS** (0) |
| ScreenLayout content | **PASS** (13/13) |
| a11y hotspots (REDHAT scope) | **PASS** |
| Maestro data oracles non-optional | **PASS** (static audit) |
| Maestro full HTG executed | **FAIL / incomplete** |
| Count-strong oracles for 12/5 | **FAIL residual** |

**Lens verdict:** Presentation remediation holds. HTG still open.

### 2) code-reviewer / test-quality (fakeability / false-green)

| Gate | Result |
|------|--------|
| Prior gate-results false-green | **Retracted** (good) |
| SKIP_SEED never pass | **PASS** |
| titleOverrides + rename oracle | **Residual HIGH** |
| Dual-lens APPROVED completeness | **Theatre / HIGH** |
| e2e-verification.json stale PASS | **HIGH residual** |
| Rewrite surfaces Zero wiring (chat/articles/feed) | **Real useQuery/mutate paths present** — not wholesale stubs |
| Drawer retry / loading | **Semantic stubs MED** |

**Lens verdict:** Not clean. GATE residual + process theatre.

### 3) Gate pre-check

| Question | Answer |
|----------|--------|
| Are HTG steps executable? | **Yes** — driver `scripts/e2e/run-sprint24-human-gate.sh` + parameterized Maestro flows exist |
| Are oracles real / fail-closed? | **Mostly** for chat-screen, rows 0..2, rename text; **weak** for articles count, whats-new items, share durability |
| gate-results still fail? | **Yes** — do not demand pass; sprint not complete |

---

## What improved since false-green retraction (do not over-credit)

1. **Honest fail** preserved at `gate-results.json` + sprint goal state.  
2. **REDHAT-FIX-UI-001** greps independently green; fresh review-artifact PASS for theme/a11y/contract presentation.  
3. **GATE-FIX-001** landed real fixes: parameterized Dev Client openLink, non-optional data oracles, fail-closed human-gate driver, rename dialog Maestro-safe input, drawer rename mutator + evidence that rename Maestro can COMPLETE.  
4. **SKIP_SEED** cannot invent pass.  
5. **no-convex-client** still clean on live re-run.

---

## Recommendation

| Option | Choice |
|--------|--------|
| **clean** | **NO** |
| **needs REDHAT-FIX** | **No new REDHAT-FIX required for theme/a11y presentation** (REDHAT ACs hold under independent greps). Optional small follow-up for drawer `onRetry` no-op / residual a11y if treated as contract. |
| **needs GATE-FIX** | **YES — residual GATE work still open** (extend GATE-FIX-001 or GATE-FIX-002): run full 7/7 driver; write honest gate-results only on real pass; harden oracles (whats-new findings 0..4, articles count/path, rename durability past override, share Maestro with token); scrub dual-lens theatre + stale e2e-verification.json |

**Overall consolidator recommendation:** **`needs GATE-FIX`**  
**Sprint complete?** **NO**  
**Write gate-results pass?** **NO**

---

## Suggested fix order (for implementers)

1. Run `bash scripts/e2e/run-sprint24-human-gate.sh` with real simulator + Metro + seed (no SKIP_SEED/SKIP_UI); capture all step logs under `.tmp/GATE-FIX-001/`.  
2. Harden oracles: `whats-new-feed-finding-0` (or assertNotVisible empty), articles multi-card or count header, rename re-assert after override TTL / relaunch.  
3. Seed or publish path: ensure share_token available for step 7; Maestro must show `/article/` not source-grep.  
4. Fix dual-lens verify-manifest to GATE-FIX AC-1..6 (kill sprint-23 pointers); refuse APPROVED until 7/7.  
5. Invalidate/delete stale `.tmp/.../e2e-verification.json` PASS @ 4009dd97.  
6. Only then WRITE_GATE_RESULTS=1 with honest pass.

---

## JSON summary (machine)

```json
{
  "verdict_clean": false,
  "high_count": 5,
  "report_path": ".spec/prds/mk6-migration/tasks/sprint-24-full-rn-app-rewrite-off-convex-onto-zero/.tmp/red-hat-post-gate-land.md",
  "recommendation": "needs GATE-FIX",
  "sprint_complete": false,
  "gate_results_must_remain": "fail",
  "top_findings": [
    "Full 7/7 HTG not executed post-land; dual-lens full_htg_7_of_7=not_yet_this_cycle",
    "titleOverrides can green rename Maestro without durable Zero proof",
    "Articles/Whats New/Share incomplete; weak shell oracles; seed lacks share_token",
    "GATE-FIX dual-lens/verify-manifest points at Sprint 23 ACs — review theatre",
    "Stale e2e-verification.json still overall PASS at false-green commit 4009dd97"
  ]
}
```
