# Red-Hat Review Report — Sprint 20

**Report Date:** 2026-07-19T20:45:00Z  
**Target:** Sprint 20 — E2E Maestro Harness and Cold-Boot Reference Flow  
**Path:** `.spec/prds/mk6-migration/tasks/sprint-20-e2e-maestro-harness-and-cold-boot-reference-flow`  
**Reviewed ref:** current `main` at `e264c2c54be876ce077a6437b3fff4383af6b74e`  
**Freshness:** This is a new report. `red-hat-20260717T213125Z.md` was not used as evidence.  
**Scope:** all 10 task contracts, all AC/TC commands, `SPRINT.md` Human Testing Gate, current landed source, current local Maestro artifacts, and trunk state. No product or task files were edited.

## Executive Summary

**Verdict: NEEDS-REVISION / gate not proven.** The current main branch contains useful fail-closed harness and Zero-provider code, and the local `junit.xml` is a real-looking one-test success. However, the claimed completed sprint is not contract-complete: the required capstone verifier, D03-06 review artifact, fork-safety test, durable Zero integration test, and testID audit test are absent; no CI-dispatched run is evidenced; and the only current video evidence is a `reference-flow.mov.sb-*` sidecar while `video.log` records `Host recording is already in progress`, so the required `reference-flow.mov` artifact does not exist.

The completion metadata is therefore a proxy pass. `sprint-goal-state.json` claims all six layers green, but it points at `main_sha: b084dd5` rather than the current `e264c2c`, claims CI-artifact evidence without a CI run, and the repository has no Sprint-20 `gate-results.json`.

## Completion-audit checklist

| Explicit requirement | Evidence checked | Result |
|---|---|---|
| Fresh independent review | New report path above; old 2026-07-17 report excluded | PASS |
| All 10 contracts | 10 task markdown files enumerated and AC/TC commands extracted | PASS (reviewed) |
| Human Testing Gate | `SPRINT.md:37-50` and six documented steps | FAIL: steps 4/3 are not proven by CI/valid video; steps 2/3 lack independent captured query evidence |
| Real Maestro evidence | `.tmp/maestro-reference-flow-run/junit.xml`, `final.png`, reset log, install logs, video log | PARTIAL: JUnit/screenshot exist; video failed and exact required video is absent |
| Main trunk | `git log`, `git rev-parse`, branch ancestry | PASS: review is against current `main`; metadata is stale |
| No product edits | only this report written by this review | PASS |

## HIGH Confidence Findings

### H1 — Sprint claims completion without the required capstone/verifier artifacts
**Severity:** Critical  
**Confidence:** HIGH  
**Evidence:** `D03-07:16,44,52,83,110` requires `scripts/e2e/capstone-verdict.sh` and `capstone-verdict.json`; `test -e scripts/e2e/capstone-verdict.sh` and `test -e .tmp/maestro-reference-flow/capstone-verdict.json` both fail. `sprint-goal-state.json:45,83,211` substitutes the harness run and prose claims for the missing verifier. No `gate-results.json` exists in the Sprint-20 directory.  
**Impact:** The go/no-go gate cannot independently derive green/red from the evidence or be replayed by a later sprint.

### H2 — Required CI-dispatched proof is absent
**Severity:** Critical  
**Confidence:** HIGH  
**Evidence:** `D03-05:28-30,50-54` and `D03-07:28-31,106-110` require a completed GitHub Actions self-hosted run and downloadable artifact. `gh` is not installed locally, no CI run metadata/artifact is present in the repository, and `sprint-goal-state.json:119-123` explicitly describes local files while admitting “CI upload step is ... responsibility.” Local evidence is not CI evidence.  
**Impact:** AC-1/AC-3 of D03-05, AC-3 of D03-06, and AC-3 of D03-07 remain unverified.

### H3 — Video artifact requirement fails on the only current run
**Severity:** High  
**Confidence:** HIGH  
**Evidence:** `D03-03:52,142`, `D03-05:53,112`, `D03-06:31,114`, and `D03-07:16,51` require non-empty `reference-flow.mov`. Current directory contains no `reference-flow.mov`, only `reference-flow.mov.sb-99cdcea4-JsW9Yj` (270 MB). `video.log` says `Error starting video recorder ... Resource busy` / `Host recording is already in progress`. The workflow uploads `.tmp/maestro-reference-flow/`, but the harness still names the missing exact file.  
**Impact:** The local run cannot satisfy the JUnit+log+video gate; a successful JUnit is not sufficient.

### H4 — D03-06 was marked complete with its required review/test surfaces missing
**Severity:** High  
**Confidence:** HIGH  
**Evidence:** `D03-06:16,28-31,50-54,83-87` requires `docs/ci/D03-06-adversarial-review.md` and `tests/ci/fork-safety.test.ts`. Both paths are absent (`git ls-files` and filesystem checks). `actionlint` is unavailable, so the required captured actionlint result cannot be reconstructed.  
**Impact:** Fork-vs-same-repository expression behavior and the trust-boundary review are not standing, replayable checks.

### H5 — Required real Zero durable-read integration test is absent
**Severity:** High  
**Confidence:** HIGH  
**Evidence:** `S-COLDBOOT-02:16,88-95,167-169,214` requires `services/platform/tests/integration/sprint20-reference-zero-durable.test.ts`; that file does not exist. `sprint-goal-state.json:34,184` acknowledges this as a follow-up while still marking AC-2 satisfied/non-blocking. The existing builder-query test only checks source shape; its own run printed two real-substrate cases as `SKIPPED` without `COLDBOOT_IT=1`.  
**Impact:** The central claim “durable message is observable via Zero, not the response body” is not independently tested.

### H6 — Required testID uniqueness audit is absent
**Severity:** High  
**Confidence:** HIGH  
**Evidence:** `S-COLDBOOT-03:93-99,181-182` requires `tests/integration/sprint20-testid-audit.test.tsx`; the file is absent. The screen source at `app/(drawer)/chat/reference.tsx:91-110` renders the assistant selector per agent row, so a uniqueness assertion is material rather than optional.  
**Impact:** The Maestro selector contract is unproven; the current flow only proves one observed screen state.

### H7 — D03-04’s primary live zero-cache test is fictional/unresolvable
**Severity:** High  
**Confidence:** HIGH  
**Evidence:** `D03-04:48-55,169-176,219` verifies `services/platform/tests/integration/nonprod-namespace-zero-sync.test.ts`; that path is absent. The current `namespace-reset.json` proves only the CLI reset (`ok:true`, 122 tables truncated, three fixture IDs); it does not prove a live Zero query returns the reference conversation and zero messages.  
**Impact:** Reset determinism is proven at most against Postgres/CLI output, contrary to the task’s explicit real-zero-cache constraint.

### H8 — D03-01 boundary test is red/timeout on the current trunk
**Severity:** High  
**Confidence:** HIGH  
**Evidence:** Fresh command: `PLATFORM_IT=1 pnpm exec vitest run tests/integration/sprint20-maestro-harness.test.ts tests/integration/sprint20-maestro-harness-artifacts.test.ts tests/integration/sprint20-zero-builder-query.test.ts`. Result: **17 passed, 1 failed**; `tests/integration/sprint20-maestro-harness.test.ts:88` (“fails closed when EXPO_DEV_BUILD_PATH is not a real bundle”) timed out after 5000 ms. The test is the D03-01 AC-1/TC-2 contract (`D03-01:88-124,139-143`).  
**Impact:** The invalid-bundle red control is not green and may start reset/Zero work before rejecting the invalid bundle.

## MEDIUM Confidence Findings

### M1 — D03-03 AC-2 evidence check is too weak and current uninstall evidence is empty
**Severity:** Medium  
**Confidence:** HIGH  
**Evidence:** `D03-03:83,173-176,218-221` uses `rg -l . file1 file2`, which succeeds when only one file is non-empty. Current `simctl-uninstall.txt` is 0 bytes while `simctl-install.txt` is non-empty. This oracle does not prove both lifecycle operations were captured.

### M2 — D03-03 AC-3 verification regex cannot match the documented default mode
**Severity:** Medium  
**Confidence:** HIGH  
**Evidence:** `D03-03:113,175,220` verifies `"mode":"[a-z-]+"`; current `dev-client-setup.json` has `"mode":"server-list+already-running"`, containing `+`. The source at `run-maestro-reference-flow.sh:18-20,98-100` documents/produces the plus-bearing modes. The task’s own allowed values include `server-list+already-running`.

### M3 — D03-03 AC-4 has no executable forced-failure fixture or matching test
**Severity:** High  
**Confidence:** HIGH  
**Evidence:** `D03-03:139-163,176` requires `.tmp/forced-failure-flow.yaml` and a TC-4 test. `test -e .tmp/forced-failure-flow.yaml` fails; `rg 'TC-4|forced-failure|lsof' tests/integration/sprint20-maestro-harness-artifacts.test.ts` finds no TC-4 or forced-failure implementation. The current `cleanup()` (`run-maestro-reference-flow.sh:109-115`) is source-only evidence, not a forced failing-run proof.

### M4 — S-COLDBOOT-03 reset/idempotency contract is not actually wired to its named test
**Severity:** High  
**Confidence:** HIGH  
**Evidence:** `S-COLDBOOT-03:137-145,183,223` names a test title “namespace reset returns conversation 020 to a deterministic zero-message state.” `tests/integration/sprint20-coldboot-journey.test.ts` has no such title and its real-service cases return early unless `PLATFORM_IT=1 COLDBOOT_IT=1`; the AC command therefore cannot provide the promised deterministic reset proof.

### M5 — Current gate evidence is self-authored and internally inconsistent with the required artifact contract
**Severity:** Medium  
**Confidence:** HIGH  
**Evidence:** `sprint-goal-state.json:51,122,167` records `final.png` and a `.mov.sb-*` sidecar as if they satisfy the named screenshot/video artifacts; `SPRINT.md:45-50` calls step 4 “Check CI artifacts.” There is no downloadable artifact or independently recomputed gate result. `SPRINT.md:20` also says `Progress: 0/10 tasks completed` while frontmatter/status and goal JSON say completed.

### M6 — `main` metadata is stale relative to the reviewed trunk
**Severity:** Medium  
**Confidence:** HIGH  
**Evidence:** Current `HEAD` is `e264c2c`; `sprint-goal-state.json:61,139` records protected/main SHA `b084dd5`. `e264c2c` is a later status-sync commit. This does not make the code bad, but it makes the stored completion proof non-binding for the reviewed trunk and should be regenerated after final verification.

## AC verdict table (all 10 task contracts)

Legend: **PASS** means source/evidence is sufficient for the narrow AC; **PARTIAL** means implementation exists but required real evidence/oracle is missing; **FAIL** means the required file/command/assertion is absent or contradicted by current evidence.

| Task | AC | Verdict | Evidence / reason |
|---|---|---|---|
| D03-01 | AC-1 build missing/invalid | ⚠️ PARTIAL | Missing-path case passes, but invalid-bundle test timed out; `tests/integration/sprint20-maestro-harness.test.ts:88-124`. |
| D03-01 | AC-2 backend fail-closed | ✅ PASS (narrow) | Three backend preflight cases passed in fresh Vitest run; source guards `run-maestro-reference-flow.sh:28-35`. |
| D03-02 | AC-1 runner online/probed | ⚠️ PARTIAL | `runner-status` implementation exists; no captured current runner status output/CI run. |
| D03-02 | AC-2 named simulator | ⚠️ PARTIAL | Provision script exists and has real `xcrun` probes; no task evidence artifact proving current runner state. |
| D03-02 | AC-3 installable Expo build | ⚠️ PARTIAL | Build script validates `.app`/`Info.plist`; stored path is a D03-02 worktree path, not a current-trunk evidence artifact. |
| D03-02 | AC-4 missing simulator/build fails | ⚠️ PARTIAL | Negative tests/source exist, but no completed live runner-status capture. |
| D03-03 | AC-1 boot/install/run/capture | ❌ FAIL | JUnit/screenshot exist; exact `reference-flow.mov` absent and recorder log reports resource-busy failure. |
| D03-03 | AC-2 stale install prevented | ⚠️ PARTIAL | terminate/uninstall/install source exists; uninstall evidence is empty and TC oracle does not require both files non-empty. |
| D03-03 | AC-3 dev-client mode recorded | ❌ FAIL | Current mode contains `+`, but documented `rg` oracle only permits `[a-z-]+`; no valid command result. |
| D03-03 | AC-4 failure cleanup/artifacts | ❌ FAIL | Forced-flow fixture and TC-4 implementation absent; video cleanup is contradicted by recorder failure. |
| D03-04 | AC-1 live Zero read | ❌ FAIL | Required `nonprod-namespace-zero-sync.test.ts` is absent; reset JSON is CLI-only. |
| D03-04 | AC-2 fingerprint idempotence | ⚠️ PARTIAL | reset emits fingerprint, but no captured consecutive-reset comparison in current evidence. |
| D03-04 | AC-3 `repl:status` membership | ⚠️ PARTIAL | command/source exists; no `repl:status` output artifact. |
| D03-04 | AC-4 production guard | ⚠️ PARTIAL | guard exists in CLI history/source, but no captured negative run in Sprint-20 evidence. |
| D03-05 | AC-1 real CI artifact bundle | ❌ FAIL | No CI run/artifact; local exact video missing. |
| D03-05 | AC-2 missing build preflight | ⚠️ PARTIAL | fail-closed source and test exist; no task-specific captured invocation. |
| D03-05 | AC-3 failure uploads | ❌ FAIL | No deliberate broken-flow evidence; required video is absent. |
| D03-05 | AC-4 actionlint/pins/permissions | ⚠️ PARTIAL | SHA/static audit passes; `actionlint` unavailable and no captured review artifact. |
| D03-06 | AC-1 fork safety test | ❌ FAIL | `tests/ci/fork-safety.test.ts` absent. |
| D03-06 | AC-2 review + actionlint evidence | ❌ FAIL | `docs/ci/D03-06-adversarial-review.md` absent; actionlint unavailable. |
| D03-06 | AC-3 real artifact review | ❌ FAIL | No CI artifact; exact video absent. |
| D03-06 | AC-4 secrets/permissions/retry audit | ⚠️ PARTIAL | Workflow visibly has `contents: read`, concurrency, nonprod secret names, no `continue-on-error:true`; no required review capture. |
| D03-07 | AC-1 local capstone green | ❌ FAIL | `capstone-verdict.sh` and `capstone-verdict.json` absent; exact video absent. |
| D03-07 | AC-2 missing build red verdict | ❌ FAIL | Capstone verifier is absent, so no red verdict can be produced. |
| D03-07 | AC-3 CI capstone reproduction | ❌ FAIL | Capstone script and CI run/artifact are absent. |
| S-COLDBOOT-01 | AC-1 cold boot without Convex URL | ✅ PASS (narrow) | Current JUnit has `failures="0"`; flow asserts `chat-screen` (`.e2e/maestro/reference-flow.yaml:79-84`); layout uses Zero only (`app/_layout.tsx:3-5,133-160`). Env provenance is only local launcher text. |
| S-COLDBOOT-01 | AC-2 Convex-free boot module | ✅ PASS | `app/_layout.tsx:3-5,135-159`; no Convex import/provider/client there and `ZeroProvider` remains. |
| S-COLDBOOT-01 | AC-3 type clean | ⚠️ PARTIAL | No fresh `pnpm tsgo --noEmit` output in Sprint-20 evidence. |
| S-COLDBOOT-02 | AC-1 round-trip reply | ⚠️ PARTIAL | JUnit/screenshot and source flow exist; no independent durable-via-Zero integration proof and no named screenshot file in artifact root. |
| S-COLDBOOT-02 | AC-2 durable Zero read | ❌ FAIL | Required `services/platform/tests/integration/sprint20-reference-zero-durable.test.ts` absent. |
| S-COLDBOOT-02 | AC-3 Convex-free Hono + Zero path | ✅ PASS | `app/(drawer)/chat/reference.tsx:1,6,29-79` uses Zero query and `/api/chat-runs`, with no `convex/react`. |
| S-COLDBOOT-03 | AC-1 fresh Postgres→Zero round-trip | ⚠️ PARTIAL | Current local JUnit/screenshot/reset exist, but no captured DB query/second-run pair and exact CI artifact proof. |
| S-COLDBOOT-03 | AC-2 unique selector audit | ❌ FAIL | Required `tests/integration/sprint20-testid-audit.test.tsx` absent. |
| S-COLDBOOT-03 | AC-3 deterministic reset test | ❌ FAIL | Named test title/file contract is absent; current test only conditionally runs unrelated real-service cases. |

## Human Testing Gate review

| Step | Runnable as documented? | Evidence |
|---|---|---|
| 1. Cold boot/open app | YES, but only local | `reference-flow.yaml:23-84`; current JUnit `tests=1 failures=0`; `final.png` exists. |
| 2. Send through fleet/Postgres | PARTIAL | Flow sends the literal (`reference-flow.yaml:87-91`), but no captured DB query artifact; completion JSON is a claim. |
| 3. Observe Zero-synced reply/screenshot | PARTIAL | Flow waits for `chat-assistant-message` and takes screenshot; no independent Zero durable test; screenshot is `final.png`, not a root `reference-chat-reply` file. |
| 4. Check CI artifacts | NO | No CI run/artifact; local recorder failed and exact `.mov` is missing. |
| 5. Missing build fails closed | PARTIAL | Source guards and most tests pass; invalid-bundle test timed out. |
| 6. Reset known seed | PARTIAL | `namespace-reset.json` says `ok:true`; the required live Zero read test is absent. |

## Stub / test-theatre findings

- **HIGH — Test theatre / proxy completion:** `sprint-goal-state.json` converts local JUnit + prose into six green layers without the required CI artifact, capstone verdict, or live Zero test.
- **HIGH — Unowned oracle:** D03-04, S-COLDBOOT-02, S-COLDBOOT-03 and D03-07 each name test/script paths that do not exist.
- **HIGH — Artifact theatre risk:** a 270 MB `.mov.sb-*` sidecar is reported as video evidence even though the required `.mov` was never created and `video.log` reports recorder failure.
- **MEDIUM — Weak lifecycle oracle:** `rg -l . fileA fileB` can pass with only one non-empty file.
- **MEDIUM — Skippable integration lane:** `sprint20-zero-builder-query.test.ts` and `sprint20-coldboot-journey.test.ts` explicitly skip real-substrate cases unless `COLDBOOT_IT=1`; a green default run is not e2e evidence.

## Recommendations

1. Reopen Sprint 20 / block downstream Sprints 22–26 and 29 until all FAIL/PARTIAL ACs are remediated.
2. Add and run the missing `capstone-verdict.sh`, D03-06 review/test, durable Zero integration test, testID audit, and reset/forced-failure fixtures.
3. Fix video recording so the exact `reference-flow.mov` is finalized, then run a deliberately broken flow and verify JUnit failure plus screenshot/log/video cleanup.
4. Dispatch `ci-e2e.yml` on the registered self-hosted runner, download the run artifact, and preserve command output and artifact hashes in a review/gate-results artifact.
5. Regenerate `sprint-goal-state.json` and `gate-results.json` from the final current `main` SHA only after independent recomputation.

## Metadata

- **Agents consulted:** current-project review panel was dispatched read-only for RN, GitHub Actions, Mastra/real-service, and general red-hat tracks; this report also contains an independent deterministic audit.
- **Confidence framework:** HIGH = directly contradicted/missing/failed; MEDIUM = implementation exists but replayable evidence is missing.
- **Commands run:** filesystem/oracle checks; workflow pin/secret static audit; `PLATFORM_IT=1 pnpm exec vitest run tests/integration/sprint20-maestro-harness.test.ts tests/integration/sprint20-maestro-harness-artifacts.test.ts tests/integration/sprint20-zero-builder-query.test.ts` (17 passed, 1 failed timeout).
- **Report status:** `needs-revision`.
