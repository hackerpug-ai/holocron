# US-044: Initialize Convex project with Agent + Workflow components

> Status: ✅ Completed
> Completed: 2026-03-05T12:00:00Z
> Commit: pending-verification
> Reviewer: orchestrator

> > Task ID: US-044
> > Type: task
> > Priority: P0
> > Appetite Fit: medium (fits)
> > Assignee: convex-implementer
> > PRD Version: 5.0.0
> > PRD Section: §12 UC-BM-01
> > Stability: stable

> ## CRITICAL CONSTRAINTS

> MUST: Install Convex dependencies: `convex`, `@convex-dev/react`, `@convex-dev/agent`, `@convex-dev/workflow`, `@ai-sdk/openai`
> MUST: Initialize Convex project with `npx convex dev`
> MUST: Configure ConvexProvider in app root layout (alongside existing Supabase)
> NEVER: Remove Supabase yet - run in parallel during POC
> STRICTLY: Use existing project structure

> ## SPECIFICATION

> **Objective:** Initialize Convex project with Agent + Workflow components for POC validation
> **Success looks like:** `npx convex dev` runs without errors, ConvexProvider wraps app without breaking existing functionality

> ## GUARDRAILS

> ### WRITE-ALLOWED
> - package.json (MODIFY - add dependencies)
> - convex/_generated/api.ts (AUTO-GENERATED)
> - convex/schema.ts (NEW)
> - convex/components.config.ts (NEW)
> - app/_layout.tsx (MODIFY - add ConvexProvider)

> ### WRITE-PROHIBITED
> - lib/supabase.ts - do not modify during POC
> - Any Supabase query files - preserve for A/B testing

> ## VERIFICATION GATES

> Gate 1 (Install): `pnpm add convex @convex-dev/react @convex-dev/agent @convex-dev/workflow @ai-sdk/openai`
> Gate 2 (Init): `npx convex dev` initializes project
> Gate 3 (Build): `pnpm build` succeeds without errors

> ## TEST EVIDENCE (MANDATORY)

> After all gates pass, implementer MUST save test evidence:
> ```bash
> mkdir -p ./.tmp/evidence
> pnpm build 2>&1 | tail -50 > ./.tmp/evidence/US-044.txt
> ```
> AUTOMATIC FAIL: Missing evidence file or mismatch with reviewer's test run.

> ## ACCEPTANCE CRITERIA

> | # | Given | When | Then | Verify |
> |---|-------|------|------|--------|
> | 1 | Project has no Convex dependencies | Run `pnpm add convex @convex-dev/react @convex-dev/agent @convex-dev/workflow @ai-sdk/openai` | Dependencies install without errors | `pnpm list convex` |
> | 2 | Convex not initialized | Run `npx convex dev` | Convex project created with deployment URL | `cat convex.json` |
> | 3 | ConvexProvider not in app | Add ConvexProvider to app/_layout.tsx | App starts without errors | `pnpm dev` |
> | 4 | Convex schema empty | Create convex/schema.ts with placeholder | Types generate without errors | `ls convex/_generated/api.ts` |

> ## DESIGN

> ### Pattern (from Convex docs)

> ```typescript
> // convex/components.config.ts
> import { defineSchema } from "convex/server";
> import { Agent } from "@convex-dev/agent";
> import { WorkflowManager } from "@convex-dev/workflow";
>
> export default defineSchema({
>   // Schema will be populated in US-048
> });
> ```

> ```typescript
> // app/_layout.tsx
> import { ConvexProvider, ConvexReactClient } from "convex/react";
>
> const convex = new ConvexReactClient(process.env.EXPO_PUBLIC_CONVEX_URL!);
>
> export default function RootLayout() {
>   return (
>     <ConvexProvider client={convex}>
>       {/* existing Supabase provider */}
>       <Slot />
>     </ConvexProvider>
>   );
> }
> ```

> ### Anti-pattern (DO NOT)

> ```typescript
> // BAD: Removing Supabase during POC
> // BAD: Using Convex before Agent/Workflow components are configured
> // BAD: Not setting EXPO_PUBLIC_CONVEX_URL environment variable
> ```

> ## CODING STANDARDS

> - **brain/docs/coding-standards/**: TypeScript strict mode, environment variable handling
> - **Convex docs**: Follow component configuration patterns

> ## DEPENDENCIES

> None - this is the first task in the epic

> ## NOTES

> This task sets up the Convex infrastructure for POC validation. The Agent and Workflow components will be configured in later tasks after schema is defined.
