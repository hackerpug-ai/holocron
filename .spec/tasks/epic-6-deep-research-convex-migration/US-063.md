# US-063: Remove `@supabase/supabase-js` and delete Supabase client files

> Task ID: US-063
> Type: task
> Priority: P1
> Appetite Fit: small (fits)
> Assignee: devops-engineer
> PRD Version: 5.0.0
> PRD Section: §12 UC-BM-06
> Stability: stable
> Dependencies: US-058, US-062

## CRITICAL CONSTRAINTS

MUST: Run `pnpm remove @supabase/supabase-js`
MUST: Delete lib/supabase.ts
MUST: Delete lib/supabase-admin.ts
MUST: Verify build succeeds after removal
MUST: Run full test suite
NEVER: Keep any Supabase-related code
STRICTLY: Clean up environment variables documentation

## SPECIFICATION
**Objective:** Complete removal of Supabase dependencies and files
**Success looks like:** Zero Supabase imports, build succeeds

## GUARDRAILS
### WRITE-ALLOWED
- package.json (MODIFY - remove dependency)
- lib/supabase.ts (DELETE)
- lib/supabase-admin.ts (DELETE)
- .env.example (MODIFY - remove Supabase vars)

### WRITE-PROHIBITED
- Any Convex files (already migrated)
- Any component files (already migrated)

## VERIFICATION GATES
Gate 1 (Remove): Package removed from package.json
Gate 2 (Delete): Supabase client files deleted
Gate 3 (Build): TypeScript compiles without errors
Gate 4 (Tests): All tests pass

## TEST EVIDENCE (MANDATORY)
```bash
mkdir -p ./.tmp/evidence
echo "=== package.json ===" > ./.tmp/evidence/US-063.txt
grep "supabase" package.json 2>&1 >> ./.tmp/evidence/US-063.txt || echo "No supabase in package.json" >> ./.tmp/evidence/US-063.txt
echo "=== lib files ===" >> ./.tmp/evidence/US-063.txt
ls lib/supabase*.ts 2>&1 >> ./.tmp/evidence/US-063.txt
echo "=== build ===" >> ./.tmp/evidence/US-063.txt
pnpm build 2>&1 | tail -20 >> ./.tmp/evidence/US-063.txt
```

## ACCEPTANCE CRITERIA
| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | @supabase/supabase-js installed | Run pnpm remove | Package removed | Check package.json |
| 2 | lib/supabase.ts exists | Delete file | File deleted | `ls lib/supabase.ts` fails |
| 3 | lib/supabase-admin.ts exists | Delete file | File deleted | `ls lib/supabase-admin.ts` fails |
| 4 | Files deleted | Build project | Build succeeds | `pnpm build` exits 0 |
| 5 | Build succeeds | Run tests | Tests pass | `pnpm test` exits 0 |

## DESIGN
### Pattern (Clean Removal)
```bash
# Step 1: Remove package
pnpm remove @supabase/supabase-js

# Step 2: Delete client files
rm lib/supabase.ts
rm lib/supabase-admin.ts

# Step 3: Update environment documentation
# Remove from .env.example:
# EXPO_PUBLIC_SUPABASE_URL=
# EXPO_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=

# Step 4: Verify build
pnpm build

# Step 5: Run tests
pnpm test

# Step 6: Verify no imports
grep -r "from.*supabase" --include="*.ts,*.tsx" .
# Should return nothing
```

### Anti-pattern (DO NOT)
```bash
# BAD: Keeping files "just in case"
# git rm lib/supabase.ts
# DELETE THEM COMPLETELY

# BAD: Commenting out instead of deleting
# // import { supabase } from '@/lib/supabase';
# DELETE THE LINE ENTIRELY

# BAD: Keeping environment variables
EXPO_PUBLIC_SUPABASE_URL=  # DELETE THIS LINE
# Remove from all env files
```

## CODING STANDARDS
- **brain/docs/coding-standards/**: Clean code removal
- **Package management**: Dependency cleanup

## DEPENDENCIES
- US-058 (realtime hooks deleted)
- US-062 (skill migrated)

## NOTES
This is the final cleanup step. Make sure all previous tasks are completed before running this. After this task, there should be ZERO Supabase references in the codebase.
