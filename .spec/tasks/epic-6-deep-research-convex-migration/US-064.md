# US-064: Verify zero Supabase imports (`grep -r "supabase" src/`)

> Task ID: US-064
> Type: task
> Priority: P1
> Appetite Fit: small (fits)
> Assignee: devops-engineer
> PRD Version: 5.0.0
> PRD Section: §12 UC-BM-06
> Stability: stable
> Dependencies: US-063

## CRITICAL CONSTRAINTS

MUST: Search entire codebase for "supabase" string
MUST: Check all file types: .ts, .tsx, .js, .jsx, .json, .md
MUST: Report any found references
MUST: Fix or remove any remaining references
NEVER: Skip any directories
STRICTLY: Achieve zero grep results

## SPECIFICATION
**Objective:** Comprehensive verification that no Supabase references remain
**Success looks like:** `grep -r "supabase" .` returns zero results

## GUARDRAILS
### WRITE-ALLOWED
- Any files containing Supabase references (FIX or DELETE)

### WRITE-PROHIBITED
- Adding new Supabase code

## VERIFICATION GATES
Gate 1 (Source): grep returns no results for "supabase" in src/
Gate 2 (Config): No Supabase config in any file
Gate 3 (Docs): Documentation updated to remove Supabase references
Gate 4 (Final): Full codebase search returns zero

## TEST EVIDENCE (MANDATORY)
```bash
mkdir -p ./.tmp/evidence
echo "=== Searching src/ ===" > ./.tmp/evidence/US-064.txt
grep -r "supabase" src/ --include="*.ts,*.tsx,*.js,*.jsx" 2>&1 >> ./.tmp/evidence/US-064.txt || echo "No results in src/" >> ./.tmp/evidence/US-064.txt
echo "=== Searching hooks/ ===" >> ./.tmp/evidence/US-064.txt
grep -r "supabase" hooks/ --include="*.ts,*.tsx" 2>&1 >> ./.tmp/evidence/US-064.txt || echo "No results in hooks/" >> ./.tmp/evidence/US-064.txt
echo "=== Searching lib/ ===" >> ./.tmp/evidence/US-064.txt
grep -r "supabase" lib/ --include="*.ts" 2>&1 >> ./.tmp/evidence/US-064.txt || echo "No results in lib/" >> ./.tmp/evidence/US-064.txt
echo "=== Searching brain/skills/holocron/ ===" >> ./.tmp/evidence/US-064.txt
grep -r "supabase" brain/skills/holocron/ 2>&1 >> ./.tmp/evidence/US-064.txt || echo "No results in skill/" >> ./.tmp/evidence/US-064.txt
```

## ACCEPTANCE CRITERIA
| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | US-063 complete | Search src/ | No results | grep output |
| 2 | US-063 complete | Search hooks/ | No results | grep output |
| 3 | US-063 complete | Search lib/ | No results | grep output |
| 4 | US-062 complete | Search skill/ | No results | grep output |
| 5 | All searched | Final check | Zero total | Comprehensive grep |

## DESIGN
### Pattern (Comprehensive Search)
```bash
# Search all TypeScript/JavaScript files
grep -r "supabase" . --include="*.ts,*.tsx,*.js,*.jsx"

# Search JSON files (dependencies)
grep -r "supabase" . --include="*.json"

# Search documentation
grep -r "supabase" . --include="*.md"

# Search environment files
grep -r "supabase" . --include=".env*"

# Expected: Zero results from all searches

# If results found, categorize:
# 1. Import statements → Delete
# 2. Type references → Replace with Convex types
# 3. Config references → Remove
# 4. Documentation → Update to Convex
```

### Anti-pattern (DO NOT)
```bash
# BAD: Only searching src/
grep -r "supabase" src/
# Must search entire codebase

# BAD: Ignoring case sensitivity
grep -r "supabase" .
# Should also check: Supabase, SUPABASE

# BAD: Skipping node_modules
# node_modules is usually excluded by default, but verify

# BAD: Not checking skill files
# Skills may have Supabase references
```

## CODING STANDARDS
- **Clean code**: No dead code or orphaned references
- **Migration completeness**: Full dependency removal

## DEPENDENCIES
- US-063 (package and files removed)

## NOTES
This is the verification gate for the entire migration. If any Supabase references are found, they MUST be removed before this task can be marked complete. Document any exceptions (e.g., migration analysis files kept for reference).
