# US-046: Implement hybrid search in Convex and benchmark vs Supabase

> Task ID: US-046
> Type: task
> Priority: P0
> Appetite Fit: large (fits)
> Assignee: convex-implementer
> PRD Version: 5.0.0
> PRD Section: §12 UC-BM-01
> Stability: stable
> Dependencies: US-045

## CRITICAL CONSTRAINTS

MUST: Achieve ≥90% search quality match with Supabase pgvector
MUST: Implement vector search using Convex native capabilities
MUST: Implement full-text search for keyword matching
MUST: Combine vector + FTS for hybrid search
NEVER: Accept <90% match - this is GO/NO-GO criteria
STRICTLY: Document benchmark methodology and results

## SPECIFICATION

**Objective:** Implement hybrid search in Convex and benchmark against Supabase
**Success looks like:** Convex hybrid search returns ≥90% matching results compared to Supabase

## GUARDRAILS

### WRITE-ALLOWED
- convex/documents/queries.ts (MODIFY - add hybridSearch, vectorSearch, fullTextSearch)
- convex/documents/search.ts (NEW - search implementations)
- scripts/benchmark-search.ts (NEW - A/B comparison)
- .tmp/benchmark-results.json (NEW - output)

### WRITE-PROHIBITED
- lib/supabase.ts - preserve for comparison
- Any production code paths - POC only

## VERIFICATION GATES

Gate 1 (Vector Search): Convex vector search returns results
Gate 2 (FTS): Convex full-text search returns results
Gate 3 (Hybrid): Combined search works correctly
Gate 4 (Benchmark): ≥90% match achieved

## TEST EVIDENCE (MANDATORY)

```bash
mkdir -p ./.tmp/evidence
pnpm run benchmark-search 2>&1 | tail -100 > ./.tmp/evidence/US-046.txt
```

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | Documents with embeddings | Query vector search | Returns semantically similar results | `npx convex run documents:vectorSearch` |
| 2 | Documents with text content | Query full-text search | Returns keyword matches | `npx convex run documents:fullTextSearch` |
| 3 | Search query | Run hybrid search | Returns combined vector + FTS results | `npx convex run documents:hybridSearch` |
| 4 | 10 test queries | Compare Convex vs Supabase | ≥90% overlap in top 10 results | Benchmark script output |

## DESIGN

### Pattern (Convex hybrid search)

```typescript
// convex/documents/queries.ts
export const hybridSearch = query({
  args: {
    query: v.string(),
    limit: v.optional(v.number()),
  },
  handler: async (ctx, { query, limit = 10 }) => {
    // 1. Generate embedding for query using OpenAI
    // 2. Vector search for semantic similarity
    // 3. Full-text search for keyword matches
    // 4. Merge and rerank results
    // 5. Return top N with scores
  },
});

export const vectorSearch = query({
  args: {
    embedding: v.array(v.float64()),
    limit: v.optional(v.number()),
  },
  handler: async (ctx, { embedding, limit = 10 }) => {
    // Use Convex vector search API
  },
});

export const fullTextSearch = query({
  args: {
    query: v.string(),
    limit: v.optional(v.number()),
  },
  handler: async (ctx, { query, limit = 10 }) => {
    // Use Convex searchIndex for FTS
  },
});
```

### Anti-pattern (DO NOT)

```typescript
// BAD: Not using Convex native vector search
// Implementing custom vector similarity in JS

// BAD: Ignoring FTS component
// Hybrid requires both vector AND keyword matching
```

## CODING STANDARDS

- **brain/docs/coding-standards/**: Performance benchmarking patterns
- **Convex docs**: Vector search API, searchIndex patterns

## DEPENDENCIES

- US-045 (documents table with embeddings)

## NOTES

This is the critical GO/NO-GO gate. If benchmark fails to achieve ≥90% match, escalate to user before proceeding with migration. Document all benchmark queries and results.
