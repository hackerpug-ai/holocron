# US-051: [DESIGN] Deep Research Confirmation Card Component

> Task ID: US-051
> Type: feature:design
> Priority: P0
> Appetite Fit: low (fits appetite)
> Assignee: react-native-ui-implementer
> PRD Version: 3.1.0
> Stability: stable

## CRITICAL CONSTRAINTS

### MUST
- Follow React Native Reusables component patterns
- Create Storybook story with all variants
- Use semantic color tokens via NativeWind classes

### NEVER
- Hardcode UI values - use Tailwind classes
- Import Text from react-native - use @/components/ui/text

### STRICTLY
- Component must work standalone with mock data

## SPECIFICATION

**Objective:** Create confirmation card shown when user initiates `/deep-research`

**Success looks like:** User sees topic, max iterations, estimated duration, session ID, and warning about longer processing time

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | User submits `/deep-research quantum computing` | Confirmation card renders | Shows topic 'quantum computing', default iterations (5), and estimated duration | `story renders with Default variant` |
| 2 | User submits `/deep-research --max 3 topic` | Confirmation card renders | Shows max iterations as 3 | `story renders with CustomIterations variant` |
| 3 | Confirmation card displays | Session ID is present | Session ID shown in monospace font | `story shows session ID badge` |
| 4 | Confirmation card displays | Warning about processing time | Warning icon and text visible | `story renders warning state` |

## GUARDRAILS

### WRITE-ALLOWED
- `components/deep-research/DeepResearchConfirmationCard.tsx` (NEW)
- `components/deep-research/DeepResearchConfirmationCard.stories.tsx` (NEW)

### WRITE-PROHIBITED
- `supabase/**` - backend separate task
- `hooks/useDeepResearch.ts` - needs integration task

## DESIGN

### References
- `.spec/prd/08-uc-deep-research.md`
- `components/ResultCard.tsx` (reference pattern)
- `components/ResearchProgress.tsx` (reference pattern)

### Interaction Notes
- Show session ID as copyable badge
- Display iteration count and estimated time
- Include warning about longer processing
- Match existing card styling from ResultCard

### Code Pattern

Source: `components/ResultCard.tsx:1-120`

```typescript
// See ResultCard and ResearchProgress component structure
export function ResultCard({ title, children, ...props }) {
  return (
    <View className={cn("bg-card border rounded-lg p-4", props.className)}>
      <Text variant="titleMedium">{title}</Text>
      {children}
    </View>
  )
}
```

### Anti-pattern (DO NOT)

Do NOT create confirmation card without semantic token support via NativeWind

## CODING STANDARDS

- **brain/docs/REACT-RULES.md**:
  - Text from @/components/ui/text
  - No hardcoded values
  - cn() for conditional classes

## DEPENDENCIES

No task dependencies.

## APPETITE FIT

- **Fits Appetite**: Yes
- **Complexity**: low

## METADATA

- **Source PRD Version**: 3.1.0
- **Source PRD Section**: UC-DR-01
- **Stability**: stable
- **Change Trigger**: prd_update
- **Domain**: react-native
