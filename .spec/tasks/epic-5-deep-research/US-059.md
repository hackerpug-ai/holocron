# US-059: [INTEGRATION] Wire Resume Session List to Chat

> Task ID: US-059
> Type: feature:integration
> Priority: P5
> Appetite Fit: low (fits appetite)
> Assignee: react-native-ui-implementer
> PRD Version: 3.1.0
> Stability: stable

## CRITICAL CONSTRAINTS

### MUST
- Use ResumeSessionList component from US-052
- Wire to real resume handler from US-056
- Handle empty state and selection

### NEVER
- Create new UI components - use DESIGN task output
- Bypass type checking

### STRICTLY
- Must pass app startup verification

## SPECIFICATION

**Objective:** Integrate resume session list component with real backend data

**Success looks like:** User types `/resume`, sees real incomplete sessions, can select to resume

## ACCEPTANCE CRITERIA

| # | Given | When | Then | Verify |
|---|-------|------|------|--------|
| 1 | User sends `/resume` | Incomplete sessions exist | ResumeSessionList renders with real session data | `pnpm dev and test in app` |
| 2 | User taps session card | Selection handler fires | Resume command sent with session ID | `manual test selection flow` |
| 3 | No incomplete sessions | Resume list renders | Empty state shown | `manual test with no sessions` |
| 4 | Session resumed | Backend confirms | Research progress continues from checkpoint | `manual test resume flow` |

## GUARDRAILS

### WRITE-ALLOWED
- `components/chat/MessageBubble.tsx` (MODIFY)
- `lib/types/chat.ts` (MODIFY)
- `hooks/useChatSend.ts` (MODIFY)

### WRITE-PROHIBITED
- `components/deep-research/*` - use existing DESIGN output
- `supabase/functions/**` - use existing BACKEND output

## DESIGN

### References
- `components/chat/MessageBubble.tsx` (existing)
- `lib/types/chat.ts` (existing card types)

### Interaction Notes
- Add 'resume_session_list' to CardData union type
- Handle session selection via onPress callback

### Code Pattern

See existing card rendering in `MessageBubble.tsx`

```typescript
// Add to CardData union type
type CardData =
  | { card_type: 'resume_session_list', card_data: SessionListData }
  // ... other card types

// Handle in MessageBubble
{card?.card_type === 'resume_session_list' && (
  <ResumeSessionList
    sessions={card.card_data.sessions}
    onSelect={(sessionId) => sendResumeCommand(sessionId)}
  />
)}
```

### Anti-pattern (DO NOT)

Do NOT duplicate session list logic - reuse DESIGN component

## CODING STANDARDS

- **brain/docs/REACT-RULES.md**:
  - Type safety
  - Error handling
  - Empty states

## DEPENDENCIES

- US-052: Resume Session List Design component
- US-056: Resume Command Handler

## APPETITE FIT

- **Fits Appetite**: Yes
- **Complexity**: low

## METADATA

- **Source PRD Version**: 3.1.0
- **Source PRD Section**: UC-DR-03
- **Stability**: stable
- **Change Trigger**: prd_update
- **Domain**: react-native
