# Sprint: imp-migrate-repo-monorepo-structure-1788024693

> Source: ~/.config/brain/improvements/imp-migrate-repo-monorepo-structure-1788024693.json (binding)
> Chosen option: moderate — Put every module under packages, including the phone app
> LOC budget: 2800
> Generated by: /kb-improvement-tasks-plan
> Operator split: one task file per package, sequenced by dependency, with strict real-install tests

## Objective

Move every installable module under `packages/`, add a web placeholder, make the repo root a thin pnpm workspace orchestrator, and add a Package map to root `AGENTS.md`. Fulcrum stays in-process inside platform. Product behavior unchanged.

Target layout:

```text
packages/web           # placeholder (no product code)
packages/docs-reader   # from services/worker-docs-reader
packages/mcp           # from holocron-mcp
packages/platform      # from services/platform (Fulcrum lives here)
packages/mobile        # Expo app currently at repo root
```

## Dependency graph

Leaf packages first (fewest callers, no other package depends on their path). Platform next (Docker/launchd/bin). Mobile last (it currently IS the repo root).

```text
PKG-01-WEB
  |-- PKG-02-DOCS-READER --\
  |-- PKG-03-MCP ---------- +--> PKG-04-PLATFORM --> PKG-05-MOBILE
```

| ID | Title | Assignee | Priority | Status | Depends on |
|----|-------|----------|----------|--------|------------|
| PKG-01-WEB | Create packages/web placeholder and enroll `packages/*` | bun-implementer | P1 | ⬜ Pending | — |
| PKG-02-DOCS-READER | Move docs reader into packages/docs-reader | cloudflare-workers-implementer | P1 | ⬜ Pending | PKG-01-WEB |
| PKG-03-MCP | Move MCP server into packages/mcp | mcp-implementer | P1 | ⬜ Pending | PKG-01-WEB |
| PKG-04-PLATFORM | Move backend (incl. Fulcrum) into packages/platform | mastra-implementer | P1 | ⬜ Pending | PKG-01-WEB |
| PKG-05-MOBILE | Move Expo into packages/mobile, thin root, write AGENTS.md map, prove both runtimes | react-native-ui-implementer | P1 | ⬜ Pending | PKG-02-DOCS-READER, PKG-03-MCP, PKG-04-PLATFORM |

PKG-02, PKG-03, and PKG-04 may run in parallel after PKG-01. PKG-05 is last.

## Binding ACs (do not renumber)

- AC-1: packages/ holds mobile, platform, mcp, docs-reader, and web; no active product package.json remains at repo root except the thin workspace orchestrator
- AC-2: Expo app, components, hooks, lib, screens, assets, app.json, eas.json, and metro live under packages/mobile; root package.json only orchestrates
- AC-3: pnpm-workspace.yaml is packages/* only; pnpm install from root links all five package entries
- AC-4: AGENTS.md Package map lists packages/mobile, packages/platform (Fulcrum in-process), packages/mcp, packages/docs-reader, packages/web placeholder — paths match disk
- AC-5: Path rewrite covers former services/platform and holocron-mcp callers plus Expo-root assumptions; expo start from packages/mobile and platform server both succeed; product behavior unchanged

AC-1 is fully proven only after PKG-05. Earlier tasks prove their slice with real `pnpm list -r`.

## Sprint-wide prohibitions

- No mocks, stubs, or fake install success. `pnpm install` must be a real install against the workspace lockfile.
- No Fulcrum package split. No turbo/nx. No extracting shared libs from mobile.
- No claiming Expo still works without running it from `packages/mobile`.
- Do not leave `services/platform` or `holocron-mcp` as live package roots after their move tasks.

## Shared quality gates (every task)

```bash
pnpm install
pnpm list -r --depth 0
git diff --check
```

## Notes

Authoritative contract: `~/.config/brain/improvements/imp-migrate-repo-monorepo-structure-1788024693.json`. Do not amend ACs here. Union of task file `> Files:` lists must stay inside the binding `files_in_scope`.
