Run `/kb-sprint-tasks-plan --sprint-folder /Users/inference1/Projects/holocron/.spec/prds/mk6-migration/tasks/sprint-24-full-rn-app-rewrite-off-convex-onto-zero --only GATE-FIX-001,REDHAT-FIX-UI-001`.

Expand the two conductor-added remediation rows from the authoritative evidence in the sprint's `Remediation (review + gate findings)` section. Preserve the real failure evidence: Maestro runs never reached `chat-screen`; the prior gate claim did not execute rename; S-REWRITE-06 found 55 hardcoded hex colors, only 4/14 drawer routes with `ScreenLayout`, and missing testID/accessibility coverage. Do not loosen scenarios or author a pass claim. Return the expanded task contracts and commit the spec changes to main per the skill.


LANDING CONTRACT (non-negotiable — a task is DONE only when its reviewed commit is an ancestor of the
base branch — the trunk kb-orchestrate merges to, default `main`, configurable via `shared.base-branch`):
- Committed is NOT done. Reviewed is NOT done. Done = landed on the base branch.
- Do ALL branch work in isolated worktrees. The repo's PRIMARY checkout stays on the base branch at
  all times — never `git checkout`/`switch` it onto any other branch.
- The moment a task's review is APPROVED, merge it into the base branch via kb-orchestrate
  `references/merge-to-main.sh` (flock-serialized; orchestrator-only — subagents never merge).
- Unreviewed work is NEVER merged. Route it through review first; if review cannot happen, list the
  branch explicitly as STRANDED in your final summary — never leave it silently.
- Before declaring your goal complete, run
  `~/Projects/brain/tools/landing/assert-landed.sh <project-dir>` — it must exit 0 (zero stranded
  tasks, zero orphan sprint branches). If it exits 1, landing IS your remaining work.
